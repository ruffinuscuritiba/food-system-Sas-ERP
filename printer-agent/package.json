{
  "name": "foodsaas-printer-agent",
  "version": "1.0.0",
  "description": "FoodSaaS Printer Agent — ESC/POS polling agent for thermal printers",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "pm2": "pm2 start index.js --name printer-agent --restart-delay=3000"
  },
  "dependencies": {
    "escpos": "^3.0.0-alpha.6"
  },
  "engines": {
    "node": ">=18"
  }
}
