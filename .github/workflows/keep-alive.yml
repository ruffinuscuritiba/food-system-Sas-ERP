# Keep-alive migrado para Vercel Cron Jobs (vercel.json)
# GitHub Actions tem delay de até 30 min — não confiável para manter Render ativo
# Este workflow foi desabilitado intencionalmente.
name: Keep Backend Alive (DESABILITADO)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Keep-alive agora roda via Vercel Cron (vercel.json)"
