# CLAUDE.md — Food System SaaS ERP

Memória técnica resumida do projeto. Consultar **antes** de explorar o código para evitar leituras repetidas.

⚠️ **Problema de WhatsApp/Kely/IA não respondendo?** Antes de investigar do zero, ver [`IA.md`](./IA.md) — checklist rápido + estado atual conhecido (instância Evolution, connectionId, env vars). Esse arquivo existe porque o estado quebra silenciosamente toda vez que a Kely é reconectada via QR.

---

## Stack

**Backend** (`/backend`)
- NestJS 11 + TypeScript 6
- Prisma 5.22 + PostgreSQL (Supabase em prod)
- JWT (`@nestjs/jwt`) + Passport (`passport-jwt`)
- Socket.IO 4 (`@nestjs/websockets`)
- bcrypt (hash 10 rounds)
- class-validator + class-transformer (ValidationPipe global, `whitelist` + `forbidNonWhitelisted` + `transform`)
- Cloudinary + Multer (uploads; fallback local em `/uploads`)
- Anthropic SDK + Google Generative AI (IA / Smart Import)
- Nodemailer, pdf-parse, xlsx, fast-xml-parser

**Frontend** (`/frontend`)
- Next.js 16.2 (App Router) + React 19.2
- Tailwind CSS 4 + shadcn + Radix UI + lucide-react
- Zustand 5 (auth store)
- @tanstack/react-query 5
- Axios (com interceptor JWT) + socket.io-client
- framer-motion, recharts, react-hot-toast, @hello-pangea/dnd, qrcode.react
- js-cookie (token também em cookie para middleware)

**Infra**
- Backend → Render (`render.yaml`)
- Frontend → Vercel (`vercel.json`)
- DB → Supabase PostgreSQL
- Node v24.15.0

---

## Arquitetura

**SaaS multi-tenant** com isolamento por `companyId` em todas as tabelas. Tenant é validado em duas camadas:
1. JWT carrega `companyId` (assinado no login)
2. `TenantGuard` valida que a empresa existe e não está bloqueada (`isBlocked`, `subscriptionStatus`)

**Backend** é monolito modular NestJS (módulos por domínio). Prefixo global `/api`. `PrismaService` tem readiness gate: requisições recebem 503 até `$connect` completar (frontend reusa em retry de 5xx).

**Real-time**: `SocketGateway` autentica via JWT no handshake e cria rooms por tenant (`company:${companyId}`). Eventos: `orderCreated`, `kitchenUpdate`, `dashboardUpdate`, `tableUpdate`, `onlineOrderPaid`. Clientes públicos (página de status do pedido) conectam sem token e não entram em room.

**CORS**: regex em `main.ts` libera `*.vercel.app` + localhost + domínio fixo.

---

## Estrutura de pastas

```
/backend
  prisma/
    schema.prisma        # 1151 linhas — todos os models
    seed.ts              # admin@teste.com / 123456
    migrations/
  src/
    main.ts              # bootstrap (CORS, ValidationPipe, prefix /api, readiness gate)
    app.module.ts        # registra módulos ativos
    common/
      decorators/        # @Roles, @CurrentUser
      guards/            # JwtAuthGuard, RolesGuard, TenantGuard
      filters/           # HttpExceptionFilter global
      enums/ interceptors/ pipes/ utils/ interfaces/
    database/            # PrismaModule + PrismaService (com readiness)
    socket/              # SocketGateway + SocketModule
    company-module/      # CRUD do catálogo de módulos contratados
    services/
      ai/                # Anthropic + Gemini
      storage/           # Cloudinary + local fallback
    modules/
      auth/              # Login, signup, JWT strategy, ModuleGuard, SubscriptionGuard
      users/ company/
      categories/ products/ pizza-borders/
      ingredients/ recipes/ stock/
      orders/ table-orders/ tables/
      cash/ financial/ payments/
      themes/            # CompanyTheme (cores, logo, banner)
      audit/             # AuditService (log de ações)
      notifications/ reports/ upload/
      ia/                # Chatbot + AiConversation
      loyalty/ coupons/  # Fidelidade + cupons
      smart-import/      # Cadastro inteligente (imagem/PDF/XML)
      delivery-config/ drivers/ tracking/
      online-orders/ chat/
      alerts/ super-admin/
  uploads/               # arquivos locais (fallback)

/frontend
  middleware.ts          # protege rotas (cookie `token`)
  app/                   # App Router — uma pasta por rota
    layout.tsx page.tsx
    login/ signup/ landing/ menu/   # públicas
    dashboard/ orders/ kitchen/ pdv/
    products/ categories/ ingredients/ recipes/ stock/
    tables/ order-status/ pedido/ pagamento/
    financeiro/ entregadores/ tracking/
    theme/ planos/ modulos/ pizza-borders/
    cadastro-inteligente/ bi/
    admin/ super-admin/
    api/                 # rotas API server-side do Next (proxies)
  components/
    ui/                  # shadcn base (button, card, input, label, table)
    layout/sidebar.tsx
    pdv/                 # CartSidebar, CategorySidebar, ProductCard, PizzaBuilder, PaymentModal
    dashboard/           # kpi-card, sales-chart
    chat/ tracking/ modulos/
    ClientShell.tsx ImageUpload.tsx role-guard.tsx
  hooks/                 # useKitchen, useOrders, useTables
  app/pdv/hooks/         # useCart, useCash, useCatalog (locais do PDV)
  lib/                   # api.ts, utils.ts, pdv-theme.ts
  services/              # api.ts (axios), socket.ts, env.ts, dashboard.service.ts, superAdminApi.ts
  stores/auth.store.ts   # Zustand (user/token + isAdmin/isKitchen/isCashier)
  public/
```

---

## Padrões frontend

- **App Router** do Next.js 16 (não confundir com Pages Router). `AGENTS.md` no `/frontend` avisa: API e convenções têm breaking changes em relação ao Next.js conhecido — ler `node_modules/next/dist/docs/` antes de editar.
- **Auth client**: `localStorage` (`token`, `user`) **+** cookie `token` (para `middleware.ts`). Store Zustand sincroniza ambos.
- **API client** (`services/api.ts`): axios com interceptor de request que injeta `Authorization: Bearer`, e interceptor de response que em 401 limpa storage + cookie e redireciona para `/login`.
- **Socket** (`services/socket.ts`): cliente Socket.IO com token no handshake.
- **Estilo**: Tailwind 4 + shadcn. UI components em `components/ui`. Cores tematizadas via `CompanyTheme` (primary/secondary/background/text + dark mode).
- **Estado**: Zustand para auth global; estado local com `useState`/hooks dedicados (PDV usa hooks isolados em `app/pdv/hooks`).
- **Rotas públicas** (sem auth, definidas no `middleware.ts`): `/login`, `/signup`, `/landing`, `/menu`, `/pagamento*`, `/pedido`.
- **PDV**: `useCart` (carrinho local), `useCatalog` (produtos/categorias), `useCash` (caixa aberto). Pizza tem builder próprio (`PizzaBuilder.tsx`) com bordas e tamanhos.

---

## Padrões backend

- **Estrutura por módulo**: `xxx.module.ts` + `xxx.controller.ts` + `xxx.service.ts` + `dto/`. Cada Module importa `PrismaModule`.
- **Controllers**: decoradores `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles(...)` na maioria dos endpoints. `companyId` lido SEMPRE de `req.user.companyId` (nunca do body).
- **Services**: recebem `companyId` por parâmetro e filtram **toda** query Prisma por `companyId`.
- **Validação**: DTOs com class-validator. ValidationPipe global rejeita campos não-whitelisted.
- **Transações Prisma**: ações que mudam estoque/financeiro usam `$transaction` com `IsolationLevel.Serializable` (ver `orders.service.ts`).
- **Auditoria**: `AuditService.log({ action, entity, entityId, userId, companyId, description, metadata })` chamado em login/signup e ações sensíveis.
- **Erros**: lançar `NotFoundException`, `UnauthorizedException`, `ForbiddenException`, `BadRequestException`. `HttpExceptionFilter` formata resposta.
- **Path alias**: `@/` → `src/`.

---

## Models Prisma (schema.prisma — 1151 linhas)

**Core multi-tenant**
- `Company` — tenant raiz. Campos: `plan`, `subscriptionStatus`, `dueDate`, `isBlocked`, `metaPixelId`, `googleAnalyticsId`.
- `User` — pertence a Company. `role: Role` (SUPER_ADMIN/ADMIN/MANAGER/CASHIER/KITCHEN/DELIVERY).
- `CompanyModule` — módulos ativos por tenant. Campos novos: `moduleSlug`, `status: ModuleStatus` (TRIAL/ACTIVE/INACTIVE/EXPIRED), `trialEndsAt`, `activatedAt`.
- `Module` — catálogo global de módulos contratáveis. `category: ModuleCategory` (OPERACAO/MARKETING/FINANCEIRO/AUTOMACAO), `price`, `isFree`, `benefits[]`.

**Catálogo**
- `Category` (companyId, name)
- `Product` — `costPrice`, `salePrice`, `profitMargin`, `trackStock`, `allowNegativeStock`, `deletedAt` (soft delete), `imageUrl`, `categoryId?`.
- `ProductSize` — múltiplos tamanhos com `price` próprio (`@@unique([productId, size])`).
- `PizzaBorder` + `PizzaBorderSize` — bordas com preço por `PizzaSize` (PEQUENA/MEDIA/GRANDE/FAMILIA/EXTRA_GRANDE).

**Pedidos**
- `Order` — `status: OrderStatus` (PENDING/CONFIRMED/PREPARING/READY/OUT_FOR_DELIVERY/DELIVERED/CANCELLED), `paymentMethod`, `subtotal`, `deliveryFee`, `driverFee?`, `driverId?`, `total`, timestamps por status (`confirmedAt`, `preparingAt`, `readyAt`, `outForDeliveryAt`, `deliveredAt`, `cancelledAt`, `completedAt`).
- `OrderItem` — snapshot: `productName`, `productSku`, `unitPrice`, `subtotal`, `productCost`, `cmv`, `profit`.
- `TableOrder` + `TableOrderItem` — pedidos por mesa.
- `Table` — `number`, `status: TableStatus` (FREE/OCCUPIED/RESERVED).

**Estoque**
- `Ingredient` — `stock`, `minimumStock`, `cost`, `averageCost`, `lastPurchaseCost`, `allowNegativeStock`, `deletedAt`.
- `Recipe` (1:1 com Product) + `RecipeItem` (ingrediente × quantidade).
- `StockMovement` — `type: StockMovementType` (ENTRY/EXIT/LOSS/INVENTORY/ADJUSTMENT/SALE/PURCHASE/PRODUCTION/TRANSFER/RETURN/CANCELLATION), `previousStock`, `currentStock`, `unitCost`, `totalCost`, `reason`, `referenceId`, `referenceType`, `metadata`.

**Financeiro/Caixa**
- `Cash` — `openingValue`, `balance`, `entries`, `exits`, `isOpen`.
- `Financial` — `type: FinancialType` (INCOME/EXPENSE), `category`, `amount`, `paymentMethod?`.
- `Payment` (F8) — gateway externo. `provider: PaymentProvider` (MERCADO_PAGO/PAGSEGURO), `status: PaymentStatus`, `externalId`, `checkoutUrl`, `webhookData`.

**Cliente/Fidelidade (F7)**
- `Customer` (companyId, phone)
- `LoyaltyAccount` (1:1 customer/company) — `totalPoints`, `totalCashback`.
- `PointTransaction` — `type: PointType` (EARNED/REDEEMED/EXPIRED/BONUS/CASHBACK), pode ter `expiresAt`.
- `Coupon` — `type: CouponType` (PERCENTAGE/FIXED_AMOUNT/FREE_SHIPPING), `pointsCost?` (resgate por pontos), `usageLimit`/`usageCount`.

**Entrega**
- `DriverProfile` (1:1 User) — `isAvailable`, `currentLat/Lng`.
- `DeliveryZone` — `type`, `neighborhood`, `baseFee`, `pricePerKm`, `clientFee`, `driverShare`.

**IA / BI / Auxiliares**
- `ChatSession` + `ChatMessage` (F6 — chatbot público com `sessionKey`).
- `AiConversation` + `AiMessage` (IA interna do admin).
- `KpiSnapshot` — snapshot diário por tenant (revenue, cmv, grossMargin, avgTicket, breakdown por OrderType e PaymentMethod).
- `Alert` — `type: AlertType` (LOW_STOCK/HIGH_CMV/REVENUE_DROP/TICKET_DROP/CANCELLATION_SPIKE), `severity`.
- `OperationalCost` — custos fixos/variáveis.
- `ImportSession` + `ImportItem` + `ImportLog` — Smart Import (cadastro inteligente via imagem/PDF/XML). `type: ImportType` (MENU/INVOICE), `status: ImportStatus`.

**Theme/Audit**
- `CompanyTheme` (1:1) — cores + logo + banner + darkMode.
- `AuditLog` — `action`, `entity`, `entityId`, `metadata`, `ipAddress`, `userAgent`, `userId?`, `companyId`.

---

## Regras de negócio

**Pedido (Order)**
1. `create`: busca todos os produtos pelo `productId`, valida pertencimento ao tenant, calcula `subtotal` (`quantity * salePrice`), soma `deliveryFee`, gera `total`. Cria com `status=PENDING`. Snapshota `productName`, `productSku`, `productCost`. Emite `orderCreated` + `dashboardUpdate` no socket.
2. `updateStatus` em transação Serializable:
   - **PENDING → CONFIRMED**: para cada item, busca a receita, consome ingredientes (`StockService.consumeIngredientTransactional`), calcula `itemCmv` (Σ `ingrediente.averageCost × quantidade`), atualiza `OrderItem.cmv` e `OrderItem.profit = subtotal − cmv`. Dispara hook de fidelidade (`LoyaltyService.processOrderReward`).
   - **→ CANCELLED**: restaura ingredientes (`restoreIngredientTransactional`).
   - Grava timestamp do status correspondente.
3. Emite `kitchenUpdate` + `dashboardUpdate`.

**Estoque**: consumo SEMPRE via receita (RecipeItem). Sem receita → não consome. Movimento registrado em `StockMovement` com `previousStock`/`currentStock`/`referenceId=orderId`.

**Multi-tenant**: nenhuma query pode rodar sem `companyId`. JWT é a fonte da verdade.

**Empresa bloqueada**: `Company.isBlocked=true` impede login (`AuthService.login`) e bloqueia qualquer request via `TenantGuard`.

**Soft delete**: `Product` e `Ingredient` usam `deletedAt`. Queries de listagem filtram `deletedAt: null`.

**Signup**: cria `Company` + `User` ADMIN + ativa módulos default: `TABLES`, `CASH`, `FINANCIAL`, `STOCK`, `RECIPES`, `DELIVERY`.

---

## Autenticação

**Fluxo**
1. `POST /api/auth/login { email, password }` → valida `bcrypt.compare`, checa `company.isBlocked`, assina JWT `{ sub, email, companyId, role }`, retorna `{ accessToken, user }`. Log via AuditService.
2. `POST /api/auth/signup { companyName, name, email, password }` → cria company + admin + módulos default + retorna token.
3. Frontend salva token em `localStorage` + cookie `token` (Zustand `setAuth`).
4. Requisições subsequentes: `Authorization: Bearer <token>` via interceptor axios.
5. `middleware.ts` (Next) bloqueia rotas privadas se cookie `token` ausente.
6. 401 do backend → interceptor frontend faz logout e redireciona para `/login`.

**JWT Strategy** (`jwt.strategy.ts`): extrai do Bearer, valida com `JWT_SECRET` (default `'secret'` se env faltar), `validate(payload)` busca user no banco e retorna `{ userId, email, companyId, role }` em `req.user`.

**Socket**: token via `handshake.auth.token` ou `handshake.query.token`. `jwtService.verify` → join em `company:${companyId}`. Sem token = cliente público (sem room).

---

## Permissões

**Roles** (`enum Role`): `SUPER_ADMIN`, `ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `DELIVERY` (também aparece `WAITER` em alguns controllers — string livre).

**Guards (ordem)**:
1. `JwtAuthGuard` — autentica
2. `TenantGuard` — valida empresa ativa (`req.tenantId = company.id`)
3. `RolesGuard` — lê `@Roles(...)` via Reflector e checa `user.role`
4. `ModuleGuard` — lê `@requiredModule('NOME')` e checa `CompanyModule.active`
5. `SubscriptionGuard` — checa `subscriptionStatus`/`dueDate`

**Frontend**: `useAuthStore` expõe `isAdmin()` (SUPER_ADMIN/ADMIN/MANAGER), `isKitchen()`, `isCashier()`. `<RoleGuard>` em `components/role-guard.tsx` restringe páginas.

---

## Principais services (backend)

- **AuthService** — signup/login/register, hash bcrypt(10), audit log.
- **OrdersService** — create/findAll/updateStatus/dashboard. Transação Serializable, integração com StockService, SocketGateway, LoyaltyService.
- **StockService** — `consumeIngredientTransactional`, `restoreIngredientTransactional` (recebem `tx` da transação atual). Atualiza `Ingredient.stock` e cria `StockMovement`.
- **ProductsService** — CRUD com soft-delete, parsing de `sizes` (JSON string ou array), normalização de Decimal.
- **LoyaltyService** — `processOrderReward(customerId, companyId, orderId, total)`: gera pontos/cashback no LoyaltyAccount.
- **AuditService** — `log(...)` cria AuditLog.
- **PrismaService** — readiness gate (`isReady`, `readyPromise`) + retry de `$connect` (8 tentativas × 3s).
- **SocketGateway** — emit por tenant: `orderCreated`, `kitchenUpdate`, `dashboardUpdate`, `tableUpdate`, `onlineOrderPaid`.
- **services/ai** — wrappers Anthropic e Gemini (Smart Import + chatbot + IA assistant).
- **services/storage** — Cloudinary com fallback para `/uploads` local.

---

## Principais componentes (frontend)

- **`ClientShell`** — wrapper client com providers (React Query, sidebar, toaster).
- **`Sidebar`** (`components/layout/sidebar.tsx`) — navegação principal por role.
- **`RoleGuard`** — restringe render por role.
- **PDV** (`components/pdv/`): `CategorySidebar`, `ProductCard`, `CartSidebar`, `PaymentModal`, `PizzaBuilder` (composição de pizza com bordas/tamanhos).
- **Dashboard**: `KpiCard`, `SalesChart` (recharts), `DashboardCharts`.
- **UI base** (`components/ui/`): `button`, `card`, `input`, `label`, `table`, `CurrencyInputBR`, `BarcodeScannerInput`, `ImageUploaderPreview`.
- **Hooks**: `useOrders`, `useKitchen`, `useTables` (globais); `useCart`, `useCash`, `useCatalog` (PDV).

---

## Fluxo do pedido

```
Cliente (PDV ou /menu público)
  ↓
POST /api/orders  (autenticado)  OU  POST /api/orders/public
  ↓
OrdersService.create
  ├─ Valida produtos pertencem ao tenant
  ├─ Calcula subtotal + deliveryFee → total
  ├─ Cria Order (PENDING) + OrderItems (snapshot)  [transação Serializable]
  ├─ socket.emit('orderCreated', order)            → cozinha vê em tempo real
  └─ socket.emit('dashboardUpdate', kpis)
  ↓
PATCH /api/orders/:id/status  { status: CONFIRMED }
  ↓
OrdersService.updateStatus (Serializable)
  ├─ Para cada OrderItem:
  │   ├─ Busca Recipe → consome RecipeItems (StockService.consumeIngredientTransactional)
  │   ├─ Cria StockMovement (referenceType=ORDER)
  │   ├─ Calcula cmv = Σ(averageCost × qty) → grava cmv e profit no OrderItem
  ├─ LoyaltyService.processOrderReward (se customerId)
  ├─ Grava confirmedAt
  └─ socket.emit('kitchenUpdate' + 'dashboardUpdate')
  ↓
PREPARING → READY → OUT_FOR_DELIVERY → DELIVERED
  (cada transição grava o timestamp correspondente)

CANCELLED em qualquer ponto:
  └─ Restaura ingredientes (restoreIngredientTransactional) + grava cancelledAt
```

---

## Fluxo de preços

**Produto simples**: `salePrice` direto (Decimal 10,2). `profitMargin` opcional. Pode ter `costPrice`.

**Produto com tamanhos**: `ProductSize[]` — preço por tamanho (`@@unique([productId, size])`). Frontend escolhe um size; o preço usado vem de `ProductSize.price`.

**Pizza com borda**: `Product` (sabor) + `ProductSize` (tamanho) + `PizzaBorder` → `PizzaBorderSize` (preço da borda no tamanho selecionado). Soma final = preço do tamanho + preço da borda no mesmo tamanho. `PizzaBuilder.tsx` no PDV monta a composição.

**Pedido**:
- `OrderItem.unitPrice` = preço efetivo do item (já com tamanho/borda resolvidos)
- `OrderItem.subtotal` = `unitPrice × quantity`
- `Order.subtotal` = Σ `OrderItem.subtotal`
- `Order.deliveryFee` (calculado por `DeliveryZone` se aplicável)
- `Order.driverFee` (de `DeliveryZone.driverShare`)
- `Order.total` = `subtotal + deliveryFee`

**Cupom** (`Coupon`): `PERCENTAGE`, `FIXED_AMOUNT` ou `FREE_SHIPPING`. `minOrderAmount`/`maxDiscount` limitam. Pode custar pontos (`pointsCost`).

**CMV** (calculado em CONFIRMED): `OrderItem.cmv = Σ (RecipeItem.quantity × OrderItem.quantity × Ingredient.averageCost || cost)`. `OrderItem.profit = subtotal − cmv`.

**Custo médio do ingrediente**: `Ingredient.averageCost` é atualizado em compras (PURCHASE em StockMovement). Snapshot em `KpiSnapshot.cmv`/`grossMargin`.

---

## Convenções do projeto

- **Idioma**: PT-BR em mensagens de erro, comentários, UI. Código em inglês.
- **Path alias backend**: `@/` → `src/`.
- **Validação**: sempre via DTO + class-validator. Nunca aceitar `body: any` em produção (alguns controllers legados ainda usam).
- **Multi-tenant**: `companyId` vem do JWT (`req.user.companyId`) — **nunca** do body.
- **Money**: Prisma `Decimal @db.Decimal(10, 2)`. No JS, sempre `Number(...)` antes de cálculos.
- **Soft delete**: `Product` e `Ingredient` usam `deletedAt`; listagens filtram `deletedAt: null`.
- **Snapshot em OrderItem**: `productName`/`productSku`/`productCost` copiados na criação (não dependem do estado atual do produto).
- **Stock**: só decrementa em `PENDING → CONFIRMED`; restaura em `* → CANCELLED`.
- **Migrações Prisma**: novas migrations DEVEM ser idempotentes (ver commits recentes corrigindo `add_pizza_borders` e enum duplicado).
- **Logs**: JSON estruturado em `main.ts` (`{ level, event, version, ... }`).
- **Modo operacional (PRIMORDIAL)**: ver memória `feedback_modo_operacional.md` — modo cirúrgico, patch mínimo, formato fixo (Diagnóstico → Patch → Validação → Status). Verificação obrigatória pós-correção.

---

## Observações importantes

160. **feat(menu): opt-in de marketing no checkout do cardápio + remove aviso falso de cold-start (commit `929808e6`, DEPLOY VPS PENDENTE)**: fecha o gap explicitamente sinalizado no item 158 ("não existe UI pra marcar `Customer.marketingOptIn=true`"). (1) Checkbox "Quero receber novidades e promoções no WhatsApp" no checkout do cardápio digital (`app/menu/[companyId]/page.tsx`, logo abaixo do campo telefone) — desmarcado por padrão (opt-out). `OnlineOrdersService.create()` ganha `syncCustomerOptIn()` (fire-and-forget dentro do `setImmediate` já existente, nunca atrasa/derruba o pedido): só grava `marketingOptIn:true` quando o checkbox veio marcado — se vier desmarcado/ausente, **não escreve nada** (nunca revoga opt-in dado antes por outro canal, ex.: "Adicionar Contatos" do admin ou pedido anterior). Busca `Customer` existente por `companyId+phone` normalizado; cria se não existir. (2) **`normalizePhoneBr` extraído pra `src/common/utils/phone.ts`** (antes só existia local em `whatsapp-campaigns.service.ts`) — usado agora também no opt-in do cardápio. Achado importante ao implementar: `dispatchMessage`/`sendEvolution` NÃO prefixam DDI sozinhos — um `Customer.phone` salvo sem "55" (como o `orders.service.ts customerAddressSave` do PDV já fazia, sem normalização) nunca seria alcançável numa campanha futura; `syncCustomerOptIn` já nasce usando o formato correto. `customerAddressSave` do PDV **não foi tocado** (fora do escopo desta sessão, mas é candidato a um fix futuro de mesma causa). (3) **Aviso de "servidor pode levar até 1 minuto para acordar"** removido da tela de carregamento do cardápio — resquício da era Render free-tier (cold start); backend roda em VPS sempre ativo desde a migração (item 104), o aviso só assustava clientes à toa. Retry de carga (`loadMenu`) reduzido de **8 tentativas com delay de 5-8s (até 58s de espera)** para **4 tentativas com 1.5s fixo** (~6s no pior caso) — ainda absorve uma falha de rede pontual ou o blip de ~502 observado nesta mesma sessão (item 159), sem fazer cliente reclamar de "demora 1 minuto pra abrir". `tsc --noEmit` + `npm run build` limpos em backend e frontend. **PENDENTE**: aguardando rebuild manual do VPS (item 143) — sem isso, opt-in do cardápio ainda não persiste em produção (o frontend já deployou via Vercel, mas o backend antigo ainda não tem `marketingOptIn` no `CreateOnlineOrderDto`).

159. **fix(whatsapp): P0 — Kely não respondia clientes (Company.businessHours zerado) + alerta sonoro de transbordo humano + gotejamento configurável em campanhas (commit `b1b73a39`, DEPLOY VPS PENDENTE)**: usuário reportou "prioridade máxima" — IA não respondendo clientes, no dia da inauguração da loja. **Causa raiz encontrada e corrigida via API (fix de DADO, não de código)**: `Company.businessHours` de Ruffinu's Pizzaria (`cmq7d3dxs0006gw5pabsljy87`) estava com `isOpen:false` em TODOS os 7 dias da semana — provavelmente sobrescrito sem querer numa edição anterior em `/configuracoes`. Como essa fonte tem prioridade sobre `WhatsappAiSettings.businessHoursStart/End` (item 108) e a empresa é `CLIENTE_REAL` (não bypassa por `isSalesMode` — a confusão do item 149 entre "empresa matriz vende SaaS" vs "loja real" já tinha sido corrigida antes desta sessão via `PLATFORM_SELLER_COMPANY_ID` dedicado, ver `whatsapp-ai.service.ts:1039-1053`), `isBusinessHours()` retornava `false` incondicionalmente pra qualquer hora/dia — Kely respondia TODA mensagem com "estamos fora do horário de atendimento" em vez de atender o pedido. Corrigido via `curl -X PATCH /company/settings` direto na API de produção, restaurando segunda-sábado 08:00-22:00 (domingo fechado, espelhando o `WhatsappAiSettings` legado que já estava configurado assim) — usuário deve conferir/ajustar o horário real em Configurações → Minha Loja. **Achado secundário durante o diagnóstico**: a conexão WhatsApp ativa da loja foi reprovisionada em 15/07 (`ruffinu-s-cmq7d3dx-mrmn28q6`, substitui a de 03/07 do item 149) — toda vez que isso acontece, `WhatsappAiSettings` é recriado do zero com os defaults do schema (`aiProvider:GEMINI`), perdendo o fix do item 149 (troca pra ANTHROPIC porque Gemini falhava rápido) — **não corrigido nesta sessão** (não reproduzido o erro do Gemini ao vivo desta vez), mas é um padrão de regressão a vigiar toda vez que a conexão for reconectada via QR. **Backend também piscou 502 por ~15s durante o diagnóstico** (Traefik não alcançava o container) — voltou sozinho, não investigado a fundo (não reproduzido de novo em 30s de monitoramento), possível suspeito é `NODE_OPTIONS=--max-old-space-size=512` (item 104) sob picos de carga — fica de olho pra uma próxima sessão se voltar a acontecer.

**(2) Alerta sonoro quando cliente insiste em falar com humano/gerente**: os 3 pontos do código que transferem `WhatsappConversation.mode` pra `HUMAN` (palavra-chave em `transferKeywords` — o mais comum e que ANTES não notificava ninguém; comando da IA no modo vendas R_FOOD_SAAS/LOJA_DEMO; comando `TRANSFERIR_HUMANO` no atendimento real CLIENTE_REAL) agora chamam `notifyHumanHelpRequested(...)` de forma uniforme, que por sua vez emite `SocketGateway.emitHumanHelpRequested(companyId, {conversationId, customerPhone, customerName, lastMessage})` pro room `company:${companyId}` (método novo no gateway, mesmo padrão de `emitOrderCreated`/`emitDashboardUpdate`). `WhatsappAiModule` ganhou `SocketModule` nos imports; `WhatsappAiService` injeta `@Optional() socketGateway?: SocketGateway`. **Frontend**: `ClientShell.tsx` (novo `useEffect`, roda em qualquer página do painel pra ADMIN/MANAGER/CASHIER/SUPER_ADMIN — não só na aba Conversas) conecta o socket compartilhado (`services/socket.ts`) com reconexão automática a cada 5s caso outra página o tenha derrubado (kitchen/orders/tables já chamam `.disconnect()` no unmount — o ClientShell nunca desconecta, só reconecta se cair), escuta `humanHelpRequested`, empilha num `humanAlerts[]` (dedupe por conversationId) e toca `/notification.mp3` (asset já existente, usado hoje só no `/kitchen`) **em loop** — só para quando a fila de alertas esvazia. Banner fixo vermelho pulsante no canto superior direito (`z-[10000]`) com nome/telefone/última mensagem + botões "Atender agora" (leva pra `/whatsapp-ia`) e "Dispensar" (remove só aquele alerta e para o áudio se for o último).

**(3) Gotejamento configurável na campanha recorrente (respondendo "como funciona o disparo de hoje")**: `WhatsappCampaign.maxPerRun Int @default(50)` novo (migration `20260721010000_add_whatsapp_campaign_batch_limit` idempotente, `--rolled-back` no Dockerfile) — cada clique em "Ativar"/"Retomar" dispara só os `maxPerRun` contatos elegíveis mais antigos (nunca contatados primeiro, depois quem espera há mais tempo — `getEligibleContacts` agora faz `groupBy` em `WhatsappCampaignSend` por telefone pra achar o último envio e ordena por isso). Limites em `CreateCampaignDto`/`UpdateCampaignDto`: `@Min(10) @Max(500)` — min evita ativação inútil de 1-2 por vez, max evita a plataforma virar ferramenta de disparo em massa (mesma linha ética do item 158 — não é sobre evadir detecção, é sobre não construir uma ferramenta de spam). Se sobrar gente elegível além do lote, `runCampaign()` volta a campanha pra `PAUSED` (em vez de `COMPLETED`) — clicar "Retomar" de novo dispara o próximo lote, e assim sucessivamente até a lista se esgotar (o check de `minIntervalDays` já garante que quem recebeu no lote anterior não entra nesse). **Desativação definitiva**: novo status `ARCHIVED` no enum `CampaignStatus` — diferente de `PAUSED` (retomável), uma campanha arquivada nunca mais pode ser ativada (`activateCampaign` rejeita com 400); botão "Desativar" com confirmação inline ("Definitivo? Sim/Não") ao lado de Pausar/Retomar na tabela. **Origem dos contatos de hoje**: como é loja nova inaugurando, não existe nenhum `Customer` com opt-in ainda (não existe em lugar nenhum do app uma UI de opt-in de cliente real — gap já documentado no item 158). Resolvido via `AskUserQuestion` — usuário escolheu colar a lista manualmente: nova rota `POST /whatsapp-campaigns/contacts` (`WhatsappCampaignsService.addContacts`) recebe `{contacts:[{name?,phone}]}`, normaliza telefone BR (`normalizePhoneBr` — só dígitos, adiciona DDI 55 se vier sem), busca `Customer` existente por `companyId+phone` (não há unique constraint no schema, então é `findFirst` manual em vez de `upsert` do Prisma) e marca `marketingOptIn:true` — cria se não existir. Modal "Adicionar Contatos" na página aceita colar `Nome, Telefone` (um por linha, nome opcional) com contador ao vivo de quantos foram detectados. **Validação**: `tsc --noEmit` + `npm run build` limpos em backend e frontend (rota `/campanhas-recorrentes` recompilada). **Não testado contra banco local desta vez** — Docker Desktop não estava rodando na máquina e o pedido era urgente (inauguração no mesmo dia); lógica revisada linha a linha (slice+sort+transição de status) em vez de round-trip real, diferente do padrão normal desta sessão de sempre validar contra Postgres real antes de declarar pronto — sinalizado explicitamente aqui por transparência. **PENDENTE CRÍTICO**: nada disso (fix do horário já está em produção via API direta, mas o CÓDIGO do alerta sonoro + gotejamento) existe em produção até o rebuild manual do VPS (item 143) — sem isso, a Kely responde certo agora (fix de dado já ativo), mas o alerta sonoro e o novo limite de disparo só funcionam depois do `docker compose build --no-cache backend` no terminal Hostinger.

158. **feat(whatsapp): campanhas recorrentes de reengajamento — versão compatível, sem anti-ban (commit `61481396`, NÃO deployado no VPS ainda)**: usuário pediu, em conjunto com o item 157, um sistema de disparo em massa de WhatsApp com técnicas explícitas de "anti-ban" (Spintax pra variar texto, delay randomizado 30-70s especificamente pra evadir detecção de spam, pausas por lote, regra de recorrência) — recusei a parte de evasão de detecção (spam/abuso independente de framing de "uso legítimo") e propus via `AskUserQuestion` uma versão compatível; usuário escolheu "Sim, versão compatível (Recomendado)". **Modelo implementado**: só contatos com `Customer.marketingOptIn=true` (novo campo, default `false` — opt-out por padrão) podem ser alvo de campanha; nenhum telefone recebe 2 campanhas (a mesma OU qualquer outra da empresa) dentro de um intervalo mínimo configurável (padrão 15 dias, checado globalmente via `WhatsappCampaignSend` — não é "bem-estar do contato" por dimensão de anti-spam, é throttle de reengajamento real); mensagem única por campanha (sem spintax/variação de texto); delay FIXO de 3s entre envios (documentado no código como estabilidade de conexão, não simulação de comportamento humano/evasão). **Schema**: `Customer.marketingOptIn Boolean @default(false)`; models novos `WhatsappCampaign` (name, message, status DRAFT/ACTIVE/PAUSED/COMPLETED, minIntervalDays) + `WhatsappCampaignSend` (log por envio: SENT/FAILED/SKIPPED_INTERVAL/SKIPPED_NO_OPTIN/SKIPPED_NO_PHONE). Migration `20260721000000_add_whatsapp_recurring_campaigns` idempotente + `--rolled-back` no Dockerfile. **Backend** (`src/modules/whatsapp-campaigns/`): `getSummary()` (contatos aptos agora, campanhas ativas, taxa de entrega dos últimos 30 dias); `activateCampaign()` dispara `runCampaign()` via `setImmediate` (não bloqueia a resposta HTTP) — motor sequencial (nunca paralelo), RE-CHECA o status da campanha a cada iteração do loop (permite pausar no meio do envio real); `pauseCampaign()`; substitui `{{nome}}` na mensagem. **Fix colateral necessário**: `WhatsappAiService.sendTextMessage()` retornava `Promise<void>` e engolia TODA falha de envio silenciosamente (sem conexão ativa, erro de dispatch) — impossível saber se uma mensagem realmente saiu. Mudado para `Promise<boolean>` (e `dispatchMessage()` privado idem) — os únicos outros 2 call sites (`orders.service.ts`, notificação de pedido) não usavam o retorno, então o fix não quebra nada existente; agora as campanhas rastreiam SENT vs FAILED de verdade. **Frontend**: nova página `/campanhas-recorrentes` (3 KPI cards, modal "Nova Campanha" com nome/mensagem/intervalo, tabela de histórico com badge de status + contadores enviados/falhas/pulados + botões Ativar/Pausar/Retomar); item de sidebar "Reengajamento WhatsApp" (ícone `Repeat`) na seção IA em `ClientShell.tsx`. **Testado localmente** (Postgres dev, nunca produção): contagem de contatos aptos correta, ativação dispara envio assíncrono, intervalo de 15 dias bloqueia reenvio ao mesmo contato mesmo vindo de campanha DIFERENTE, status SENT/FAILED agora preciso graças ao fix do `sendTextMessage`, campanha transiciona sozinha pra COMPLETED ao esgotar a lista — dados de teste limpos depois. `tsc --noEmit` + `npm run build` limpos em backend e frontend (rota `/campanhas-recorrentes` compilada, 60ª rota estática). **Gap consciente, não implementado**: não existe em lugar nenhum do app uma UI pra realmente marcar `Customer.marketingOptIn=true` (nenhum fluxo de opt-in) — sem isso, `eligibleContacts` fica em 0 pra clientes reais; fica pra uma próxima sessão (ex.: checkbox no checkout do cardápio digital ou no cadastro de cliente). **PENDENTE**: commit já pushado (`61481396`) mas backend precisa do rebuild manual no VPS (procedimento do item 143 — `docker compose build --no-cache backend` via terminal web Hostinger, `docker rm -f ... ; docker compose up -d`) antes da migration e dos endpoints existirem em produção; frontend deploya sozinho via Vercel no push.

157. **fix(theme+slug): Meta Pixel/GA4 nunca salvavam — quebrava o save do tema inteiro + correção de slug corrompido (DEPLOYADO, commit `e483cf38` + fix de dado via API)**: usuário pediu, pelo navegador, configuração de um Meta Pixel real (criado ao vivo no Meta Business Suite, ID final `2771400759911593`) pra Ruffinu's Pizzaria. Ao tentar salvar em `/theme`, descobri um bug de produção genuíno e não relacionado ao pedido original: **qualquer preenchimento de `metaPixelId`/`googleAnalyticsId` quebrava o salvamento do tema inteiro** (cores, logo, banner — tudo). Causa raiz: `frontend/app/theme/page.tsx` mesclava esses 2 campos no MESMO objeto de estado `theme` enviado via `PATCH /themes/:companyId` (que só atualiza `CompanyTheme`) — Prisma respondia 500 `Unknown argument metaPixelId` (campo pertence a `Company`, não a `CompanyTheme`), e o erro silencioso derrubava o save inteiro sem toast nem log visível. Só foi encontrado porque insisti em verificar via `curl` direto na API de produção depois de cada tentativa de save pela UI falhar silenciosamente, em vez de confiar no toast "Tema atualizado com sucesso!" (que sequer aparecia). **Fix**: `metaPixelId`/`googleAnalyticsId` viraram um estado `analytics` SEPARADO, salvo via `PATCH /company/settings` (endpoint correto, já existente) através de um botão dedicado "Salvar Rastreamento" — nunca mais viaja junto com o payload de tema. Backend: `UpdateCompanySettingsDto` ganhou os 2 campos (`@IsString() @IsOptional()`); `CompanyService.getSettings()`/`updateSettings()` passaram a incluir ambos no `select` e no `data` spread condicional (não existiam lá antes — DTO/service simplesmente descartavam silenciosamente qualquer valor enviado). Validado via `curl -X PATCH` direto (confirmado 500 antes do fix, 200+persistência depois) e depois pela UI real após rebuild do VPS — `metaPixelId=2771400759911593` confirmado gravado em produção via `GET /company/:id`. **Achado correlato (fix de DADO, não de código)**: `Company.slug` de Ruffinu's Pizzaria estava corrompido — continha uma URL inteira colada por engano (`"httpsfood-system-sas-erp-frontendvercelappmenucmq7d3dxs0006gw5pabsljy87"`), fazendo o link de compartilhamento do cardápio mostrar o ID bruto da empresa em vez do nome da loja. Corrigido via `curl -X PATCH .../company/settings -d '{"slug":"ruffinus-pizzaria"}'` direto na API de produção (usando o JWT já obtido) — confirmado que `https://food-system-sas-erp-frontend.vercel.app/menu/ruffinus-pizzaria` resolve corretamente. **Não investigado/não corrigido**: por que a validação de slug (`@Matches` regex de letras/números/hífen) não rejeitou uma URL inteira em primeiro lugar (possivelmente slugify aplicado antes da validação, ou o campo aceito sem re-slugificação) — só o dado da Ruffinu's foi corrigido, a lacuna de validação em si permanece no código pra uma sessão futura. **Onde o cliente configura o próprio Pixel**: mesma tela `/theme` (agora `/configuracoes?tab=aparencia`) que qualquer tenant já usa — o Pixel ID digitado ali é 100% por empresa (`Company.metaPixelId`), nunca compartilhado entre tenants; o botão que efetivamente injeta o pixel no cardápio público é o componente que lê esse campo em `menu/[companyId]` (via tema/settings públicos). `tsc --noEmit` + `npm run build` limpos em ambos.

156. **feat(menu): parecer técnico do cardápio digital — título/preview por loja + agrupamento por categoria (DEPLOYADO, commit `1f70553f`)**: pedido explícito de "o que posso aprimorar no cardápio digital" — medição real via DOM/HTML (não achismo), mesmo rigor do item 155(E). Achados: (1) **Compartilhamento sem identidade** — `curl` no HTML confirmou `<title>R_FoodSaaS ERP</title>` + `description="Sistema de gestão para restaurantes"` (metadados genéricos da plataforma) em vez do nome da própria loja — porque `app/menu/[companyId]/page.tsx` é `"use client"` e nunca teve `generateMetadata` (só Server Components suportam). Isso quebra o preview de link no WhatsApp/Instagram, principal canal de descoberta de cardápio de pizzaria. Fix: `app/menu/[companyId]/layout.tsx` (novo, Server Component) com `generateMetadata({params}: {params: Promise<{companyId:string}>})` — padrão async confirmado direto na doc oficial empacotada em `node_modules/next/dist/docs` (Next 16.2.10; `app/r/[token]/page.tsx` usa o padrão sync antigo, não copiei esse). Busca `/company/:id` + `/themes/:id` (endpoints públicos já usados client-side) e gera title/description/og/twitter por loja. **Achado dentro do achado**: `theme.bannerUrl`/`logoUrl` desta empresa são `data:image/jpeg;base64,...` (logo salva como base64 direto no banco, mesmo padrão do payload de 16,9MB do item anterior) — `og:image` com data URI é inútil pro crawler do WhatsApp/Facebook (eles baixam uma URL, não decodificam base64 embutido); adicionado guard `image = rawImage?.startsWith("http") ? rawImage : undefined` — melhor omitir a tag do que mandar uma que nunca carrega. (2) **Aba "Todos" em ordem alfabética misturada** — `document.querySelectorAll('h3')` confirmou os 35 produtos (pizza salgada, doce, esfiha, bebida) intercalados por letra inicial, sem separação visual. Fix em `page.tsx`: card do produto extraído pra `renderProductCard(product)` reaproveitável; quando `activeCategory === "Todos"`, agrupa `filtered` por `product.category?.name` num `Map`, itera na mesma ordem de `categories` (já corrigida no item da sessão anterior — `Category.sortOrder`) e renderiza subtítulo + produtos por categoria; categorias individuais (clique numa aba específica) continuam iguais, sem agrupamento (já são uma lista única). **Não implementado (consciente)**: descrições de produto repetitivas ("Massa artesanal de longa fermentação..." em quase toda pizza) — é conteúdo/copy do restaurante, não código; reescrever 35 descrições sem confirmar detalhes reais de receita com o dono arrisca inventar ingrediente errado — fica pra uma sessão de conteúdo separada, com o dono revisando antes de publicar. Validado ao vivo: `curl` no HTML server-renderizado confirma `og:title`/`og:description` corretos e ausência de `og:image` quebrado; navegador mostra "Combos Promocionais" → "Pizzas" → ... em vez de lista alfabética. `tsc --noEmit` + `npm run build` limpos. Frontend-only, sem rebuild de VPS necessário.

155. **fix(auditoria+estoque-online+smart-import+demo): QA cirúrgica do checkout/pagamento/impressão + estoque real no cardápio digital + dedup de ingrediente dentro da mesma nota + remoção do gate de lead na /demo (DEPLOYADO — commits `fa322969` + `2998297f`, VPS rebuild manual via terminal web confirmado 21/07/2026: `curl /api/health` OK + `grep -c hasLeadData dist/modules/auth/auth.service.js` → 2 ocorrências no container rodando)**: sessão em 4 partes + item (E) de restruturação visual da `/demo` pedido depois como "parecer técnico de designer/vendas".

**(E) Restruturação visual da `/demo` — pedido como parecer técnico honesto de conversão** (commit `2998297f`): medição real via DOM (não achismo) mostrou página com **11,9 telas de altura no mobile** (9633px/812px) e **35 elementos clicáveis únicos**, incluindo 4 variações de "Falar com Especialista/consultor" e 2 CTAs de "Testar" redundantes. Cor/tema não eram o problema (dark `#07090f` + laranja vibrante já é padrão moderno de SaaS) — o problema era estrutura e foco. 3 mudanças: (1) seção "Parte do mesmo ecossistema" (troca pra Oficina/Estética/Moda) estava ENTRE o header e a hero — irrelevante pro primeiro contato de quem clicou em anúncio de comida — movida pro rodapé (antes do `<footer>`), encurtando o caminho pré-conversão pra Header→Hero→Trust bar→Demos sem seção extra no meio. (2) CTA redundante "Dúvidas? Fale com um consultor agora" (dentro de "Como funciona") removido — já existem 3 outros caminhos pro mesmo `SPECIALIST_WA_URL` (header sticky, hero, footer). (3) Barra de confiança (150+ restaurantes ativos / 50 mil pedidos por mês / 99.9% uptime) — usuário confirmou que são **estimativa/chute**, não dado real medido — trocados por garantias reais e verificáveis do produto: 7 dias trial, 0% comissão no cardápio, 10 min pra começar a vender, suporte via WhatsApp direto. Linha "Mais de 150 restaurantes confiam..." no footer CTA também corrigida pra "Sem fidelidade · Cancele quando quiser · Suporte em português". **Achado correção-de-percurso**: a recomendação original citava "prova visual (cardápio ao vivo) vem depois de Como Funciona" — na inspeção do código isso já estava errado, o `MenuPhoneShowcase` (com "Abrir cardápio ao vivo →") já renderiza ANTES de "Como funciona"/testimonials; não forcei reordenação desnecessária. **Validado ao vivo**: 1 instância visível de cada elemento movido (um "2x" inicial no `document.querySelectorAll` era artefato interno do React Suspense streaming — `<div id="S:0" hidden>` — nunca visível/acessível pro usuário real, não bug). `tsc --noEmit` + `npm run build` limpos.

**(A) Auditoria QA (checkout/pagamento/impressão)** — pedida como "Engenheiro Sênior + QA", cobrindo `online-orders.service.ts`, `payments.service.ts`, `PrintRouterService.ts`/`printTicket.ts`/`printDispatcher.ts`. 4 riscos reais confirmados por leitura de código (não suposição) e todos corrigidos: (1) **Zero checagem de estoque no cardápio digital/totem** — `OnlineOrdersService.create()` e `OrdersService.updateKitchenStatus` (branch `source==='ONLINE'`) nunca tocavam ingrediente/receita, diferente do PDV. Fix: `create()` agora valida disponibilidade (soma consumo de todos os itens contra `Ingredient.stock`, respeita `allowNegativeStock`, bloqueia com 400 se faltar) ANTES de persistir; `OnlineOrdersService.consumeStockForOrder()`/`restoreStockForOrder()` (novos métodos) fazem o consumo real na confirmação e restauração no cancelamento, idempotentes via `StockMovement.referenceId+referenceType='ONLINE_ORDER'` (protege contra dupla-chamada vinda do webhook de pagamento E de ação manual da cozinha pro mesmo pedido); no board da cozinha a confirmação virou **bloqueante** (staff vê erro se faltar estoque, igual PDV) — chamada síncrona ANTES do `prisma.onlineOrder.update`, então se falhar o status não muda. Módulos: `OnlineOrdersModule` ganhou `StockModule`; `OrdersModule` ganhou `forwardRef(() => OnlineOrdersModule)`; `OrdersService` ganhou `@Optional() onlineOrdersService` + `private readonly logger` (não existia, só o import). (2) **Pedido duplicado por queda de conexão** — `OnlineOrdersService.create()` agora dedupe por companyId+telefone+total numa janela de 20s antes de criar (reenvio idêntico retorna o pedido já existente em vez de duplicar). (3) **Pagamento aprovado mas sistema nunca soube** — se o cliente pagar PIX e fechar a aba antes do polling confirmar, e o webhook do MP falhar (timeout, secret errado, MP fora do ar), o pedido ficava `PENDING` pra sempre. Fix: `PaymentsService.applyMpPaymentResult()` (extraído do webhook, reaproveitado) + `reconcilePendingPixPayments()` novo `@Cron('*/5 * * * *')` que revarre `OnlineOrder` PIX `PENDING` das últimas 2h e reconsulta o MP direto — idempotente (`if (current.paymentStatus==='APPROVED') return`, evita crédito duplo na carteira). Countdown do PIX no cardápio (`app/menu/[companyId]/page.tsx`) também não fazia nada ao chegar em `00:00` — agora mostra tela "PIX expirado" com botão "Gerar novo PIX" (reaproveita o `onlineOrderId`, chama o mesmo endpoint de novo, sem duplicar pedido) em vez de travar pra sempre com o polling rodando escondido. (4) **Impressão falha 100% silenciosa** — `printTicket()`/`printDispatcher()`/`printAllTickets()` só faziam `console.warn` se o popup fosse bloqueado; ninguém no caixa via isso. Fix: as 3 funções agora retornam (`printAllTickets` retorna `Promise<PrintBatchResult>`, já que os tickets subsequentes disparam via `setTimeout` — precisa esperar todos antes de resolver) quais setores tiveram popup bloqueado; os 4 pontos de chamada (`app/pdv/page.tsx`, `app/kitchen/page.tsx`, `app/orders/page.tsx`, `app/historico-pedidos/page.tsx`) viraram `async` e mostram `toast.error` quando algo falha. **Achado paralelo, não mexido**: a fila real `PrinterJob`/Printer Agent (Fase 2/3, item 87/115) continua **dormente** — nenhum desses 4 pontos escreve nela, só popup de navegador; quem instalou o agente físico está monitorando uma fila que ninguém alimenta (documentado, fora do escopo desta sessão). **Validado ao vivo**: subi o backend real (`node dist/main.js`) contra o Postgres **local de dev** (nunca produção — 5 empresas, dados de teste) — precisei rodar `prisma db push --accept-data-loss` nesse banco local porque tinha drift de schema pré-existente (faltava `Company.slug`, não relacionado a esta sessão, documentado no item 151). Criei ingrediente+receita de teste (estoque=1) pro produto "X-Burger" da `company-seed-001` e testei os 6 cenários via HTTP real: pedido pedindo mais que o estoque → 400; pedido válido → cria normal sem decrementar; reenvio idêntico em <20s → retorna mesmo `id`; confirmação via `/orders/kitchen/ONLINE/:id/status` → estoque 1→0 + `StockMovement` real; novo pedido com estoque zerado → bloqueado; cancelamento do confirmado → estoque restaurado 0→1 + `StockMovement` tipo `RETURN`. 6/6 passou. Limpei todos os dados de teste depois. `tsc --noEmit` + `npm run build` limpos em backend e frontend (ambos rodados de novo no final, com TODAS as mudanças desta sessão juntas).

**(B) Bug real reportado pelo usuário — nota fiscal duplicando ingrediente**: screenshot de `/ingredients` mostrando "CALABRESA FATIADA VALANDRO 1KG" 3x idêntico (cada um com estoque 0, nunca movimentado). Causa raiz em `smart-import.service.ts confirmInvoiceItems()`: o método processa os itens da nota **um por um, de forma independente** — o fix do item 152 (apelido + match exato) só cobre duplicação **entre notas diferentes**; se a MESMA nota tiver o mesmo produto em 3 linhas (comum quando o OCR da IA duplica uma linha da tabela ao extrair imagem/PDF), cada linha cria seu próprio ingrediente porque nenhuma sabe que a linha anterior, na mesma chamada, já criou um com nome idêntico. Fix: `Map<string,ingredient>` (`createdInThisBatch`) local ao loop — antes de criar um ingrediente novo, checa se um item ANTERIOR nesta mesma nota já criou um com o nome normalizado igual; se sim, reaproveita. Cuidado extra: o objeto em memória precisa ser sincronizado (`ingredient.stock = newStock` logo após o `prisma.ingredient.update`) — senão a 2ª ocorrência na nota leria o `stock` stale (0) e SOBRESCREVERIA o estoque já somado da 1ª em vez de somar em cima. **Validado ao vivo** contra o banco local: criei `ImportSession`+3 `ImportItem` idênticos ("CALABRESA FATIADA VALANDRO 1KG" x3) e chamei `POST /smart-import/confirm/invoice/:sessionId` de verdade — resultado: **1 ingrediente só**, estoque 0→1→2→3 (somado corretamente), 3 `StockMovement` de auditoria (uma por linha da nota, rastreabilidade preservada — não é bug perder isso). Limpei os dados de teste depois. **PRODUÇÃO — LIMPO em 21/07/2026**: os 3 duplicados reais (empresa Ruffinu's Pizzaria, `companyId=cmq7d3dxs0006gw5pabsljy87`, ids `cmrtljb6f0094iqhtsnjjvpn2`/`cmrtljb6k0097iqhtu3ny9tht`/`cmrtljb6o009aiqhtt2tdc6zu`, todos criados em `2026-07-20T19:07:05` — mesma nota, confirma o bug) tinham 1kg cada e zero receita referenciando qualquer um (checado antes de mexer). Fundidos via script `docker exec foodsaas-backend-backend-1 node -e "..."` rodado pelo usuário no terminal web do VPS (não pela sessão local — precisou de acesso direto à produção): `StockMovement` (2) e `IngredientAlias` (0) reatribuídos pro sobrevivente (`cmrtljb6f0094iqhtsnjjvpn2`), estoque somado pra 3, os 2 duplicados apagados dentro de uma `$transaction`. Resultado confirmado: `{"name":"CALABRESA FATIADA VALANDRO 1KG","stock":3}`. **Gotcha de sessão**: o Chrome tinha tradução automática ativa na aba do terminal, que traduziu palavras-chave do JS (`new`→`novo`, `for`→`para`, `const`→`constante`, `await`→`aguarde`) quebrando a sintaxe do script colado — resolvido desativando a tradução da página antes de recolar. **Tentativa de acesso direto ao terminal via `claude-in-chrome` (extensão no Chrome pessoal do usuário) falhou**: a URL do terminal web (`bos2.hostingervps.com/:porta/`) é uma sessão efêmera vinculada à aba/token que a abriu — navegar até ela de uma aba nova retorna 403; não há como "assumir" essa sessão de fora, só o usuário colando comandos na aba que ele mesmo abriu.

**(C) Remoção do gate de lead na `/demo` — decisão via `AskUserQuestion`**: usuário reportou "150+ pessoas visualizaram, nem uma testou" e pediu pra descobrir o motivo. Inspeção ao vivo da página real (mobile 375×812, onde cai a maior parte do tráfego de anúncio) revelou: clicar em "Testar Basic/Pro/Enterprise" abre um formulário obrigatório (Nome*, E-mail*, Nome do estabelecimento* — WhatsApp opcional) ANTES de qualquer acesso ao sistema — descompasso entre a promessa do botão ("Testar") e a ação real (cadastro). Perguntei ao usuário como resolver (4 opções: remover totalmente / reduzir pra 1 campo / mover a trava pra depois de engajamento / não mexer); escolheu **remover totalmente**. Implementado: botão "Testar X" (`app/demo/page.tsx`) chama `enterDemoWithLead(demo, {name:"",email:"",whatsapp:"",restaurantName:""})` direto, sem abrir o `LeadCaptureModal` (componente mantido no código, só parou de ser triggado — reversível). **Efeito colateral que precisou de fix junto**: `AuthService.demoAccess()` (`auth.service.ts`) disparava incondicionalmente 3 avisos por chamada — salvar Lead, e-mail pro admin, WhatsApp "🔥 Lead quente!" pro dono — que com o gate removido disparariam a CADA clique com campos em branco, virando ruído que afogaria um lead de verdade (o exit-intent, que já existia e continua mandando dado real pra `/leads`, é agora o ÚNICO jeito de capturar lead nessa página). Fix: `hasLeadData = !!(dto.name || dto.email || dto.whatsapp || dto.restaurantName)` guarda os 3 avisos — só disparam quando pelo menos um campo real chega (nunca mais vai acontecer via este botão, mas a função continua correta se algo mais no futuro chamar `demoAccess` com dado de verdade). Também removido `leadCapturedRef.current = true` de `enterDemoWithLead` (antes suprimia o exit-intent pra sempre depois do clique — errado agora que o clique não captura nada; o exit-intent precisa continuar podendo aparecer se a pessoa sair sem converter). Evento de pixel/GA renomeado de `"Lead"`/`generate_lead` pra `"DemoEntered"` (custom)/`demo_entered` — evita poluir o sinal de otimização de anúncio do Meta/GA com cliques que não são leads de verdade (o evento `Lead` real continua reservado pro exit-intent). **Validado ao vivo**: `POST /auth/demo-access` com todos os campos em branco contra o backend local → token válido retornado (HTTP 201), zero `Lead` criado no banco (confirmado via query). `tsc --noEmit` + `npm run build` limpos.

**(D) Pedido geral do usuário — "modernizar visualmente sempre que possível, porque o que é novo é mais aceito"**: registrado como preferência de trabalho (não uma tarefa isolada) — aplicar quando eu mexer em qualquer tela, priorizando o que já é convenção moderna de SaaS (ex.: item C acima — demo sem fricção é exatamente esse tipo de "moderno que converte mais"), em vez de abrir uma frente de redesign geral sem alvo concreto.

1. **Next.js 16 NÃO é o que parece**: `frontend/AGENTS.md` avisa que há breaking changes — checar `node_modules/next/dist/docs/` antes de escrever código de App Router.
2. **Módulos comentados em `app.module.ts`**: `ChatModule`, `AlertsModule` estão desabilitados ("schema/code mismatch"). `PaymentsModule` foi reabilitado (fix: `MERCADOPAGO` → `MERCADO_PAGO` em `payments.service.ts` e `payments.controller.ts`). Models existem no schema.
3. **PaymentMethod enum** **não** inclui `BANK_TRANSFER` no nome (usa `TRANSFER`). Frontend deve usar exatamente: `CASH | PIX | CREDIT_CARD | DEBIT_CARD | TRANSFER`.
4. **JWT_SECRET fallback `'secret'`**: se a env não estiver setada em produção, qualquer um pode forjar token. Validar deploy.
5. **CORS em `SocketGateway`** está `origin: '*'` (não restrito como o HTTP). Aceitar como intencional para clientes públicos.
6. **CompanyModule tem dois nomes**: campo legacy `module` (string) + novo `moduleSlug`. `ModuleGuard` ainda checa o legacy `module`.
7. **Body limit JSON 10MB** — para imagens base64 (logo, banner) em `CompanyTheme`.
8. **Readiness gate**: requisições durante boot recebem 503 → frontend deve ter retry de 5xx (não verificado se está implementado no interceptor atual).
9. **Endpoint público `/api/orders/public`** — sem auth — usado por cardápio público `/menu`. Aceita `companyId` no body (única exceção à regra).
10. **Seed**: `admin@teste.com / 123456` (SUPER_ADMIN), `admin@food.com / 123456` (ADMIN), `company-seed-001`.
11. **Backend prod**: `https://api.srv1747711.hstgr.cloud` (VPS Hostinger KVM1, migrado em 11/06/2026 — Render suspenso). Frontend prod: `https://food-system-sas-erp-frontend.vercel.app`. DB prod: PostgreSQL 18-alpine no VPS (container Docker, dados em volume `postgres_data`). Antigo backend Render: `https://food-system-backend-no7d.onrender.com` (manter ativo enquanto não confirmar tudo OK, depois suspender). **Nomes reais dos containers no VPS** (Docker Compose project = `foodsaas-backend`): backend = `foodsaas-backend-backend-1`, postgres = `foodsaas-postgres`. Para exec: `docker exec foodsaas-backend-backend-1 <cmd>`. **Login platform@foodsaas.internal**: senha `FoodSaaS2024` (resetada em 13/06/2026 via terminal web Hostinger — era UUID aleatório). **Deploy VPS**: clicar "Implantar" no Hostinger Docker Manager → projeto `foodsaas-backend` → reconstrói do GitHub source automaticamente. Terminal VPS: Gerenciador Docker → `foodsaas-backend` → Terminal → `exit` para chegar ao shell do host.
12. **Smart Import** processa imagem/PDF/XML com IA (Anthropic + Gemini). Sessão fica em `ImportSession` → `ImportItem` (`confirmed`/`savedId`) antes de virar Product/Ingredient.
13. **Pizza Borders** é módulo novo (migration `add_pizza_borders` precisou de fixes idempotentes em prod).
14. **TypeScript backend 6.x**: pode quebrar com libs que esperam TS 5. Verificar antes de bump.
15. **node_modules_bak/** e `gen_output.txt` no repo são lixo — não commitar.
16. **Smart Import — env vars no Render**: `GEMINI_API_KEY` e `GEMINI_MODEL` já estão configurados manualmente no Render (não vinham no `render.yaml` antes). `OPENROUTER_API_KEY` é opcional (fallback). `ANTHROPIC_API_KEY` é o último fallback. Gemini é primário e usa `responseMimeType: 'application/json'`.
17. **Smart Import — AnthropicProvider**: não tratava `textContent` (requests sem imagem, ex. PDF com texto extraído) — corrigido para enviar mensagem texto-only sem bloco `image` quando `textContent` está presente.
18. **Smart Import — Prisma Json field**: todos os saves de `ImportItem.data` usam `JSON.parse(JSON.stringify(item))` para garantir objeto plain (não Proxy/class instance). Bug em investigação: campos em branco na tela de revisão — logs diagnósticos adicionados em `getSession` e `runMenuExtraction` para ver no Render o shape real do `data`.
19. **Smart Import — Gemini timeout**: `AbortSignal.timeout` em `gemini.provider.ts` aumentado de 60s para 120s para evitar falha em imagens grandes. Erro original: "Tempo limite atingido ao processar a imagem".
20. **CompanyTheme.pizzaPricingMode**: campo `String @default("MAX")` adicionado. Valores: `"MAX"` (cobra o sabor mais caro) ou `"HALF"` (cobra a média). Configurável na página `/theme`. Menu lê via `GET /api/themes/:companyId` e aplica em `confirmFlavors`.
21. **Menu cardápio — CEP autocomplete**: campo CEP usa ViaCEP (`https://viacep.com.br/ws/{cep}/json/`) para preencher rua/bairro/cidade/UF automaticamente ao digitar 8 dígitos.
22. **Menu cardápio — checkout mobile**: modal usa `flex-col max-h-[92vh]` com header fixo, área scrollável e botão "Confirmar" fixo no rodapé. Resolve overflow em telas 320-390px.
23. **PizzaSizeConfig**: modelo novo para configurar `slices`, `maxFlavors`, `isActive`, `label` por tamanho (PEQUENA/MEDIA/GRANDE/FAMILIA/EXTRA_GRANDE) por empresa. Módulo `pizza-size-configs` com endpoint público `GET /api/pizza-size-configs/public?companyId=xxx`. Página `/pizza-borders` reformulada com tabs "Tamanhos & Sabores" e "Bordas Recheadas". Defaults criados automaticamente na primeira consulta por empresa. `(this.prisma as any).pizzaSizeConfig` em uso enquanto `prisma generate` não roda no Render.
24. **Complements**: sistema de complementos iFood-style — hierarquia Produto → Complement → ComplementOption. 4 tipos: `INGREDIENTES`, `ESPECIFICACOES`, `CROSS_SELL`, `DESCARTAVEIS`. Campos: `required`, `chargesExtra`, `multipleChoice`, `minOptions`, `maxOptions`. Módulo `complements` com endpoint público `GET /api/complements/public/product/:productId?companyId=xxx`. Página `/complements` no painel admin. `(this.prisma as any).complement` e `.complementOption` em uso enquanto client não regenera.
25. **Bebidas / categoryType**: `Category` tem campo `categoryType String @default("normal")` e `displayColumns Int @default(4)`. Quando `categoryType = "bebidas"`, página de produtos abre `BeverageModal` com fluxo 3 passos: tipo (preparado/industrializado) → busca por EAN ou nome via Open Food Facts API → formulário pré-preenchido. `Product` tem `productType String @default("standard")` e `eanCode String?`. Menu exibe grid responsivo (2/3/4 colunas) para categorias bebidas.
26. **Open Food Facts**: integração sem chave de API. EAN (8-14 dígitos numéricos) → `https://world.openfoodfacts.org/api/v0/product/{ean}.json`. Busca por nome → `https://world.openfoodfacts.org/cgi/search.pl?search_terms={q}&action=process&json=1&page_size=8&fields=code,product_name,brands,image_url`. Auto-fill: nome, EAN, imagem, marca no formulário do produto.
27. **maxFlavors no cardápio**: menu usa `globalMaxFlavors = Math.max(...pizzaSizeConfigs.filter(isActive).map(c => c.maxFlavors))` para limitar os botões de partes (2/3/4 sabores). `(this.prisma as any)` casts em pizza-size-configs e complements serão resolvidos após `prisma generate` no Render deploy.
28. **Product.videoUrl / hasVideo**: `Product` tem `videoUrl String?` e `hasVideo Boolean @default(false)`. `hasVideo` é auto-derivado de `videoUrl` no service (create e update). PDV exibe botão Eye ativo (azul) se produto tem vídeo, EyeOff desabilitado (opacity 0.4, cursor not-allowed) se não tem. Modal desktop: centralizado com backdrop. Mobile: overlay fullscreen estilo reels com autoplay. Botão editar (Pencil) removido do PDV. Cardápio digital também exibe botão "Vídeo" nas listas e grid de bebidas.
29. **OnlineOrder model**: `OnlineOrder` foi adicionado ao `schema.prisma` (migration `20260525000000_add_online_orders` cria a tabela mas o model estava faltando no schema). `OnlineOrdersModule` registrado em `app.module.ts`. Após salvar no DB, emite `orderCreated` + `dashboardUpdate` via SocketGateway (setImmediate para não bloquear resposta). `OnlineOrdersService` injeta `SocketGateway` (via `SocketModule` importado em `OnlineOrdersModule`).
30. **Render build — regras críticas**: (a) `prisma db seed` REMOVIDO do `buildCommand` — seed é dev-only e bloqueava todo deploy. (b) Novas migrations DEVEM usar `pg_constraint` / `information_schema` checks em vez de `EXCEPTION WHEN duplicate_object` (código `42P07` não é capturado por esse handler). (c) Toda migration nova precisa de `--rolled-back <nome>` no `render.yaml` antes do `prisma migrate deploy`. (d) Se Render builda commit antigo (cache), push commit vazio para forçar rebuild do HEAD.
31. **PDV responsivo**: sidebar de navegação `hidden md:flex`, header compacto em mobile, categorias como scroll horizontal em mobile, lista de produtos full-width com cards compactos em mobile, cart drawer full-screen em mobile. Desktop preservado. `PizzaBuilder` agora aceita prop `sizes?: SizeOption[]` com tamanhos reais do produto (preço por tamanho); fallback para 5 tamanhos padrão se não fornecido.
32. **DB sync migration `20260529000000_fix_db_sync`**: adiciona `EXTRA_GRANDE` ao enum `PizzaSize`, `customerId` à `LoyaltyAccount`, cria tabelas `OnlineOrder` e `PaymentWebhook` caso não existam. Todas as operações são idempotentes via `pg_constraint`/`information_schema` checks.
33. **WhatsApp IA module**: módulo completo NestJS em `src/modules/whatsapp-ai/`. Models: `WhatsappConnection` (provider: EVOLUTION_API|CLOUD_API_META), `WhatsappAiSettings` (aiProvider, mode AUTO/HYBRID/MANUAL, systemPrompt, businessHours), `WhatsappConversation` (context JSON com carrinho), `WhatsappMessage`. Migration `20260528200000_add_whatsapp_ai`. Página admin `/whatsapp-ia` com 4 tabs: Conexões, Configurar IA, Conversas (split-pane), Estatísticas. Webhook público: `GET/POST /api/whatsapp-ai/webhook/:connectionId`. Comandos inline no texto da IA: `[CMD:ADD_ITEM:id:qty]`, `[CMD:CONFIRM_ORDER:type:address:phone]`, `[CMD:TRANSFER_HUMAN]`, `[CMD:CLOSE]`. RAG de cardápio via `buildMenuContext()`. Providers suportados: Gemini (primary) e Anthropic.
34. **Super Admin impersonation UI**: removido banner amarelo `bg-amber-400` em `ClientShell.tsx`. Substituído por pill button discreto `fixed bottom-5 right-5 z-[9998]` que abre mini popup (backdrop + actions: abrir cardápio, sair da visualização).
35. **PDV beverages grid**: quando `Category.categoryType === "bebidas"`, PDV troca layout de lista por grid responsivo: desktop `grid-cols-5` (xl) / `grid-cols-3`, mobile `grid-cols-2`. Cards compactos com imagem quadrada, nome, preço e botão "+Adicionar". `Category` interface no PDV inclui `categoryType?: string`.
36. **BeverageModal debounce**: campo de busca na tela de cadastro de bebidas tem debounce 500ms (mínimo 3 chars) via `useRef<setTimeout>`. Busca também dispara no Enter e no botão de lupa.
37. **ClientShell MODULE_NAV — whatsapp-ia**: slug `"whatsapp-ia"` mapeado para `href: "/whatsapp-ia"`, label "WhatsApp IA", ícone `MessageCircle`. Aparece na sidebar quando módulo está ACTIVE ou TRIAL.
38. **Preço mínimo "A partir de"**: helper `productMinPrice(p)` e `productPriceLabel(p)` em `app/pdv/page.tsx` e `app/menu/[companyId]/page.tsx`. Produtos com `sizes.length > 1` exibem "A partir de R$ X,XX" (menor preço dos tamanhos). Produtos sem sizes ou com 1 size exibem o preço normal. Product type no menu inclui `sizes?: { size: string; price: number }[]`.
39. **PizzaBuilder — maxFlavors dinâmico**: `PizzaBuilder.tsx` aceita prop `sizeConfigs?: Record<string, { maxFlavors: number }>`. `maxFlavors` é calculado por tamanho selecionado (`sizeConfigs[selectedSize.size]?.maxFlavors ?? 2`). PDV busca `GET /api/pizza-size-configs/public?companyId=xxx` ao carregar e passa `sizeConfigs` para o builder. Label de sabores mostra `X/N selecionados`. Ao trocar tamanho, sabores excedentes são removidos automaticamente.
40. **Digital Menu — pizza 1 sabor**: partes disponíveis agora `[1, 2, 3, 4]` (antes `[2, 3, 4]`), filtradas por `globalMaxFlavors`. `confirmFlavors` exige ao menos 1 sabor. Composição de 1 sabor usa nome direto `"Pizza Nome"`. Fração exibida como `"inteiro"` para 1 sabor.
41. **OrderDetailsForm compartilhado**: componente `components/shared/OrderDetailsForm.tsx` com todos os campos de atendimento: tipo (DINE_IN/DELIVERY/PICKUP), número da mesa, nome/telefone do cliente, CEP (autocomplete ViaCEP 600ms debounce), rua, número, complemento, bairro, cidade. Usado no `PaymentModal` (quando `orderDetails` não é passado) e no cart drawer do PDV. `PdvOrderDetails` em `PaymentModal.tsx` é alias de `OrderDetails`. `closePaidOrder` no PDV monta `fullAddress` concatenando rua + número + complemento + bairro + cidade.
42. **WhatsApp — notificação automática de pedido**: `WhatsappAiService.sendOrderNotification()` — busca primeira conexão WhatsApp ativa da empresa (`WhatsappConnection.isActive=true`), monta mensagem com resumo do pedido (itens, total, tipo, status emoji) e envia via `dispatchMessage`. `OrdersService` injeta `WhatsappAiService` com `@Optional()` e chama `setImmediate` (não bloqueante) após transações CONFIRMED/READY/OUT_FOR_DELIVERY/DELIVERED/CANCELLED quando `order.customerPhone` está presente. `WhatsappAiModule` registrado em `AppModule` e importado em `OrdersModule`.
43. **Order model — campos ausentes**: `Order` NÃO tem `customerPhone`, `customerName` nem `orderType` (esses campos existem em `OnlineOrder`). `OrdersService.updateStatus` lê `customerPhone` via `order.customer?.phone` (inclui `customer: true` no findFirst). `orderType` é acessado via `(order as any).orderType`. Nunca assumir que campos de `OnlineOrder` existem em `Order`.
44. **FinancialModule**: registrado em `app.module.ts` (estava faltando — causava 404 em `/api/financial` e `/api/financial/summary`). Endpoints disponíveis: `GET /api/financial` (lista), `GET /api/financial/summary` (entries/exits/balance/totalSales/totalOrders/ticketAverage), `POST /api/financial` (criar). Roles: SUPER_ADMIN/ADMIN/MANAGER.
45. **Página Financeiro — reescrita completa**: `app/financeiro/page.tsx` agora usa dados reais. Tab Extrato: `GET /api/financial` + `GET /api/financial/summary` + `GET /api/cash/current`. Modal "Nova Transação": POST `/api/financial` com type/category/description/amount/paymentMethod. Filtro INCOME/EXPENSE + busca por texto. Export CSV real. Tab Relatório: breakdown por forma de pagamento + resultado líquido usando despesas do summary. Tab Mensalidade: KPIs reais de totalSales/totalOrders/ticketAverage. Tab Configurações: persiste em localStorage.
46. **Módulos adicionados ao app.module.ts**: `IngredientsModule`, `RecipesModule`, `StockModule`, `DriversModule`, `FinancialModule` — estavam ausentes causando 404 nessas rotas.
47. **Open Food Facts — proxy Next.js**: para evitar CORS ao buscar dados de bebidas do browser, criados dois API routes server-side: `app/api/off/search/route.ts` (`GET /api/off/search?q=...`) e `app/api/off/product/[ean]/route.ts` (`GET /api/off/product/:ean`). Frontend chama esses proxies em vez de `world.openfoodfacts.org` diretamente.
48. **Products page — campo brand removido**: `brand` não existe no `Product` schema nem no `CreateProductDto`. Enviar `fd.append("brand", ...)` no FormData causava 400 (`forbidNonWhitelisted`). Campo removido do submit — a marca fica embutida no nome do produto.
49. **Entregadores page — reescrita completa**: `app/entregadores/page.tsx` reescrita com API real. `GET /api/drivers` → lista real. Modal "Novo Entregador": `POST /api/drivers` com name/email/password/phone/vehicleType/vehiclePlate. Modal "Editar": `PATCH /api/drivers/:id` com toggle isActive/isAvailable. Stats derivadas dos dados reais (online/ocupado/offline). `drivers.controller.ts` import corrigido: `@/auth/jwt-auth.guard` → `@/common/guards/jwt-auth.guard`.
50. **Fase 1 — Complementos (COMPLETO)**: fluxo de complementos implementado e validado end-to-end. `OrderItemComplement` persiste por item individualmente (transação Serializable). `orders.service.ts` `create()` usa `tx.orderItem.create()` individual por item (elimina risco de `createdOrder.items[i]` com heap order errado). `findAll()` inclui `selectedComplements` via `(this.prisma as any)`. PDV: `openProductAdd()` faz GET do endpoint público de complementos; se `groups.length > 0` abre modal; se 0 adiciona direto. `cartTotal` inclui preço de complementos. `closePaidOrder` mapeia `complements[]` por item. Frontend kitchen/orders exibe complementos por item na tela e na impressão. Validação: 10/10 PASS com dados reais.
51. **Build de produção — corrigido**: problema do `tsBuildInfoFile` stale resolvido com `"tsBuildInfoFile": "./dist/.tsbuildinfo"` no `tsconfig.json` + script `prebuild` em `package.json` que limpa `dist/` e `.tsbuildinfo` antes de cada build. `npm run build` agora é idempotente (3 execuções consecutivas: 373 arquivos cada). `npm run start:prod` (`node dist/main`) sobe em 3s sem ts-node. `Dockerfile`: `EXPOSE 3001` (era 3000) e `prisma db seed` removido do CMD (era risco crítico em produção).
52. **Fase 2 — OrderType (AUDITORIA PENDENTE DE IMPLEMENTAÇÃO)**: `enum OrderType {DELIVERY, DINE_IN, PICKUP}` existe no schema mas `Order` model NÃO tem campo `orderType`, `customerName`, `customerPhone` nem `deliveryAddress`. `OnlineOrder` tem todos esses campos. PDV envia `orderType` no POST /orders mas é silenciosamente descartado. `reports.service.ts` lê `(order as any).orderType` que retorna sempre `undefined` → TODOS os pedidos do PDV são contados como `dineIn` nos relatórios, independente do tipo real. A cozinha identifica o tipo apenas pelo campo `notes` (texto livre). Implementação da Fase 2 = adicionar os 4 campos ao model `Order` + migration idempotente + persistir no service + exibir nos cards.
53. **Item 3 — Ordenação manual de Categorias e Produtos (COMPLETO)**: `Category.sortOrder Int @default(0)` + `Product.sortOrder Int @default(0)` + `Product.isFeatured Boolean @default(false)` (preparado para áreas de destaque/promoções, não usado ainda). Migration `20260530000000_add_sort_order` idempotente (`ADD COLUMN IF NOT EXISTS` + backfill `ROW_NUMBER() PARTITION BY companyId ORDER BY name ASC` que só sobrescreve `sortOrder=0`). Índices compostos `(companyId, sortOrder)`. Backend: `categories.service.findAll/create/reorder` e `products.service.findAll/publicMenu/create/reorder` — `create` calcula `(max+1)`, `reorder` valida ownership por `findMany({id:{in:ids}, companyId})` antes do `$transaction` (preserva SECURITY-001). Endpoints novos: `PATCH /api/categories/reorder` e `PATCH /api/products/reorder` body `{items:[{id,sortOrder}]}`. **IMPORTANTE**: no `products.controller.ts` a rota `@Patch("reorder")` precisa ficar ANTES de `@Patch(":id")` (mesma constraint do Nest match-by-order). Frontend: `app/categories/page.tsx` e `app/products/page.tsx` usam `@hello-pangea/dnd` via `dynamic({ssr:false})` — drag handle `<GripVertical>`, rollback otimista em erro de API. Drag-and-drop apenas para ordenação (NÃO move produto entre categorias — `reorder` só toca `sortOrder`). `app/menu/[companyId]/page.tsx` removeu `.sort()` alfabético — confia em `publicMenu` orderBy `[{sortOrder:'asc'},{name:'asc'}]`. Validação E2E PASS: GET retorna ordenado, reorder persiste após refresh, cross-tenant bloqueado com `BadRequestException("Categoria/Produto fora da empresa")`, PDV e Cardápio Digital respeitam.
54. **Item 4 — Fase A + Adapter Cozinha/Impressão (COMPLETO)**: complementos agora funcionam end-to-end no Cardápio Digital e pedidos do cardápio chegam na cozinha como os do PDV. Mudanças: (a) **A1**: `components/shared/ComplementsModal.tsx` (novo) — componente único usado por PDV (theme="dark") e Cardápio Digital (theme="light"); botões `min-h-[56px]` (touch); progress bar de obrigatórios; subtotal vivo. `app/menu/[companyId]/page.tsx` agora chama `GET /complements/public/product/:id?companyId=X` antes de adicionar ao carrinho; CartItem inclui `complements[]`; payload do POST `/online-orders` envia `complements` por item. `app/pdv/page.tsx` refatorado para usar o mesmo componente. (b) **A2**: validação server-side em `OnlineOrdersService.create` — recarrega `Complement` por `productId+companyId` e valida `required`/`min`/`max`/`multipleChoice` + anti-spoof de option-id; rejeita com `BadRequestException`. (c) **A4**: coerência min/max em `app/complements/page.tsx` — toggles ajustam valores correlatos, input max desabilitado em escolha única, valida `min ≤ max` e `required ⇒ min ≥ 1` no submit. (d) **Adapter (Caminho 2)** — `OrdersService.findAllForKitchen(companyId)` une `Order` (PDV) + `OnlineOrder` (Cardápio) em shape único `{id, source: "PDV"|"ONLINE", status, items[{ selectedComplements[] }], ...}`; status normalizado via `mapOnlineStatusToKitchen` (`DELIVERING→OUT_FOR_DELIVERY`, `COMPLETED→DELIVERED`, `CANCELED→CANCELLED`); `updateKitchenStatus(source, id, status, userId, companyId)` roteia para `Order` ou `OnlineOrder.orderStatus` mapeado de volta. **IMPORTANTE**: `OnlineOrder` usa `orderStatus`, NÃO `status`. Endpoints novos: `GET /api/orders/kitchen` e `PATCH /api/orders/kitchen/:source/:id/status`. Frontend (`hooks/useKitchen.ts`, `app/kitchen/page.tsx`, `app/kitchen/KitchenBoard.tsx`, `app/orders/page.tsx`) trocaram `/orders` por `/orders/kitchen`, usam `key=${source}-${id}` e exibem badge `[PDV]`/`[ONLINE]`. Validação E2E PASS 15/15: GET público retorna grupos, validação rejeita pedido sem obrigatório/com excesso/com option falso, pedido ONLINE com complementos cria e aparece em /orders/kitchen com `source=ONLINE`, pedido PDV idem com `source=PDV`, complementos chegam em ambos via `selectedComplements`, status PREPARING persiste após refresh (DB salva como `PREPARING` em `Order.status` e `OnlineOrder.orderStatus`), cross-tenant 4/4 bloqueado (B não altera status PDV/ONLINE de A, B não vê pedidos de A em /orders/kitchen, B não cria complemento em produto de A — novo `assertOwnership` em `ComplementsService.create` validando `productId+companyId`). Pagamento, webhooks e schemas `Order`/`OnlineOrder` intactos.
55. **Item 4 — Fase B (COMPLETO)**: hierarquia P>C>G + duplicate + reorder + upload imagem + vínculo por categoria.

**Backend**: `complements.service.ts` reescrito com método dedicado `applyComplementsPriority(groups)` que aplica P>C>G + dedup por `name.toLowerCase().trim()` (docstring de bloco explica regra de prioridade e estabilidade). `findByProduct(productId, companyId)` busca produto → categoria → query única `OR(productId | productId=null+categoryId=cat | productId=null+categoryId=null)` → aplica regra. `assertScopeAndOwnership` rejeita `productId+categoryId` simultâneos e valida ownership de ambos. Novos métodos: `duplicate(id, companyId)` (copia grupo + options com `"(cópia)"` no nome, sortOrder=max+1 dentro do mesmo escopo), `reorderGroups(companyId, items)`, `reorderOptions(complementId, companyId, items)`. `complements.controller.ts` adicionou `PATCH /complements/reorder`, `POST /complements/:id/duplicate`, `PATCH /complements/:id/options/reorder` ANTES de `@Patch(':id')` (Nest match-by-order). `imageUrl` na option persiste via base64 no JSON existente (sem multipart).

**Frontend** (`app/complements/page.tsx` reescrita): radio "Aplicar a" (🌐 Global / 📂 Categoria / 🍔 Produto) com dropdowns condicionais; badges visuais de escopo com cores diferentes (roxo/azul/laranja); agrupamento por escopo (`buckets`); botão "Duplicar grupo" (ícone Copy azul); DnD `@hello-pangea/dnd` separado por escopo (cada bucket é uma `Droppable` distinta — impede reorder cruzado); DnD de opções dentro de cada grupo; upload de imagem por opção via `ImageUploaderPreview` (thumbnail 40x40 aparece ao lado do nome); todos os botões/inputs com `min-h-[44px]` (touch); modal sticky-bottom em mobile (`items-end sm:items-center`); coerência min/max preservada da Fase A.

**Validação E2E backend** (`test_fase_b.sh`) — **12/12 PASS**:
- Hierarquia P>C>G: cria 3 escopos com mesmo nome "Adicionais" → GET retorna apenas o PRODUTO com a opção "Catupiry Produto" (vence)
- Remoção progressiva: remove P → retorna C (Catupiry Categoria); remove C → retorna G (Catupiry Global); remove G → retorna 0 grupos
- Exclusividade: `POST {productId:X, categoryId:Y}` → 400 "Defina apenas UM escopo"
- Multiempresa 3 cenários: B cria grupo em categoria de A → 404; B reordena grupos de A → 400 "fora da empresa"; B duplica grupo de A → 404
- B2 duplicate: cria grupo "Bordas" com [Catupiry, Cheddar] → cópia tem nome "Bordas (cópia)" + ambas options copiadas
- B4 reorder grupos: troca sortOrder 1↔10, persistência confirmada via findAll
- B4 reorder opções: troca [Catupiry, Cheddar] → [Cheddar, Catupiry], persiste
- B3 upload: opção criada com `imageUrl` base64 (114 bytes de PNG 1x1), retorna no payload

**Bugs descobertos e corrigidos durante teste**: lixo de execuções anteriores (grupos "Adicionais" antigos isActive=true) precisou ser limpo via `UPDATE Complement SET isActive=false WHERE name IN (...)` antes do teste 7 passar. Python no Windows com codec cp1252 não imprime "→" — exportar `PYTHONIOENCODING=utf-8` antes de rodar shell tests.

**Não tocados** (consumem automaticamente via GET público): `components/shared/ComplementsModal.tsx`, `app/pdv/page.tsx`, `app/menu/[companyId]/page.tsx` — herança transparente.

**Não implementado** (consciente): teste manual de DnD em browser real (touch/tablet). Backend testado, frontend type-check OK. Validação visual requer interação humana.
56. **Sprint UX-01 (COMPLETO)** — 4 fixes pós-auditoria: (M-01) `ComplementsModal.tsx` agora renderiza `<img>` 40x40 da opção quando `imageUrl` existe → torna visível para o cliente final a feature B3 da Fase B (antes a imagem era cadastrada no admin mas nunca aparecia no PDV/cardápio). (C-01) Cardápio digital `app/menu/[companyId]/page.tsx` recebe `pb-44` no `<main>` quando há item no carrinho + `paddingBottom: calc(11rem + env(safe-area-inset-bottom))` inline + CTA flutuante também ganha `env(safe-area-inset-bottom)` → último item não fica mais coberto pelo botão "Ver pedido" em mobile/iOS com notch. (I-01) `app/orders/page.tsx` função `printOrder` adiciona badge `[PDV]`/`[ONLINE]` no HTML impresso (azul `#1d4ed8` para online, preto `#111` para PDV) — operador na cozinha distingue origem mesmo no papel térmico. (I-01) extrai source via `(order as any).source` (vem do adapter `/orders/kitchen` do Item 4). (A-02) `ImageUploaderPreview` ganha prop `maxFileSizeMB` (default 5MB), valida antes da compressão e mostra erro amigável em caixa vermelha com ícone svg (`bg-red-50 border border-red-200 rounded-xl`, `text-sm font-medium`) — antes era um `text-xs` discreto. Type-check OK em todos os arquivos. Nenhum backend tocado.
65. **feat(demo-vitrine): demos populadas como vitrines comerciais de pizzaria**: `DemoVitrineService` em `super-admin/demo-vitrine.service.ts` + endpoint `POST /api/super-admin/demo/vitrine`. Cada tier recebe: categorias com banners, produtos com imagens locais, clientes, entregadores, pedidos históricos + hoje, WhatsApp IA (Carol) com 3 conversas de exemplo, registros financeiros. Resultado real: Bella Napoli (BASIC): 4 cats, 24 prods, 10 clientes, 33 pedidos, R$804 hoje. Don Corleone (PRO): 5 cats, 28 prods, 50 clientes, 128 pedidos, R$2412 hoje. Milano (ENTERPRISE): 5 cats, 28 prods, 185 clientes, 369 pedidos, R$5631 hoje. Imagens: 20 JPEGs baixados de Unsplash para `frontend/public/demo-assets/` (pizzas/, bebidas/, sobremesas/, combos/, banners/) — URLs absolutas Vercel no banco, sem base64. Seguro: só toca `demo-basic-001`, `demo-pro-001`, `demo-enterprise-001`. Idempotente. Commits `e7537d6f` + `36f07840`.
57. **Deploy — 7 commits pushados (CONCLUÍDO)**: estratégia de commits separados aprovada e executada. Sequência: (1) `49379d8d` fix(security): SECURITY-001 tenant guards recipes+tables. (2) `a1377c75` feat(item-3): manual sort order + DnD + SECURITY-001 fixes cat/prod (8 arquivos, +312/-36, **migration 20260530000000_add_sort_order** idempotente). (3) `d55b6947` feat(super-admin): visible impersonation bar. (4) `16c6d8f2` feat(complements): Phase A — modal cardápio + validação server-side (inclui Sprint UX-01 M-01 e C-01 nos mesmos arquivos). (5) `d3314b87` feat(complements): Phase B — P>C>G + duplicate + reorder + upload imagem. (6) `f0d449d6` feat(orders): Item 4 Caminho 2 — Adapter Kitchen unificado (**migration 20260529100000_add_order_type_fields** idempotente, inclui UX-01 I-01 badge impressão e UX-02 fix #4 toast useKitchen). (7) `6bf9a623` chore(ux+infra): UX-01 A-02 ImageUploaderPreview + Dockerfile/package.json/tsconfig.json build fixes + **render.yaml com 2 entradas migrate resolve --rolled-back para as migrations novas** (CLAUDE.md §30c) + CLAUDE.md atualizado. Total: 33 arquivos +2276/-658. `git status` clean. **Pendente git push**: Crítico #1 da Sprint UX-02 (`/orders/kitchen` quebrado em prod) só se resolve ao deployar commit 6. Backend deploy ANTES de frontend (Render auto-deploya ao push). Cleanup TESTE UX já executado em prod (3 grupos / 8 opções removidos da Alexandria — companyId cmpkj25po00021r8u0dvvxo3x).
59. **Fix: React hydration error #418 em `/modulos`**: `components/modulos/HeroModules.tsx` usava `Math.random()` inline nos `style` e `transition` props das partículas decorativas (linhas 42-56). Next.js App Router faz SSR de componentes `"use client"` para o HTML inicial — valores diferentes entre servidor e cliente geravam divergência de hydration (React error #418). Fix: dados das 20 partículas movidos para `useState<Particle[]>([])` + `useEffect` (render client-only). Commit `8f4dbe94`.
61. **Fix: `CompanyModuleModule` ausente do `app.module.ts`**: `src/company-module/` existia com controller/service/module completos mas nunca foi registrado em `app.module.ts` — todos os endpoints `/api/company-module/*` retornavam 404 (página `/modulos` não carregava os módulos da empresa). Fix: adicionado `import { CompanyModuleModule }` + entrada em `imports[]`. Type-check OK. Commit `51517aac`. **Observação**: o controller usa `'src/modules/auth/jwt-auth.guard'` (caminho legado) em vez de `@/common/guards/jwt-auth.guard` — ambos os arquivos existem, então funciona, mas o padrão do projeto é o `@/common/guards/`.
60. **Fix: `Promise.all` silenciando KPIs no BI por causa de `/alerts` 404**: `app/bi/page.tsx` usava `Promise.all([kpi, revenue, alerts])` — como `AlertsModule` está comentado no `app.module.ts` (schema/code mismatch), o 404 em `/alerts?unread=false` fazia o `catch {}` engolir o erro E deixava KPIs e revenue sem dados. Trocado para `Promise.allSettled` com verificação individual de `status === "fulfilled"` por resposta. O 404 em si persiste (módulo desabilitado intencionalmente), mas não afeta mais os outros dados. Commit `8f4dbe94`.
64. **feat(whatsapp-ia): seletor visual de provedor no formulário de nova conexão**: `app/whatsapp-ia/page.tsx` — `ConnectionsTab` agora exibe 3 cards clicáveis (WhatsApp Business / Evolution API / Meta Cloud API) em vez do `<select>` antigo. "WhatsApp Business (Recomendado)" mostra apenas Nome + Telefone + banner "Após salvar poderá conectar via QR Code"; envia `provider:"EVOLUTION"` ao backend (sem alterar contrato da API). Evolution API mantém todos os campos técnicos. Meta Cloud API mantém Phone Number ID + token + webhook. `providerType` é estado local (UI-only), `form.provider` continua sendo o valor real enviado. Reset de campos técnicos ao trocar de tipo. Sem alterações de backend, schema, service ou migration. Commit `9b4899ca`.
63. **feat(sidebar): "Configurar IA" visível em todos os planos**: `ClientShell.tsx` — removido `moduleSlug: "whatsapp"` do item "Configurar IA" na seção "Atendimento". A função `canAccessModule` só restringe role DEMO; para usuários regulares já retornava `true`. Remoção do `moduleSlug` torna a intenção explícita: item sempre visível para SUPER_ADMIN/ADMIN/MANAGER independente de plano ou módulo ativo. Efeito colateral: DEMO também passa a ver o item (antes ficava oculto se "whatsapp" não estivesse em `activeSlugs`). Commit `d8ea3056`.
62. **feat(whatsapp-ia): Carol evoluída para persona de venda consultiva**: apenas `buildSystemPrompt()` em `claude-cart.service.ts` foi alterado — 8 novas diretivas adicionadas dentro da seção `━━━ DIRETRIZES DE COMPORTAMENTO ━━━` (regras 7-14). Nenhum outro método, schema, controller ou migration tocado. Diretivas adicionadas: (7-8) Descoberta — faz UMA pergunta antes de registrar item (qtd pessoas, entrega/retirada, preferência); calibra tamanho pela resposta. (9) Recomendação — quando cliente indeciso, indica UM produto específico com justificativa curta. (10-11) Upsell — máx 1 sugestão complementar por mensagem, prioridade bebida > borda > sobremesa; se recusar não insiste. (12) Objeções de preço — não oferece desconto automático; sugere alternativa de melhor custo-benefício. (13) Fechamento — bloqueia `pedido_finalizado: true` enquanto endereço, pagamento ou tentativa de upsell estiverem faltando. (14) Linguagem — máx 3 frases por mensagem, máx 1 emoji. Commit `781ab2cf`.
67. **Sprint Segurança P2 — Rate Limiting + Helmet + prisma generate + fix as any (COMPLETO)**:
(a) `@nestjs/throttler@6.5.0` + `helmet@8.2.0` instalados. `ThrottlerModule.forRoot([{ ttl: 60_000, limit: 100 }])` em `AppModule` com `ThrottlerGuard` global. `@Throttle({ default: { limit: 5, ttl: 60_000 } })` em `POST /auth/login` (anti-bruteforce) e `@Throttle({ default: { limit: 10, ttl: 60_000 } })` em `POST /orders/public` (anti-spam). `helmet({ contentSecurityPolicy: false })` em `main.ts` antes do `json()` middleware (`CSP desabilitado` porque backend serve `/uploads` estático).
(b) `npx prisma generate` executado — client regenerado com todos os modelos incluindo `onlineOrder`, `whatsappConnection`, `whatsappAiSettings`, `whatsappConversation`, `whatsappMessage`, `orderItemComplement`, `complement`, `complementOption`. Todos os `(this.prisma as any).modelName` eliminados em: `orders.service.ts`, `whatsapp-ai.service.ts`, `whatsapp-ai/services/order-notification.service.ts`, `complements.service.ts`, `online-orders.service.ts`. Dois `as any` residuais mantidos justificados: `orderStatus: mapped as any` (mapeamento de string para enum OnlineOrderStatus) e `anthropicMsgs as any` (role string vs literal union). `tsc --noEmit` limpo. `npm run build` OK (373 arquivos).
(c) **Sprint 3 — Migrations (ANÁLISE, NÃO EXECUTADO)**: 20 entradas `--rolled-back` no `render.yaml` são harmless em produção pois o banco já tem todas as tabelas. Risco real: novo banco/restore de backup ficaria com lacunas. Solução: baseline migration (`prisma migrate diff --from-empty --to-schema-datamodel` + `migrate resolve --applied` no Supabase + remoção dos 20 `--rolled-back`). Requer acesso direto ao DATABASE_URL de produção para executar `migrate resolve --applied` na nova baseline. Não urgente enquanto não houver restore ou novo ambiente.
66. **Auditoria + correções P0/P1 de segurança (6 commits atômicos)**: auditoria Staff+ identificou nota 6.0/10. Aplicadas 6 correções sem alterar lógica de negócio. (B-01) `super-admin.service.ts` — SA_EMAIL/SA_PASSWORD movidos para env vars `SUPER_ADMIN_EMAIL`/`SUPER_ADMIN_PASSWORD` (commit `ca4e9d13`). (B-02) Fallback `|| 'secret'` removido de 4 arquivos (`jwt.strategy.ts`, `super-admin.guard.ts`, `super-admin.module.ts`, `super-admin.service.ts`) — app falha explicitamente se JWT_SECRET não setado (commit `040105dd`). (B-05/B-06) `docker-compose.yml` refatorado: porta 5432 removida do host, JWT_SECRET e POSTGRES_PASSWORD agora obrigatórios via `${VAR:?error}`, backend ganhou healthcheck, postgres pinado a `16.6-alpine`, criado `.env.example` (commit `410c61d2`). (A-08/A-09) `services/env.ts` e `app/api/warmup/route.ts` — URLs hardcoded de produção removidas, app avisa no console se NEXT_PUBLIC_API_URL não setada (commit `2bf3366c`). (A-12) `demo-vitrine.service.ts` — hash bcrypt fake `'$2b$10$demopasswordhash'` substituído por `bcrypt.hash('Demo@Driver123', 10)` real (commit `1be77456`). (A-13) `deleteCompany` no super-admin agora executa todos os 37 deleteMany dentro de um único `$transaction({ timeout: 60s })` — atomicidade garantida contra crash mid-delete (commit `c55f12ad`). Pendentes (P1-P4): rate limiting, Helmet, validação JWT no middleware, criptografia de apiToken, índices compostos, code dead cleanup.
68. **fix(demo): auto-bootstrap contas demo + fix interceptor 401 (COMPLETO)**: diagnóstico identificou 2 causas raiz. (1) Contas demo (`demo-basic@foodsaas.demo`, `demo-pro@foodsaas.demo`, `demo-enterprise@foodsaas.demo`) nunca existiram em prod — `POST /api/super-admin/demo/init` era manual e nunca foi chamado. (2) Interceptor Axios em `frontend/services/api.ts` redirecionava para `/login` em **qualquer** 401, incluindo o próprio `POST /auth/login` — usuário via tela de login em vez de toast de erro. Correções: (a) `DemoBootstrapService` novo (`src/modules/super-admin/demo-bootstrap.service.ts`) implementa `OnApplicationBootstrap` — aguarda `prisma.readyPromise`, conta usuários demo, se `count < 3` executa `initDemoCompanies()` + `populateAll()` automaticamente. No-op em restarts subsequentes. Registrado em `SuperAdminModule.providers`. (b) `frontend/services/api.ts` interceptor: `const isLoginAttempt = requestUrl.includes("auth/login")` — condição `!isLoginAttempt` garante que 401 no login mostre erro sem redirecionar. `tsc --noEmit` limpo em ambos. `npm run build` OK. Commit `30590af5`.
69. **fix(demo): resolve 403 em POST /auth/login para usuários DEMO (COMPLETO)**: causa raiz — `DemoGuard` (global APP_GUARD) não tinha whitelist de rotas públicas. Quando um DEMO token estava em `localStorage` (de sessão anterior), o interceptor Axios o injetava em **todas** as requisições incluindo `POST /auth/login`. O guard via `role=DEMO + method=POST` → `ForbiddenException(403)`. Loop auto-perpetuante: primeiro login bem-sucedido → DEMO token em localStorage → próxima tentativa de login → 403 eterno. Correção dupla (defesa em profundidade): (a) `backend/src/common/guards/demo.guard.ts` — whitelist `/auth/(login|signup|register)` no topo de `canActivate`, antes de qualquer verificação de role/método. Regex: `/\/auth\/(login|signup|register)/.test(url)` → `return true`. (b) `frontend/services/api.ts` request interceptor — `const skipAuth = config.url?.includes("auth/login") || auth/signup || auth/register` → não injeta `Authorization` header quando chamando endpoints de autenticação. `tsc --noEmit` limpo em ambos. `npm run build` backend + frontend OK. Commit `579d38ff`.
70. **feat(ia-demo): página pública de demo da IA Humanizada — Luna consultora comercial (COMPLETO)**: URL pública `https://food-system-sas-erp-frontend.vercel.app/ia-demo`. **Backend** — `ia.service.ts`: método `streamPlatformDemo(messages, res)` com SSE streaming via Anthropic (`claude-sonnet-4-6`, `ANTHROPIC_MODEL` env override). `PLATFORM_DEMO_SYSTEM_PROMPT` hardcoded com contexto completo: plataforma, módulos (PDV/Cardápio Digital/Cozinha/Estoque/Financeiro/BI/Delivery/WhatsApp IA), 3 planos (Basic/Pro/Enterprise), regras de comportamento consultivo, links de demo. Sem persistência em DB (stateless). `ia.controller.ts`: `POST /api/ia/platform-demo` — sem JWT, `@Throttle({default:{limit:15,ttl:60_000}})`, validação manual (máx 50 msgs × 2000 chars/msg), `@UseGuards(JwtAuthGuard)` movido de class para métodos individuais (endpoints autenticados existentes intactos). **Frontend** — `app/ia-demo/page.tsx` ("use client"): design dark premium com glow violeta radial, `Bot` avatar Luna, streaming chunk-by-chunk (cursor blink animado), `RichText` renderer para `**bold**` + newlines, 6 suggestion chips visíveis apenas no 1º turno, textarea auto-resize (máx 120px), spinner no botão durante loading. Footer: botão WhatsApp "Falar com Consultor" + botão "Testar o Sistema" (/demo), `env(safe-area-inset-bottom)` iPhone. `middleware.ts` + `ClientShell.tsx`: `/ia-demo` adicionado em PUBLIC_ROUTES de ambos. **ChatModule permanece desabilitado** (schema/code mismatch) — o novo endpoint vai direto no IaModule. Rate: 15 req/min/IP. Commit `d873091f`.
71. **fix(demo): demo banner no longer covers mobile header/hamburger on PDV**: causa — barra DEMO (`z-9999 top-0`) sobrepunha o mobile top bar (`z-40 top-0`). ClientShell: `isDemoUser = user?.role === "DEMO"` + `planLabel`. Mobile top bar: `top-8` (32px) quando isDemoUser. Layout wrapper: `md:pt-8 pt-[5.5rem]` (32+56=88px) quando isDemoUser. Banner: `env(safe-area-inset-top, 0px)` via inline style, mobile (<640px) exibe "Demo {plan} · Somente leitura" com `truncate max-w-[calc(100vw-4rem)]`, desktop texto completo. Commits `a4f60a70` + `2d5ee4c0`.
73. **feat(leads): melhorias de qualificação + stats + KPIs + follow-up log (COMPLETO)**: (a) `LeadsService.upsert()` retorna `null` e não persiste quando todos os 4 campos qualificadores (name, company, whatsapp, recommendedPlan) estão ausentes — visitantes anônimos não geram linhas no banco. (b) Auto-classificação: `status=QUALIFICADO` quando `recommendedPlan || whatsapp`; `NOVO` caso contrário; transição NOVO→QUALIFICADO em updates; nunca rebaixa de CONTATADO/PERDIDO. (c) `triggerFollowUp()` emite log JSON estruturado `{event:LEAD_QUALIFIED, leadId, nome, empresa, whatsapp, plano, resumo}` — arquitetura pronta para WhatsApp/E-mail/CRM via TODO(follow-up). (d) `getStats()`: `groupBy status` + `groupBy recommendedPlan` → `{total, novos, qualificados, contatados, perdidos, porPlano:{BASIC,PRO,ENTERPRISE}}`. (e) `SuperAdminController`: `GET leads/stats` adicionado ANTES de `GET leads` (match-by-order NestJS). (f) `/super-admin/leads`: 4 KPI cards (Total, Qualificados, Leads do Mês, Plano Mais Recomendado) acima da tabela; `Promise.all([listLeads, getStats])` na carga inicial. Commit `1c93f007`.
72. **feat(leads): captura persistente de leads da Kely (COMPLETO)**: model `Lead` no schema (sessionToken unique, name, company, whatsapp, recommendedPlan, conversationSummary, status DEFAULT 'NOVO'). Migration `20260606000000_add_lead` idempotente. `LeadsModule` (service upsert/findAll + controller POST público rate-limited 30req/min + GET admin com `@Roles(SUPER_ADMIN|ADMIN|MANAGER)`). `AppModule` registra `LeadsModule`. `SuperAdminModule` importa `LeadsModule`; `SuperAdminController` expõe `GET /api/super-admin/leads` (SuperAdminGuard). `ia-demo/page.tsx`: `SESSION_TOKEN = crypto.randomUUID()` gerado once no module scope; `buildConversationSummary(msgs)` pega últimas 10 msgs; `saveLead()` best-effort (`fetch /api/leads` silently ignores error); salva automaticamente ao detectar `[PLANO:X]` no streaming e ao submeter o form de lead. `/super-admin/leads/page.tsx`: tabela Nome/Empresa, WhatsApp (link wa.me), Plano badge colorido, Data, Status, expand resumo da conversa. `render.yaml`: `--rolled-back 20260606000000_add_lead` adicionado. Commit `786d03da`.
75. **fix(delivery): Fase 1 — Delivery Core Stabilization (COMPLETO)**: 7 arquivos alterados, zero novos models/migrations/rotas/módulos. (1) `orders.module.ts`: adicionado `exports: [OrdersService]` — prerequisito para os módulos de tracking e entregadores. (2) `tracking.gateway.ts`: import de `JwtService`+`OrdersService`+`OrderStatus`+`Logger`; `handleConnection` agora parseia JWT do handshake (`auth.token || query.token`) e armazena `{userId, companyId}` em `client.data` (clientes sem token conectam como públicos — sem rejeição); `server: Server` → `server!: Server` (TypeScript strict); `handleDelivered` substituído: `prisma.order.update` direto → `ordersService.updateStatus(orderId, DELIVERED, userId, companyId)` com guard de autenticação; `handlePickedUp` mantido (só atualiza `pickedUpAt`, não é transição de status). (3) `tracking.module.ts`: removido `PrismaService` provider direto (global); adicionado `imports: [AuthModule, OrdersModule]`. (4) `drivers.service.ts`: inject `OrdersService`; `assignOrder(orderId, driverId, companyId, userId)` — primeiro persiste `driverId+assignedAt` via Prisma direto, depois delega `OUT_FOR_DELIVERY` para `ordersService.updateStatus`. (5) `drivers.controller.ts`: `assignOrder` passa `req.user.userId`. (6) `drivers.module.ts`: adicionado `imports: [OrdersModule]`. (7) `app.module.ts`: adicionados `TrackingModule`+`DeliveryConfigModule` em imports; corrigido import path legado em `delivery-config.controller.ts` (`@/auth/jwt-auth.guard` → `@/common/guards/jwt-auth.guard`). Build backend OK (373 arquivos).
74. **feat(leads): WA Click Tracking P0 — rastreamento de conversão WhatsApp (COMPLETO)**: campo `waClickedAt DateTime?` adicionado ao model `Lead` (schema.prisma). Migration `20260606100000_add_wa_clicked_at` idempotente (`ADD COLUMN IF NOT EXISTS`). `render.yaml` atualizado com `--rolled-back 20260606100000_add_wa_clicked_at`. **Backend**: `LeadUpsertDto` recebe `waClicked?: boolean`; null-guard de sessão anônima agora inclui `&& !dto.waClicked` (clique WA sem dados ainda persiste); `findUnique` select adiciona `waClickedAt: true` para first-touch attribution; `upsert.create` define `waClickedAt: new Date()` quando `dto.waClicked`; `upsert.update` só seta `waClickedAt` se ainda não existir (first-touch, sem sobrescrever); `getStats()` adiciona 4º item ao `Promise.all`: `count({ where: { waClickedAt: { not: null } } })` → campo `waClicked: number` no `LeadStats`; `leads.controller.ts` sanitiza `waClicked: body.waClicked === true ? true : undefined`. **Frontend ia-demo**: `saveLead()` assinatura recebe `waClicked?: boolean`; `fetch` ganha `keepalive: true` (resolve iOS Safari que cancela requests ao abrir nova aba); 3 botões WA recebem `onClick={() => saveLead({ waClicked: true })}`: (1) Footer "Falar com Consultor", (2) inline CTA "Falar com consultor agora", (3) PlanCard "Solicitar proposta" via prop `onWaClick?: () => void`. **Frontend leads**: `LeadStats` recebe `waClicked: number`; grid muda para `grid-cols-2 md:grid-cols-5`; 5º KPI card "Cliques WhatsApp" com accent `emerald`; `ACCENT_CLASSES` adicionado `emerald`. `prisma generate` OK. Build backend + frontend OK.
77. **feat(whatsapp-ia): Mercado Pago + WhatsApp IA — pagamentos automáticos (COMPLETO + 5 riscos eliminados)**: integração end-to-end de PIX e Link de Pagamento (Cartão Crédito/Débito) via Mercado Pago no fluxo de atendimento WhatsApp IA. Arquitetura auto-contida no módulo `whatsapp-ai/` sem tocar `PaymentsModule`. (1) `claude-cart.service.ts`: `SolicitacaoPagamento` interface adicionada a `StructuredResponse`; system prompt da Carol recebe 3 novas diretrizes (15-17): coleta `formaPagamento` (pix/credit_card/debit_card), ativa `solicitacao_pagamento.requer_acao=true` ao finalizar, informa ao cliente o próximo passo; JSON de resposta obrigatório inclui campo `solicitacao_pagamento`. (2) `wa-payment.service.ts` (novo): `WaPaymentService` com `createPix(orderId, companyId, total, customerPhone, customerName, description)` → chama `POST /v1/payments` MP com `payment_method_id: "pix"` + `notification_url = /api/whatsapp-ai/webhook/mp-payment` + `external_reference = "WA_ORDER|orderId|companyId"`; `createPaymentLink(...)` → chama `POST /checkout/preferences` MP; ambos retornam mock quando `MERCADOPAGO_ACCESS_TOKEN` não configurado; **HTTPS warning** no getter `backendUrl` — loga `WARN` se não começar com `https://` (Render env var `BACKEND_URL` obrigatória em prod). (3) `whatsapp-ai.module.ts`: importa `ConfigModule` + `forwardRef(() => OrdersModule)` (circular dep resolvido). (4) `whatsapp-ai.service.ts`: injects `WaPaymentService`; `@Inject(forwardRef(() => OrdersService)) private ordersService?` (circular dep); **`normalizePaymentMethod(raw)`** converte aliases PT-BR ("cartão", "débito", "pix") para `pix|credit_card|debit_card`; `runClaudeStructuredResponse` usa `normalizePaymentMethod(rawMetodo)` antes de ramificar PIX vs link; `handleMpPaymentWebhook` agora: (a) **idempotência** — `findFirst({status})`, skip se já `CONFIRMED`; (b) **substitui bypass** — chama `ordersService.updateStatus(orderId, CONFIRMED, 'SYSTEM', companyId)` que dispara estoque+fidelidade+socket+WhatsApp-notif; fallback para `prisma.order.update` apenas se `ordersService` não disponível. (5) `whatsapp-ai.controller.ts`: `@Post('webhook/mp-payment')` adicionado ANTES de `@Post('webhook/:connectionId')` (match-by-order). (6) `orders.module.ts`: `forwardRef(() => WhatsappAiModule)`. (7) `PaymentsModule` reabilitado: `payments.service.ts` + `payments.controller.ts` corrigidos (`MERCADOPAGO` → `MERCADO_PAGO`); `app.module.ts` importa `PaymentsModule`. `tsc --noEmit` limpo. `npm run build` OK.

76. **feat(delivery): Fase 2 — DeliveryZone conectado ao fluxo de criação de pedidos (COMPLETO)**: raiz do bug: `Order.deliveryFee` vinha sempre do frontend sem validação; menu sempre enviava `deliveryFee: 0`; `driverFee` nunca era populado; `deliveryZoneId` não existia no model. Correções end-to-end: (1) `schema.prisma`: `Order` recebe `deliveryZoneId String?` + `deliveryZone DeliveryZone? @relation(...)` + `@@index([deliveryZoneId])`; `DeliveryZone` recebe `orders Order[]`. (2) Migration `20260607000000_add_delivery_zone_to_order` idempotente (`ADD COLUMN IF NOT EXISTS` + FK via `information_schema` check + `CREATE INDEX IF NOT EXISTS`). (3) `render.yaml`: +1 `--rolled-back` entry. (4) `prisma generate`. (5) `delivery-config.controller.ts`: `@UseGuards(JwtAuthGuard)` movido de class para cada método protegido; novo `GET /delivery-config/public?companyId=X` sem auth (retorna apenas `id/name/neighborhood/clientFee/type`, **NÃO** expõe `driverShare`). (6) `delivery-config.service.ts`: `findAllPublic(companyId)` com `select` seguro. (7) `orders.service.ts`: inject `DeliveryConfigService` via `@Optional()`; `create()` — se `deliveryZoneId` enviado pelo frontend, busca zona por id+companyId; senão se `neighborhood` presente busca por `getFeeForNeighborhood`; se zona encontrada: seta `driverFee = zone.driverShare`, `deliveryZoneId = zone.id`, override `deliveryFee` apenas quando frontend enviou 0. (8) `orders.module.ts`: import `DeliveryConfigModule`. (9) `online-orders.service.ts`: inject `DeliveryConfigService`; `create()` — mesma lógica de lookup por `neighborhood`; `total` recalculado como `subtotal - discount + deliveryFee` (corrige bug onde menu enviava `total = subtotal - discount` sem taxa). (10) `online-orders.module.ts`: import `DeliveryConfigModule`. (11) `OrderDetailsForm.tsx` (PDV): `OrderDetails` recebe `deliveryZoneId?: string`; busca `GET /delivery-config/public?companyId=X` ao montar; quando `bairro` muda → auto-match de zona → preenche `deliveryFee` + `deliveryZoneId` + mostra badge "Taxa: R$ X,XX" ao lado do label. (12) `app/pdv/page.tsx`: `closePaidOrder` envia `neighborhood: details.bairro` + `deliveryZoneId: details.deliveryZoneId` no POST /orders. (13) `app/menu/[companyId]/page.tsx`: `deliveryZones` state; fetch de zonas em `loadMenu`; `selectedZone` auto-matched quando `neighborhood` muda (input manual ou via CEP autocomplete); `deliveryFee: selectedZone?.clientFee ?? 0` no POST; linha "Taxa de entrega" com valor real ou "Bairro sem cobertura" exibida no resumo do checkout; total exibido inclui a taxa. `tsc --noEmit` limpo. `npm run build` OK (backend + frontend).

78. **feat(drivers+whatsapp): DriverEarning/Payment pipeline + WA order via ordersService (COMPLETO)**: `DriverPaymentStatus` enum (PENDING/PAID) + `DriverEarning` model (`orderId @unique`, `customerFee`, `driverAmount`, `platformFee`, `driverPaymentId?`) + `DriverPayment` model (`earnings[]`, `totalAmount`, `paidAt?`, `financialId?`). Migration `20260610000000_add_driver_earnings` idempotente. `prisma generate` limpo. `orders.service.ts`: após transação DELIVERED, `setImmediate` cria `driverEarning.upsert` (condição: `orderType=DELIVERY AND driverId!=null AND driverFee>0`); fire-and-forget com `console.error` no catch. `drivers.service.ts`: `listEarnings`, `listPayments`, `myEarnings`, `myPayments`, `createPayment` (batch de PENDING → `DriverPayment`), `payPayment` (cria `Financial(EXPENSE, REPASSE_ENTREGADOR)` + marca PAID em transação). `drivers.controller.ts`: `GET me/earnings`, `GET me/payments` (para driver logado); `GET :id/earnings`, `GET :id/payments`, `POST :id/payments` (admin cria batch), `PATCH payments/:paymentId/pay` (admin quita) — todos com `@Roles(ADMIN|MANAGER|SUPER_ADMIN)` menos os `me/*`. **Também neste commit**: WhatsApp IA `createOrderFromCart` reescrito para usar `ordersService.create` (estoque, loyalty, socket, WA notif todos disparados); `bairro` adicionado ao `CartStatus` interface + system prompt guia Carol a extrair bairro sem inventar. `tsc --noEmit` limpo. `npm run build` OK. Commit `c987b664`.

79. **fix(tables): persistência real de mesas via Order.tableId (COMPLETO)**: `tableItems` era estado React efêmero — mesa ficava OCCUPIED após reload mas modal exibia R$0,00. Correção: `Order.tableId String?` + `Table.dineInOrders Order[]` (back-relation separada de `TableOrder[]` já existente). Migration `20260611000000_add_table_id_to_order` idempotente (`ADD COLUMN IF NOT EXISTS` + FK pg_constraint + `CREATE INDEX IF NOT EXISTS`). `orders.service.ts create()`: salva `tableId` no order; DINE_IN block aceita `tableId` direto além de `tableNumber` para marcar Table OCCUPIED. `tables.service.ts findAll()`: inclui `dineInOrders { where: { status: notIn[CANCELLED] }, include: { items } }`. `app/tables/page.tsx`: ao abrir mesa reconstrói `tableItems` de `table.dineInOrders[].items[]` (banco real); `sendOrderToKitchen` passa `tableId: selectedTable.id`; `closeTable()` calcula total dos Orders persistidos (fallback para `tableTotal` local se banco retornar 0). Kitchen preservada: `Order` continua sendo criado normalmente para cada item. `tsc --noEmit` limpo em backend e frontend. `npm run build` OK. Commit `4674a7ab`.

80. **feat(integrations): Provider Pattern iFood — Passos 1-8 completos (commit `0083c8f1`)**: módulo `src/modules/integrations/` completo. `IntegrationProvider` enum (IFOOD/RAPPI/MOCK) + 4 novos models: `IntegrationConfig`, `IntegrationOrder`, `IntegrationEventLog`, `ProductCatalogMap`. `Order` recebe `channel String @default("PDV")` + `externalOrderId String? @unique`. Migration `20260615000000_add_integrations` idempotente. `IIntegrationProvider` interface + `MockProvider` (sandbox, sempre válido, id externo = interno) + `IfoodProvider` (HMAC sha256) + `IntegrationProviderFactory`. `IntegrationsService`: `processWebhook` (ACK imediato via `setImmediate`), idempotência via `externalOrderId @unique`, funneling para `ordersService.create/updateStatus`. `IntegrationsController`: admin endpoints com JWT+Roles; webhook público rate-limited 60/min. Página `/integracoes` com 4 tabs (Provedores, Mapeamento, Pedidos, Log). Sidebar: item "Integrações" com ícone `Cable` na seção Marketplace. **rawBody risk**: `main.ts` não tem `rawBody: true` — HMAC real do iFood usará JSON.stringify fallback (não bloqueia MOCK/sandbox). `render.yaml`: `--rolled-back 20260615000000_add_integrations` adicionado. `orders.service.ts`: `findAllForKitchen` usa `o.channel` como source; `updateKitchenStatus` roteia `source !== ONLINE` para `updateStatus`.

81. **fix(menu): apply company theme color to CSS variable (commit `16fb5fb8`)**: `app/menu/[companyId]/page.tsx` — ao carregar tema, `document.documentElement.style.setProperty("--color-primary", primary)` sincroniza a CSS variable global com a cor da empresa. Antes, elementos com `bg-[var(--color-primary)]` sempre mostravam o laranja hardcoded do `globals.css (#f97316)` em vez da cor configurada. Elementos com `style={{ backgroundColor: theme.primaryColor }}` já funcionavam corretamente.

82. **feat(kitchen+orders): impressão profissional 80mm + source badges reais (commit `480f341e`)**: `app/kitchen/page.tsx` — `printKitchenOrder` artesanal (300px, CSS inline, `window.open` manual) substituído por 3 linhas usando `buildKitchenTicket` + `printTicket` (80mm `@page`, fontes 22px, complementos, fonte bold para cozinha). `KitchenBoard.tsx` + `app/orders/page.tsx` — badge de fonte agora exibe o valor real (`order.source ?? "PDV"`) com cores distintas: ONLINE=azul, MOCK=roxo, IFOOD=vermelho, PDV/outros=cinza. Antes: todos não-ONLINE mostravam "PDV" independente da fonte real.

83. **feat(printing): FoodSaaS Printer Agent — Phase 1 (commit `fc0f69b5`)**: arquitetura multi-setor sem quebrar sistema existente. **Backend**: `orders.service.ts findAllForKitchen` inclui `product.category.categoryType` nos itens PDV via Prisma `include { product { include { category { select { categoryType } } } } }`; para itens de `OnlineOrder` (JSON field sem relação direta), faz batch-lookup por `productId` em uma única query e mapeia `categoryType`. **Frontend — novos arquivos**: `printDispatcher.ts` (abstração entre templates e dispositivos; fase 2 hook point para ESC/POS Agent; `printAllTickets()` com gap 350ms anti-throttle popup); `BarTicket.tsx` (template 80mm setor bar: somente bebidas, sem preços, fontes grandes, destaque azul, filtra `categoryType === "bebidas"`); `DeliveryLabel.tsx` (etiqueta delivery: endereço grande, pagamento, troco highlight CASH, sem custos internos); `PrintRouterService.ts` (classifica itens por `categoryType` → KITCHEN ou BAR; determina DELIVERY se `orderType === "DELIVERY"`; sempre adiciona CASHIER; métodos `printAll()`, `printSector()`, `route()`). **Frontend — pontos de chamada**: `app/kitchen/page.tsx` usa `PrintRouterService(KITCHEN+BAR)`; `app/orders/page.tsx` usa `PrintRouterService` com todos os setores; `app/historico-pedidos/page.tsx` idem; `app/pdv/page.tsx` ganha **impressão automática pós-pagamento** (sem nenhuma antes), monta `PrintableOrder` do carrinho local com `companyName` fetchado via `/company/:id`. `tsc --noEmit` limpo. Sem migrations. Sem novos models.

84. **fix(tables): P0 — sync selectedTable + auto-confirm + rollback (commit `434f580b`)**: 3 bugs raiz: (1) `selectedTable` era set uma vez no click e nunca atualizado quando `fetchTables` refrescava a lista — `dineInOrders` ficava stale, `closeTable` calculava R$0,00. Fix: `fetchTables` agora chama `setSelectedTable(prev => fresh.find(t => t.id === prev.id) ?? prev)` para sincronizar o objeto. (2) `sendOrderToKitchen` criava pedido PENDING mas nunca confirmava — estoque nunca consumido, stock-consume nunca rodava. Fix: após `POST /orders` bem-sucedido, chama `PATCH /orders/:id/status CONFIRMED` (mesmo fluxo do PDV). Rollback do item otimista em caso de erro. (3) `tableItems` era reconstruído manualmente em dois lugares inconsistentes. Fix: `useEffect([selectedTable])` único que reconstrói `tableItems` de `selectedTable.dineInOrders` sempre que o objeto muda — inclusive após sync do fetchTables. Também: removido `saveTableOrder` (dead code, usava `PATCH /tables/:id/order` inexistente), simplificado `closeTable` (sem fallback para stale `tableTotal`), `addItemToTable` passa `orderType: "DINE_IN"`. `tsc --noEmit` limpo.

85. **fix(demo+keepalive): tema/sidebar demos + keepalive WhatsApp IA (commit `b789135`)**: 2 bugs raiz na sidebar de demos. (1) `DemoBootstrapService.ensureDemoAccounts()`: branch "demos já existem (count >= 3)" só chamava `patchDemoCategoryNames()` — `initDemoCompanies()` não rodava em deploys subsequentes, então `CompanyTheme` e módulos nunca eram re-aplicados. Fix: novo método `DemoVitrineService.patchDemoThemesAndModules()` que roda idempotente a cada boot, garante `primaryColor` correto (Basic=#16a34a, Pro=#2563eb, Enterprise=#7c3aed) e `ALL_MODULES` ativos via `companyModule.upsert` para cada demo. Wired no bootstrap "demos present" branch. (2) `ClientShell.tsx` slug extraction usava `??` (null-coalescing), mas `moduleSlug` tem `@default("")` no schema — empty string não cai no fallback `?? m.module`. Fix: trocado `??` por `||` + `.toLowerCase()` → "TABLES" e "tables" viram sempre "tables". Keepalive: intervalo 13 min → 5 min + `visibilitychange` listener (pinga ao tab recuperar foco). `vercel.json`: cron `*/5 * * * *` em `/api/warmup` (requer Vercel Pro; em Hobby funciona como fallback). Sem migrations, sem novos models.

87. **feat(printers): FoodSaaS Printer Agent — Phase 2 infrastructure (commit `f5554457`)**: infraestrutura de banco + API para impressoras sem quebrar `window.print()`. **Schema**: enums `PrinterConnectionType` (BROWSER/NETWORK/USB), `PrinterPaperWidth` (MM_58/MM_80), `PrinterRole` (KITCHEN/BAR/COUNTER/DELIVERY), `PrintJobStatus` (PENDING/SENT/PRINTED/FAILED). Models: `Printer` (companyId, name, brand, connectionType, address, paperWidth, isOnline, isActive, lastSeenAt), `PrinterProfile` (`@@unique[printerId,role]`), `PrinterJob` (companyId, printerId, orderId?, template, payload Json, status, attempts, printedAt?, failReason?). Back-relations em Company. Migration `20260620000000_add_printer_models` idempotente. `prisma generate` limpo. **Backend**: `PrintersModule` com `PrintersService` (CRUD printers + upsertProfile com ownership check + findJobs + enqueueJob), `PrintersController` GET/POST/PATCH/DELETE para `/printers`, `/printers/profiles`, `/printers/jobs`, `/printers/:id/heartbeat`. Registrado em `app.module.ts`. **Frontend**: `app/impressoras/page.tsx` — 4 tabs (Impressoras / Perfis / Fila / Histórico), badges online/offline, toggle isActive, status jobs com ícones coloridos. Sidebar: item "Impressoras" ícone `Printer` na seção Marketplace. `render.yaml`: `--rolled-back` adicionado. **Preservado**: `window.print()`, KitchenTicket, Receipt80mm, PrintRouterService — comportamento atual 100% intacto. Phase 3 (ESC/POS agent polling) consome jobs via `PATCH /printers/jobs/:id/status`. `tsc --noEmit` + `npm run build` OK.

86. **fix(whatsapp-ia): suporte Evolution API v1+v2 + logs diagnóstico (commit `ff04c32`)**: Bug raiz: `handleEvolutionWebhook` lia `data.key` e `data.message` diretamente (Evolution v1), mas Evolution API v2 envolve cada mensagem em `data.messages[0]` — `phone=""` e `text=""` → `processIncoming` retornava sem resposta (Kely muda). Fix cirúrgico: `const msgData = Array.isArray(data?.messages) && data.messages.length > 0 ? data.messages[0] : data` — suporta ambos formatos sem quebrar v1. Extras: ignora grupos (`@g.us`/`@broadcast`), extrai texto de `buttonsResponseMessage`, normaliza evento com `-` além de `_`. Logs `WARN` adicionados em todas as saídas silenciosas de `processIncoming` (connection não encontrada, `isActive=false`, `settings=null`, `settings.isActive=false`, `mode=MANUAL`) para diagnóstico via Render logs. **Checklist diagnóstico**: (1) `/api/whatsapp-ia` → Conexões → verificar `isActive=true`; (2) Configurações IA → verificar `isActive=true` e `mode=AUTO`; (3) Conversas → verificar se conversa não está em modo `HUMAN`; (4) Render logs: buscar por `[AI]` e `[WH]` para ver o motivo exato da falha. `tsc --noEmit` limpo. Sem migrations.

93. **feat(whatsapp-ai): Kely enriquecida — contexto completo do sistema + fallback Gemini automático (COMPLETO)**: 2 arquivos alterados, zero migrations. `whatsapp-ai.service.ts`: auto-reset HUMAN mode (checa timestamp do último `ASSISTANT` message — se > 60 min, `update({ mode:'AI' })` e continua processando); `runClaudeStructuredResponse` busca `deliveryZones` + `pizzaBorders` (com sizes) + `settings.businessHours` + `settings.paymentMethods` e passa os 4 novos params (`deliveryContext`, `pizzaBordersContext`, `businessHoursInfo`, `paymentInfo`) para `claudeCart.chat()`. `claude-cart.service.ts`: sistema prompt expandido com seção `━━━ QUEM VOCÊ É ━━━` (Kely como voz da empresa, não só bot de pedidos), 11 diretrizes novas para responder dúvidas (bairros, pagamento, horário, ingredientes, bordas), seções condicionais `━━━ INFORMAÇÕES OPERACIONAIS ━━━` / `━━━ ENTREGA ━━━` / `━━━ BORDAS RECHEADAS ━━━` injetadas dinamicamente quando dados existem. **Fallback Gemini**: `chat()` refatorado em 3 métodos — `chatAnthropic()` tenta Anthropic; se falhar (sem créditos = HTTP 4xx ou qualquer erro), `log.warn` + chama `chatGemini()` com mesmo system prompt e histórico; Gemini usa `responseMimeType:'application/json'` + `role:'model'` para assistant. Fallback 100% transparente para o cliente. `tsc --noEmit` limpo. `npm run build` OK (exit 0).

91. **fix(whatsapp-ai): observabilidade — debug→warn em todos os pontos de bloqueio (COMPLETO)**: auditoria identificou 4 bugs de observabilidade em `whatsapp-ai.service.ts`: (1) `conv.mode === 'HUMAN'|'PAUSED'` (L504) tinha `log.debug` → invisível em Render prod → trocado para `log.warn` com contexto `phone=`. (2) `settings.mode === 'MANUAL'` (L516) idem → `log.warn`. (3) `!rawResponse` após `geminiChat` (L623) — NENHUM log nem fallback — quando Gemini retorna 200 OK com `candidates:[]` (filtro de segurança/quota), IA ficava completamente silenciosa sem nenhum log. Fix: adiciona `log.warn` + envia mensagem de fallback ao cliente. (4) event-drop em `handleEvolutionWebhook` (L369) tinha `log.debug` → trocado para `log.log` (visível em prod). **Causa raiz mais provável do sintoma "Kely não responde"**: conversa em `mode=HUMAN` (operador transferiu manualmente em algum momento — afeta todas as mensagens futuras do mesmo número). **Verificação**: `SELECT id, mode FROM "WhatsappConversation" WHERE "companyId"='xxx' AND mode='HUMAN'` — se encontrar, executar `UPDATE SET mode='AI'`. `tsc --noEmit` limpo. `npm run build` OK.

90. **feat(delivery-tracking): Central de Rastreamento Admin (COMPLETO)**: página `/delivery-tracking` com mapa Leaflet em tempo real dos entregadores. `hooks/useDriverTracking.ts` — conecta ao socket `/tracking` (usando `getTrackingSocket` existente), emite `track:order` para cada pedido `OUT_FOR_DELIVERY` ativo (entra no room `order:{id}` que o TrackingGateway já usa), ouve `driver:location` para atualizar posições em tempo real; HTTP fallback `setInterval(30s)` via GET `/drivers` + GET `/orders/kitchen`; semeia `liveLocations` com `currentLat/currentLng` do DB (GPS persistido). `components/delivery-tracking/TrackingMap.tsx` — react-leaflet com `dynamic({ssr:false})`; ícone 🛵 laranja para driver em rota, cinza para disponível via `L.divIcon`; fix do webpack (delete `_getIconUrl`, `L.Icon.Default.mergeOptions` com CDN unpkg); Popup com nome, veículo, placa, endereço de entrega, "GPS ao vivo · HH:MM". `app/delivery-tracking/page.tsx` — 4 KPI cards (Online/Em rota/Disponíveis/Atualização 30s), split layout `flex-col lg:flex-row`, mapa flexível + lista lateral 320px; `DriverRow` mostra badge colorido de status. `ClientShell.tsx` — "Rastreamento" com `MapPinned` na seção Operação, roles ADMIN/MANAGER. `npm install leaflet react-leaflet @types/leaflet` (v1.9.4 / v5.0.0 / v1.9.21). `tsc --noEmit` limpo. `npm run build` OK (59 páginas). **Não alterados**: TrackingGateway, schema, migrations, DriverEarning, DriverPayment, DeliveryZone.

89. **feat(driver-pwa): DTO Location Validation + Fase C Tracking+GPS (COMPLETO)**: (a) `backend/src/modules/drivers/dto/update-location.dto.ts` criado — `@IsNumber @Min(-90) @Max(90) lat` e `@IsNumber @Min(-180) @Max(180) lng`. Ambos endpoints `PATCH /me/location` e `PATCH /:id/location` passaram a usar `UpdateLocationDto`. (b) `frontend/services/trackingSocket.ts` — singleton para namespace `/tracking` com auth JWT no handshake; `disconnectTrackingSocket()` para cleanup. (c) `app/driver/page.tsx` reescrito: conecta `trackingSocket` no mount + inicia `navigator.geolocation.watchPosition` (throttle 5s) após socket conectado; emite `driver:register(profileId)`, `driver:location({driverId, orderId, lat, lng})`; `orderId` é o primeiro `OUT_FOR_DELIVERY` ou primeiro ativo; botão "Coletei" → `driver:picked_up({orderId})` + update otimista de status; botão "Entregue" → `driver:delivered({orderId})` via socket (TrackingGateway chama `ordersService.updateStatus(DELIVERED)`) + reload após 1.2s; indicador visual de GPS ativo (bolinha verde + texto "transmitindo localização"). `tsc --noEmit` limpo em backend e frontend. `npm run build` OK.

88. **feat(driver-pwa): PWA Entregador Fase A+B — backend P0 + frontend core (COMPLETO)**: MVP funcional do app do entregador sem GPS/mapas. **Fase A Backend** (`drivers.service.ts` + `drivers.controller.ts`): (a) `updateMyLocation(userId, lat, lng)` — ownership via JWT userId, nunca por param URL; (b) `availableOrders(userId)` — retorna `Order[status=READY, driverId=null, companyId=profile.companyId]`; (c) `acceptOrder(userId, orderId)` — `$transaction(Serializable)` check-and-assign atômico; `ConflictException(409)` se `driverId!==null`; `ordersService.updateStatus(OUT_FOR_DELIVERY)` fora da tx (garante stock+socket+loyalty); (d) `myOrders` inclui `READY` além de `OUT_FOR_DELIVERY|DELIVERED`; (e) todos endpoints admin com `@UseGuards(RolesGuard) @Roles(ADMIN...)` (findAll, findOne, create, update, PATCH :id/location, assignOrder). Rota order: todos `me/*` ANTES de `/:id`. **Fase B Frontend**: `stores/auth.store.ts` adiciona `isDelivery()`. `components/ClientShell.tsx`: `isDriverPage = pathname.startsWith("/driver")` — bypass admin sidebar, render ThemeProvider+Toaster apenas; redirect `useEffect` envia DELIVERY → `/driver`. 5 páginas criadas em `app/driver/`: `layout.tsx` (Bottom Nav 5 itens: Início/Entregas/Ganhos/Histórico/Perfil, `fixed bottom-0`, `env(safe-area-inset-bottom)`), `page.tsx` (home: card perfil + pedidos ativos com botões "Coletei"/"Entregue" — sem funcionalidade ainda), `orders/page.tsx` (GET /me/available, botão Aceitar → POST /me/accept/:orderId, 409 mostra toast e recarrega), `earnings/page.tsx` (GET /me/earnings + /me/payments, KPI saldo pendente + total recebido), `history/page.tsx` (GET /me/orders filtrado DELIVERED), `profile/page.tsx` (GET /me, somente leitura). `public/manifest.json` criado (standalone, start_url=/driver). `tsc --noEmit` limpo. `npm run build` OK (backend 373 arquivos, frontend /driver + /driver/* 5 rotas compiladas). **Riscos remanescentes**: GPS/socket/tracking não implementados (Fase C); botões "Coletei"/"Entregue" sem funcionalidade (`toast("Em breve")`)— precisam de Fase C; lat/lng sem validação de range no DTO.

96. **Diagnóstico ao vivo: WhatsApp IA Cloud API não entrega mensagens reais (07/06/2026)**:
sessão de debug remoto via Chrome MCP revelou stack de causas combinadas. **Bug confirmado em `isBusinessHours()` `whatsapp-ai.service.ts:170-176`**: quando `businessHoursEnd < businessHoursStart` (horário cruza meia-noite, comum em pizzaria — ex: 18h-02h), a comparação `cur >= start && cur <= end` retorna **sempre false** → Kely responde sempre `settings.offlineMessage`. Caso real: settings estava `08:00 → 01:00` bloqueando dia inteiro. **Fix proposto** (não aplicado nesta sessão — task chip `task_12b8b501`): detectar overnight e usar OR (`if (endMin > startMin) return cur >= startMin && cur <= endMin; return cur >= startMin || cur <= endMin;`). **Causa raiz #2 (Meta config)**: app Meta no modo "Em desenvolvimento" bloqueia mensagens reais — solucionado durante a sessão clicando toggle "Ao vivo" no painel `developers.facebook.com/apps/1764223077628003`. **Causa raiz #3 (Meta blocker, NÃO resolvido)**: Cloud API exige (a) forma de pagamento cadastrada (Meta avisou "Está faltando uma forma de pagamento para 4 das suas contas") e (b) número totalmente migrado para WABA — o chip 556793512844 estava no app WhatsApp Business comum no celular, então mensagens chegavam com ✓✓ cinza no celular mas NUNCA disparavam o webhook. **Aprendizado UI §64**: opção "WhatsApp Business (Recomendado)" no modal Nova Conexão cria registro com `provider:"EVOLUTION"` mas `apiUrl=""` e `instanceName=""` — sem servidor Evolution provisionado em algum lugar, esse caminho não funciona. Não é fallback gratuito como o label "Recomendado" sugere. **Estado final da sessão**: businessDays expandido para `0,1,2,3,4,5,6` + horário `00:00-23:59` via API (mascara o bug enquanto não há fix), conexão Evolution vazia deletada, Cloud API ativa mas bloqueada pelos requisitos Meta acima. Decisão do usuário: voltar e resolver Cloud API.

95. **feat(orders): Order#number + OrderType enum + @CompanyId + auto-free-table (COMPLETO, commit `e9d50e50`)**: 8 arquivos, migration idempotente, zero breaking change. (1) `schema.prisma`: `Order.number Int @default(0)` — número sequencial por tenant para exibição humana (Pedido #42); `Order.orderType` convertido de `String @default("DINE_IN")` para `OrderType @default(DINE_IN)` (enum já existia no PostgreSQL via `OnlineOrder`); índices compostos `@@index([companyId, status])` + `@@index([companyId, createdAt])` adicionados. (2) Migration `20260608100000_add_order_number_enum_indexes` idempotente: `ADD COLUMN IF NOT EXISTS "number"` + `DO $$ IF data_type='text' THEN ALTER COLUMN "orderType" TYPE "OrderType" USING "orderType"::"OrderType" END IF $$` + `CREATE INDEX IF NOT EXISTS` para os 2 novos índices. (3) `orders.service.ts create()`: dentro da transação Serializable, `findFirst({orderBy:{number:'desc'}})` → `nextNumber = (last?.number ?? 0) + 1` → salvo no create. (4) `orders.service.ts updateStatus()`: ao atingir DELIVERED ou CANCELLED com `tableId` não-nulo, `count({tableId, id:{not:id}, status:{notIn:[DELIVERED,CANCELLED]}})` — se 0 pedidos ativos restantes, `table.update({status:'FREE'})` dentro da mesma transação. (5) `@CompanyId()` decorator criado em `src/common/decorators/company-id.decorator.ts` — extrai `req.user.companyId` do JWT, lança `UnauthorizedException` se ausente. Todos os endpoints protegidos de `orders.controller.ts` migrados de `req.user.companyId` para `@CompanyId() companyId: string`. (6) `demo-vitrine.service.ts`: `orderType` cast para `OrderType` enum após `prisma generate`. (7) `render.yaml` + `Dockerfile` com `--rolled-back` para as 2 novas migrations. `tsc --noEmit` limpo. `npm run build` OK.

94. **refactor(multi-tenant): unificação Order + Kely via ProductsService (COMPLETO)**: 7 arquivos, migration idempotente, zero breaking change. (1) `prisma/schema.prisma`: campo `neighborhood String?` adicionado ao model `Order` (bairro agora persiste no pedido, não só no lookup de zona). (2) Migration `20260608000000_add_neighborhood_to_order`: `ALTER TABLE "Order" ADD COLUMN IF NOT EXISTS "neighborhood" TEXT`. (3) `Dockerfile`: `--rolled-back 20260608000000_add_neighborhood_to_order` adicionado. (4) `orders.service.ts create()`: salva `neighborhood: data.neighborhood || null` no Prisma create (antes era usado só para lookup e descartado). (5) `products.module.ts` + `categories.module.ts`: adicionados `exports: [ProductsService]` / `exports: [CategoriesService]` — agora exportáveis para outros módulos. (6) `whatsapp-ai.module.ts`: imports `forwardRef(() => ProductsModule)` + `forwardRef(() => CategoriesModule)` adicionados. (7) `whatsapp-ai.service.ts`: `@Optional() ProductsService` + `@Optional() CategoriesService` injetados no constructor; `runAiResponse` substitui queries Prisma diretas (`prisma.product.findMany` + `prisma.category.findMany`) por `productsService.publicMenu(companyId)` + `categoriesService.findAll(companyId)` com fallback automático para Prisma quando services não disponíveis. Efeito: qualquer mudança em `ProductsService.publicMenu` (soft-delete, sortOrder, isActive, novas inclusões) reflete automaticamente no cardápio que a Kely usa, sem duplicar lógica. `tsc --noEmit` limpo. `npm run build` OK (exit 0).

97. **fix(whatsapp-ai): Gemini fallback automático quando Anthropic sem créditos (commit `0158f6c5`, deployado via `9fdf9a1b`)**: `runAiResponse` em `whatsapp-ai.service.ts` não tinha try/catch em torno de `anthropicChat()` — quando Anthropic retorna 400 "credit balance too low", a exceção propagava para o outer catch que enviava "Desculpe, tive um problema temporário". Fix: `anthropicChat()` envolvido em try/catch; no catch, loga WARN e chama `geminiChat()` com `this.config.get('GEMINI_MODEL') ?? settings.aiModel ?? 'gemini-2.0-flash'` como fallback transparente. Também corrigido modelo padrão `gemini-1.5-flash` (deprecated, retorna 404) → `gemini-2.0-flash` em 3 locais: `whatsapp-ai.service.ts` (2x) e `claude-cart.service.ts` (1x). **DEPLOYADO** em 09/06/2026 via deploy manual `9fdf9a1b` ("chore: force redeploy"). Confirmar via `GET /api/whatsapp-ai/health` → `{"anthropicKeySet":true,"geminiKeySet":true,...}`. **Causa raiz do build travado**: commit `8ffa4be5` importava `WhatsappAiPromptService` que só foi criado em `a3da8efb` (commit seguinte) — importação de arquivo inexistente causou falha em 1m. Deploy de `0158f6c5` ficou 1h22m em "Awaiting build logs" por freeze do Render (container de build não alocado). Solução: cancelar via UI + "Deploy latest commit" manual. Ver item 100.

98. **Bridge local Baileys (C:\Users\Ruffinus Pizzaria\Desktop\qr-scan\bridge.mjs)**: script Node.js que conecta WhatsApp via Baileys e faz ponte com FoodSaaS. Roda via PM2 como `kely-bridge`. **Bug LID corrigido nesta sessão**: `msg.phone` do outbox continha JID completo (`20298166485236@lid`); bridge concatenava `@s.whatsapp.net` criando JID inválido (`@lid@s.whatsapp.net`) → crash loop. Fix: `if (msg.phone.includes('@')) jid = msg.phone` (usa JID direto sem transformação). **Keepalive PM2** `kely-keepalive` criado em `C:\Users\Ruffinus Pizzaria\Desktop\qr-scan\keepalive.mjs` — pinga `https://food-system-backend-no7d.onrender.com/api/` a cada 4 min para evitar Render free tier dormir. `pm2 save` persiste ambos processos. **Limitação**: bridge e keepalive só funcionam enquanto o PC estiver ligado. Solução permanente: UptimeRobot (uptimerobot.com) + Evolution API no Render.

99. **Evolution API no Render (https://evolution-api-kely.onrender.com)**: serviço deployado em `09/06/2026` para substituir a bridge local como relay WhatsApp 24/7. Usa banco Neon PostgreSQL separado. Redis mostra `redis disconnected` em loop mas cai para cache em memória e funciona. Próximos passos: (1) configurar instância no manager da Evolution API, (2) apontar webhook para `https://food-system-backend-no7d.onrender.com/api/whatsapp-ai/webhook/:connectionId`, (3) atualizar a `WhatsappConnection` no FoodSaaS com `apiUrl`, `instanceName` e `apiToken` da Evolution API, (4) desligar bridge local após validar fluxo completo.

100. **Render deploy stuck — diagnóstico e procedimento de recuperação**: quando build fica em "Awaiting build logs..." por mais de 5 minutos, o container de build foi alocado mas travou antes de executar qualquer comando (bug de infra Render free tier). Procedimento: (1) abrir o deploy específico em `dashboard.render.com/web/srv-d87r260jo6nc73cs2m90/deploys/<deployId>`; (2) clicar "Cancel deploy"; (3) na lista de deploys, clicar botão "Manual Deploy" → "Deploy latest commit". O novo deploy alocará um container diferente e rodará normalmente. **Não usar "git commit --allow-empty"** como método principal — Render pode ignorar empty commits e não criar novo deploy. **Diagnóstico de falha em 1 minuto**: se build falha em ~1min, provável erro de importação TypeScript (arquivo inexistente) — verificar `tsc --noEmit` localmente antes de pushear. **Causa raiz do padrão de falha do projeto**: `8ffa4be5` importou `WhatsappAiPromptService` (criado em `a3da8efb`, commit posterior) → import de arquivo inexistente → falha TypeScript em ~1min. `0158f6c5` incluía o arquivo mas seu auto-deploy ficou travado (freeze de infra); deploy manual resolveu. **Keepalive PM2 em `C:\Users\Ruffinus Pizzaria\Desktop\qr-scan\keepalive.mjs`**: pinga `/api/` a cada 4 min para manter Render free tier acordado. Ver item 98.

58. **Sprint UX-02 — diagnóstico PizzaBuilder: INCONCLUSIVO (favor falso positivo Chrome MCP)**: "tela 100% preta" reportada ao clicar pizza no cardápio digital. Análise estática: 3 componentes envolvidos — `components/pdv/PizzaBuilder.tsx` (PDV apenas, retorna `<div space-y-6>` inline sem overlay), wrapper PDV em `app/pdv/page.tsx:1358` (`fixed inset-0 z-[100] bg-black/80` + container `bg-[#050816]` quase preto INTENCIONAL do tema dark), e Modal "Montar Pizza" em `app/menu/[companyId]/page.tsx:1150` (`fixed inset-0 z-50` + backdrop `bg-black/50` + container `bg-white max-w-md` — esse foi o que abriu). DOM debug durante incidente confirmou modal renderizado corretamente ("Montar Pizza Preço = sabor mais caro"). Causa provável: overlay z-index 2147483646 da extensão Chrome MCP (1494×650) cobriu viewport durante captura. Modal C estaticamente correto. Reprodução em browser real sem Chrome MCP fica pendente — frontend caiu durante teste. Recomendação: NÃO BLOQUEIA git push (componente está como está no código de prod; bug, se existir, já está lá).

101. **Migração Evolution API: Render free → VPS Hostinger (10/06/2026, CONCLUÍDA até config; falta scan QR)**: o serviço Evolution no Render free (item 99) nunca gerou QR Code. Diagnóstico ao vivo via Chrome MCP revelou 4 causas empilhadas: (a) `AUTHENTICATION_API_KEY` vazia no serviço Evolution do Render → 401 Unauthorized (corrigido com valor `foodsaas2024kely`); (b) Redis habilitado por default na imagem Docker mas inacessível → flood `redis disconnected` (corrigido com `CACHE_REDIS_ENABLED=false`); (c) versao WhatsApp Web desatualizada `2.3000.1015901307` vs Baileys atual `2.3000.1035194821` (env `CONFIG_SESSION_PHONE_VERSION`); (d) **causa raiz final**: deploy da Evolution falha ~50% das vezes com "Exited with status 1" — **falta de RAM no free tier 512MB** (instancias recarregam do banco a cada boot, thrashing de reconexao estoura RAM). QR sempre `count:0`/`base64:0`. **Decisao**: migrar para **VPS Hostinger KVM 1** (4GB RAM, R$52,99/mes 1 mes pago PIX, datacenter EUA — Hostinger NAO tem datacenter no Brasil). Hostinger tem **template Docker "Evolution API" de 1 clique** (instala Evolution+Postgres+Redis). **Resultado**: Evolution **2.3.7** rodando em `https://evolution-api-j9ur.srv1747711.hstgr.cloud` (HTTPS via Traefik+letsencrypt), API_KEY `hXsEa8Cd4QKhM8Zp5WyUEMjiPivbZVY0`, whatsappWebVersion `2.3000.1041212653` (atual). **QR TESTADO E FUNCIONA** (`POST /instance/create` retorna `qrcode.base64` ~13KB de primeira). Backend Render (`srv-d87r260jo6nc73cs2m90`) env vars `EVOLUTION_API_URL` + `EVOLUTION_API_KEY` atualizadas para o VPS (deploy 17:02). **Pendente**: deletar conexao antiga "Kley" em `/whatsapp-ia` + Nova Conexao WhatsApp Business + escanear QR com celular da pizzaria (etapa fisica do usuario); depois desligar bridge local PM2 (item 98) e deletar Evolution antiga do Render (free, sem custo). **CUIDADO editar env Render**: form React carrega secrets lazy (ao focar) — digitar com teclado real concatena; usar `setSelectionRange(0, len)` antes; focar campo via JS `values[idx].focus()` (clicar por coordenada apos scroll erra de linha — quase sobrescrevi DATABASE_URL). Detalhes completos na memoria `project_evolution_vps_migration.md`.

102. **Kely WhatsApp NO AR 24/7 no VPS Hostinger (10/06/2026, CONCLUÍDO)**: continuacao do item 101. Causa raiz do "QR nao abre": usuario acessa /whatsapp-ia via super-admin "Configurar IA" = impersonation da empresa platform@foodsaas.internal (companyId cmq7d3dxs0006gw5pabsljy87). Tentativas antigas ficavam com conexoes fantasma sob outra empresa/sessao -> polling GET /connections/:id/qr retornava 404 "Conexao nao encontrada" e o provision NEM rodava (zero instancias no VPS). Sob a empresa CERTA (platform) a lista estava LIMPA. **Solucao**: super-admin login -> "Configurar IA" -> /whatsapp-ia da empresa platform (banner "VISUALIZANDO: R FoodSaaS Plataforma") -> Nova Conexao -> WhatsApp Business -> "Kely" -> "Conectar via QR Code". Provision criou instancia kely-cmq7d3dx-mq8orbg9 + conexao cmq8orft8000910as6fe39ms5 + webhook. **Bug webhook v2.3.7** (commit 68f51870): payload precisa ser {webhook:{enabled,url,webhookByEvents,webhookBase64,events}} aninhado camelCase (flat antigo dava 400 "instance requires property webhook"). **QR nao renderizava no modal inicial**: provision create response com QR ainda nao pronto (race) -> clicar botao WiFi verde no card -> GET qr retorna data:image/png base64 13KB -> modal exibe. **"nao foi possivel conectar ao aparelho"** no 1o scan = QR expirou (~20s) -> instance/restart + QR fresco + scan rapido -> state:open. **Bridge local kely-bridge PARADA** (pm2 stop) para evitar numero em 2 lugares = resposta duplicada. **APRENDIZADO**: para conectar a Kely SEMPRE usar super-admin -> "Configurar IA" (a impersonation define companyId, precisa ser consistente entre criar conexao e buscar QR). Vercel frontend confirmado atualizado (commit 68f51870). Pendente nao-urgente: testar msg real, verificar Configurar IA (mode AUTO), deletar Evolution antiga do Render.

104. **Migração backend Render → VPS Hostinger (11/06/2026, CONCLUÍDA)**: backend NestJS + PostgreSQL migrados do Render free tier para VPS Hostinger KVM 1 (4GB RAM, `srv1747711.hstgr.cloud`). **Motivação**: Render free dorme após inatividade → cold-start de 30-60s quebrando webhooks Evolution API + Whisper. **O que foi feito**: (1) PostgreSQL 18-alpine em Docker Compose (`/opt/foodsaas-db/`) com volume `postgres_data`; (2) pg_dump do Render + restore no VPS via container temp (6 empresas confirmadas); (3) Backend NestJS em Docker Compose (`/opt/foodsaas-backend/`) buildando da GitHub source `https://github.com/ruffinuscuritiba/food-system-Sas-ERP.git#main:backend`; Traefik roteia `api.srv1747711.hstgr.cloud` → container:3001; (4) Vercel env vars atualizadas via API (`NEXT_PUBLIC_API_URL=https://api.srv1747711.hstgr.cloud/api`, `NEXT_PUBLIC_SOCKET_URL=https://api.srv1747711.hstgr.cloud`) usando token Vercel CLI de `AppData\Roaming\xdg.data\com.vercel.cli\auth.json`; frontend redeployado com sha `4b07e410`; (5) Evolution API webhook para Kely atualizado: `POST /webhook/set/kely-cmq7d3dx-mq8orbg9` → `https://api.srv1747711.hstgr.cloud/api/whatsapp-ai/webhook/cmq8orft8000910as6fe39ms5`; (6) Keepalive PM2 (`keepalive.mjs`) atualizado para pingar VPS em vez de Render. **Verificação**: `GET https://api.srv1747711.hstgr.cloud/api/health` → `{status:"ok",ready:true}`; `GET https://food-system-sas-erp-frontend.vercel.app/api/warmup` → `{backend:{ok:true}}`. **Pendente não-urgente**: `WhatsappConnection.webhookUrl` no DB ainda aponta para Render (Evolution API já tem URL correta; só afeta se re-provisionar a conexão); suspender serviço backend no Render. **Infra VPS**: `proxy` Docker network externa (Traefik), TLS via letsencrypt, `NODE_OPTIONS=--max-old-space-size=512`.

105. **feat(tables): fluxo de comanda contínua — "Lançar" + "Lançar e Finalizar" (commit `0ff02dfe`)**: reescrita completa de `frontend/app/tables/page.tsx`. Novos botões: **"Lançar"** — envia o produto selecionado para cozinha (POST /orders + PATCH CONFIRMED) e mantém a mesa OCCUPIED aberta para lançamentos adicionais; toast "Lançado para cozinha ✓". **"Lançar e Finalizar"** — se há produto selecionado, lança primeiro; em seguida abre modal de checkout com resumo dos itens, total acumulado e seleção de forma de pagamento (PIX / Dinheiro / Crédito / Débito); confirmação chama POST /cash/movement + PATCH /tables/:id/status FREE. `paymentMethod` removido do formulário de adição (pertence apenas ao fechamento). `selectedTable` sincronizado via `setSelectedTable(prev => fresh.find(id === prev.id) ?? prev)` no `fetchTables` para eliminar stale `dineInOrders`. `useEffect([selectedTable])` reconstrói `tableItems` automaticamente após sync. `frontend/services/env.ts` também atualizado: fallback hardcoded trocado de Render para VPS, `isStaleUrl()` detecta tanto `94zd` quanto `onrender.com`.

106. **feat(feature-flags): sistema de controle de módulos por loja — Matrix bypass + ModuleGuard (commit `5806538f`)**: implementação completa do controle de feature flags baseado em identidade da loja. (1) `backend/src/common/utils/matrix.ts`: constante `MATRIX_COMPANY_ID` (env `MATRIX_COMPANY_ID`, fallback `cmq7d3dxs0006gw5pabsljy87`) + helper `isMatrixCompany(companyId)`. (2) `backend/src/common/decorators/required-module.decorator.ts`: decorator `@RequiredModule('slug')` para anotar controllers/handlers. (3) `backend/src/common/guards/module.guard.ts`: `ModuleGuard` — lê `@RequiredModule`, verifica `CompanyModule.status IN ACTIVE|TRIAL` no banco; bypass automático para empresa matriz (`isMatrixCompany`). (4) `TenantGuard`: injeta `req.isMatrix = isMatrixCompany(company.id)` após validação do tenant. (5) `CompanyModuleService`: `listCompaniesForAdmin()` + `adminActivate(requestingCompanyId, role, targetCompanyId, moduleSlug)` — somente SUPER_ADMIN ou empresa matriz podem ativar cross-tenant; valida existência da empresa destino. (6) `CompanyModuleController`: `GET /company-module/admin/companies` + `POST /company-module/admin/activate` com `@Roles(SUPER_ADMIN, ADMIN)`. (7) `ClientShell.tsx` `canAccessModule`: empresa matriz e SUPER_ADMIN sempre retornam `true`; DEMO também; outros verificam `activeSlugs`. Item "Rastreamento" ganha `moduleSlug: "delivery"` (antes aparecia para todo ADMIN). (8) `/modulos/page.tsx`: modal de provisionamento cross-tenant aparece para matriz/SUPER_ADMIN após ativar módulo — pergunta "em qual estabelecimento também ativar?" com dropdown de empresas. Build backend + frontend OK. `tsc --noEmit` limpo em ambos.

107. **feat(delivery): Rastreamento isolado — feature flag obrigatória + logs diagnóstico (commit `8db83ec3`)**: auditoria QA do módulo de entrega com 6 correções cirúrgicas. (1) `auth.service.ts`: removido `'DELIVERY'` da lista de módulos padrão do signup — nenhum plano dá direito automático ao rastreamento a partir de agora. (2) `drivers.controller.ts`: todos os endpoints admin (GET, POST, PATCH, DELETE, assign, earnings, payments) decorados com `@RequiredModule('delivery')` + `@UseGuards(RolesGuard, ModuleGuard)`. Endpoints `me/*` (driver PWA) preservados sem restrição. (3) `drivers.module.ts`: `ModuleGuard` adicionado aos providers. (4) `delivery-config.controller.ts`: endpoints autenticados decorados com `@RequiredModule('delivery')` + `ModuleGuard`; endpoint público `GET /public` preservado (cardápio digital precisa buscar zonas). (5) `delivery-config.module.ts`: `ModuleGuard` adicionado aos providers. (6) `tracking.gateway.ts`: logs estruturados `[GPS]`/`[PICKUP]`/`[DELIVERED]` com `Logger.log/warn/error`; catch silencioso `{}` substituído por `this.logger.warn(err.message)` — erros de DB não travam o broadcast de GPS; auth ausente em socket logado como `warn`. **Regra de acesso**: empresa matriz `R_FoodSaaS` bypass total; demais lojas precisam de `CompanyModule(moduleSlug='delivery', status IN ACTIVE|TRIAL)` no banco. **WhatsApp**: `order-notification.service.ts` já estava correto — `AbortSignal.timeout(15_000)` isola falhas de terceiros, todos os erros caught. `tsc --noEmit` limpo. `npm run build` OK (dist/main.js).

111. **feat(settings): Abas Pagamento + Impressão — configurações completas com preview dinâmico (COMPLETO)**: 2 novas abas no `/configuracoes`. **Schema**: 6 novos campos em `Company` — `acceptCash/acceptCreditCard/acceptDebitCard/acceptMealVoucher Boolean`, `customPaymentMethods Json?`, `printingSettings Json?`. **Migration** `20260613000000_add_payment_printing_settings` idempotente (`ADD COLUMN IF NOT EXISTS`). **UpdateCompanySettingsDto**: 6 novos campos com decoradores `@IsBoolean()/@IsObject() @IsOptional()`. **CompanyService** `getSettings/updateSettings`: select + data spreads adicionados para todos os 6 campos; `JSON.parse(JSON.stringify(...))` para JSON fields. **`components/settings/PagamentoTab.tsx`** (novo): Seção 1 (Online) — cards informativos fixos para PIX (taxa 0%, instantâneo) e Cartão Online (3,49%+R$0,40, D+2) com aviso para gateway em Integrações; Seção 2 (Entrega/Balcão) — `PaymentRow` com toggle + slot: Dinheiro, Crédito (badges Visa/MC/Elo/Hipercard/Amex), Débito (idem), Vale-Refeição (badges VR/VA/Alelo/Sodexo/Ticket); Seção 3 (Personalizados) — CRUD inline com `crypto.randomUUID()`, toggle isActive, delete, input com suporte a Enter/Escape. Salva via `PATCH /company/settings`. **`components/settings/ImpressaoTab.tsx`** (novo): layout 2 colunas (`xl:flex-row`). Esquerda — seção "Assistente de Impressão" (`useAssistant/autoPrint/autoAccept`, dependentes do toggle pai) + seção "Layout da Notinha" (`showCNPJ`, `showCategory`, `showDescription`, `addonGrouping`, formato pizza `compact|perFlavor` com botões visuais). Direita — `ReceiptPreview` sticky: bobina simulada 80mm com header (CNPJ condicional), cabeçalho do pedido, seções de categoria condicionais, itens com formato de pizza e agrupamento de add-ons reativos, totais, linha de corte ✂ e efeito de rolo. Cada toggle no lado esquerdo atualiza o preview em tempo real via state. **`configuracoes/page.tsx`**: `PagamentoTab` adicionado no render; `ImpressaoTab` agora importa de `@/components/settings/ImpressaoTab` (settings+preview) + `ImpressorasHardwareTab` importa `@/app/impressoras/page` embutido abaixo com divider "Hardware — Impressoras Cadastradas". `tsc --noEmit` limpo em backend (3 erros pré-existentes ignorados) e frontend.

110. **feat(settings): Consolidação de rotas + remoção de campos duplicados (COMPLETO)**: migração canônica das rotas de configuração para `/configuracoes?tab=X`. **Redirects (server-side, sem flash)**: `app/theme/layout.tsx`, `app/impressoras/layout.tsx`, `app/integracoes/layout.tsx` — cada um chama `redirect()` do `next/navigation` (server component), redirecionando para a aba correspondente em `/configuracoes`. **Abas agora com conteúdo real**: `configuracoes/page.tsx` usa `dynamic({ssr:false})` para importar `ThemePage` (aparencia), `ImpressorasPage` (impressao), `IntegracoesPage` (integracoes) diretamente dos arquivos de página — o import de módulo não aciona o layout redirect. **PlaceholderTab**: removidos os links que apontavam de volta para as rotas redirecionadas (evita loop). **Sidebar (`ClientShell.tsx`)**: links de "Tema / Visual", "Impressoras" e "Integrações" atualizados para `href=/configuracoes?tab=aparencia/impressao/integracoes`; `useSearchParams` adicionado para reconstituir `currentHref` e comparar corretamente hrefs com query string (active state do MenuItem). **Limpeza de duplicatas**: `app/whatsapp-ia/page.tsx` — card "Horário de Atendimento" (`WhatsappAiSettings.businessHoursStart/End/Days`) removido; substituído por aviso discreto com link para `/configuracoes?tab=loja` (fonte canônica é `Company.businessHours` desde item 108). Import `Clock` órfão removido. `tsc --noEmit` frontend limpo.

109. **feat(settings): Aba Entrega — zonas de entrega + mapa Leaflet + configurações globais (COMPLETO)**: implementação da aba "Entrega" em `/configuracoes?tab=entrega`. **Schema**: 6 novos campos em `Company` — `deliveryMethod String @default("NEIGHBORHOOD")`, `freeDeliveryAbove Decimal?`, `ownDelivery Boolean @default(true)`, `maxDeliveryRadius Int?`, `storeLat Float?`, `storeLng Float?`; 4 novos campos em `DeliveryZone` — `radiusKm Decimal?`, `lat Float?`, `lng Float?`, `color String @default("#f97316")`. **Migration** `20260612100000_add_delivery_settings` idempotente. **Backend**: `UpdateCompanySettingsDto` estendido com os 6 campos de entrega; `CompanyService.getSettings/updateSettings` inclui os novos campos no select/update; `DeliveryConfigService.create/update` agora persiste `radiusKm/lat/lng/color`. **Frontend**: `components/settings/DeliveryZoneMap.tsx` — mapa Leaflet com fix de ícone webpack, pin 🏪 customizado para loja, `Circle` para zonas RADIUS (raio em metros), labels coloridos para zonas NEIGHBORHOOD, `ClickCapture` para cliques no mapa. `components/settings/EntregaTab.tsx` — layout split: painel global (método NEIGHBORHOOD/RADIUS/ROUTE, toggle frete grátis + input condicional, toggle entrega própria, raio máximo), lista lateral de zonas (card por zona com badge de cor, toggle ativa/inativa, editar/deletar, botão "Nova zona"), mapa Leaflet interativo (toolbar "Definir local da loja" com cursor crosshair). Modal de zona: campos adaptativos por tipo (bairro para NEIGHBORHOOD, raio para RADIUS), seletor visual de 8 cores + color picker, toggle isActive. `configuracoes/page.tsx` carrega `EntregaTab` via `dynamic({ssr:false})`. Saves: `PATCH /company/settings` (configurações globais) + `POST/PATCH/DELETE /delivery-config` (CRUD de zonas). `tsc --noEmit` limpo em backend e frontend.

108. **feat(settings): Área de Configurações centralizada — Minha Loja (migration + backend + frontend)**: nova rota `/configuracoes` com sidebar de 10 abas (Identidade/Operação/Tecnologia/Gestão). Sidebar principal: item "⚙️ Configurações" adicionado em `ClientShell.tsx` logo acima de "Assinatura" com ícone `Settings`. **Schema**: 11 novos campos em `Company` — `whatsapp`, `cnpj`, `razaoSocial`, `zipCode`, `street`, `streetNumber`, `complement`, `neighborhood`, `city`, `state`, `businessHours Json?` (JSON indexado `"0"…"6"` = dom…sáb, estrutura `{open, close, isOpen}`). **Migration** `20260612000000_add_company_settings` idempotente (`ADD COLUMN IF NOT EXISTS`). **Backend**: `company/dto/update-settings.dto.ts` (DTO com validação `@Matches` para CEP, `@Length(2,2)` para UF); `CompanyService.getSettings/updateSettings` com `select` seguro (sem `password`); `CompanyController`: `GET /company/settings` + `PATCH /company/settings` — ambas as rotas ANTES de `:id` (match-by-order NestJS), requerem `JwtAuthGuard + RolesGuard`, usam `@CompanyId()` decorator. **WhatsApp IA**: `isBusinessHours()` refatorada — novo parâmetro `companyHours?: Record<string, {open,close,isOpen}>|null`; em `_processIncomingWithConnection` busca `company.businessHours` antes do check; log diagnóstico indica a fonte ("Company" vs "WhatsappAiSettings"); fallback legado preservado para empresas que ainda não preencheram os horários centralizados. **Frontend**: `app/configuracoes/page.tsx` — aba "Minha Loja" funcional com `GET /company/settings` no mount; seções Dados da Empresa (name, cnpj, razaoSocial, description, phone, whatsapp, email), Endereço (zipCode com autocomplete ViaCEP 600ms, street, streetNumber, complement, neighborhood, city, state) e Horários de Funcionamento (grade com toggle por dia + inputs `type="time"`, highlight laranja quando ativo, suporte a overnight). Demais 9 abas mostram placeholder com link temporário para a tela antiga. `tsc --noEmit` limpo em backend e frontend. **Anti-redundância**: `isBusinessHours()` agora lê de `Company.businessHours` quando preenchido — os campos `businessHoursStart/End/Days` em `WhatsappAiSettings` continuam como fallback mas devem ser deprecados progressivamente.

112. **feat(settings): Abas Equipe + Plano & Módulos — CRUD de usuários + App Store de módulos (COMPLETO)**: 2 últimas abas do `/configuracoes`, fechando a consolidação total. **Backend — users**: `backend/src/modules/users/dto/update-user.dto.ts` criado (`UpdateUserDto`: name/email/role/isActive; `ResetPasswordDto`: newPassword). `users.service.ts`: adicionado `USER_SELECT` + métodos `findByCompany(companyId)`, `findOne(id, companyId)`, `updateUser`, `deleteUser`, `resetPassword` (bcrypt hash). `users.controller.ts` reescrito com `GET/` (findByCompany), `POST /` (create existente), `POST :id/reset-password` (ANTES de `:id` — match-by-order NestJS), `GET/:id`, `PATCH/:id`, `DELETE/:id`. **Frontend — redirects**: `app/admin/layout.tsx` → `redirect("/configuracoes?tab=equipe")`; `app/planos/layout.tsx` → `redirect("/configuracoes?tab=plano")`; `app/modulos/layout.tsx` → `redirect("/configuracoes?tab=plano")`. **`components/settings/UsuariosTab.tsx`** (novo): lista de usuários com avatar por iniciais (cor determinística derivada do nome), badges de role com cor por papel (ADMIN=vermelho, MANAGER=azul, KITCHEN=laranja, CASHIER=verde, DELIVERY=indigo, WAITER=roxo, SUPER_ADMIN=preto); botões de ação apenas para ADMIN/SUPER_ADMIN (edit, reset-password, delete, toggle-active); `UserModal` (criar/editar com seletor de role, campo senha apenas no create); `ResetPasswordModal` (toggle de visibilidade); `DeleteModal` (confirmação hard-delete). **`components/settings/PlanoTab.tsx`** (novo): `PlanCard` — card gradiente com nome do plano, valor mensal, próxima renovação, status badge, botões Upgrade (→ `UpgradeModal`) e "Falar com suporte" (link WA); `ModuleCard` — grid App Store com ícone, categoria, status badge (ACTIVE/TRIAL/EXPIRED), preço, botões contextuais (Trial 14d / Contratar / Desativar / Reativar) via `POST /company-module/trial|activate` e `DELETE /company-module/:cId/:slug`; filtros por categoria (Todos/Operação/Marketing/Financeiro/Automação); `UpgradeModal` — cards gradiente por plano disponível com link WA. `configuracoes/page.tsx` atualizado: imports `UsuariosTab` + `PlanoTab` via `dynamic({ssr:false})`; render chain adiciona `equipe → UsuariosTab` e `plano → PlanoTab`; `PlaceholderTab.redirects` removeu entradas de `equipe` e `plano`. `tsc --noEmit` limpo em backend (3 erros pré-existentes) e frontend.

114. **feat(apps): App do Garçom + Tracking Público + Printer Agent Phase 3 (COMPLETO, commit `9f1b1e69`)**: 3 novos entregáveis. **(1) App do Garçom** — PWA mobile para role WAITER em `/garcom`. `layout.tsx`: Bottom Nav 4 itens (Mesas/Pedidos/Cardápio/Perfil), `fixed bottom-0`, `env(safe-area-inset-bottom)`. `page.tsx`: grid 3 colunas de mesas com cores FREE/OCCUPIED/RESERVED, badge de itens e total acumulado, auto-refresh 30s. `mesa/[id]/page.tsx`: comanda atual (itens de `dineInOrders` em aberto), cardápio inline com busca e filtro por categoria, suporte a tamanhos/sizes, "Lançar para cozinha" (`POST /orders` + `PATCH CONFIRMED`), modal de checkout (total, forma de pagamento, `POST /cash/movement` + `PATCH /tables/:id/status FREE`). `pedidos/page.tsx`: lista via `/orders/kitchen`, filtro Ativos/Todos, auto-refresh 15s. `perfil/page.tsx`: dados do usuário e logout. `ClientShell.tsx`: redirect `WAITER → /garcom` (mesmo padrão DELIVERY→/driver) + bypass de sidebar para `/garcom`. **(2) Tracking Público** — endpoint `GET /api/orders/public/track/:orderId` (sem auth, Throttle 30/min) com select seguro (itens, timestamps, driver name, company name). Página pública `app/tracking/[orderId]/page.tsx`: timeline de status com ícones e timestamps, auto-refresh 30s enquanto não entregue/cancelado, resumo de itens e totais. `/tracking` adicionado às rotas públicas no middleware e ClientShell. **(3) Printer Agent Phase 3** — `printer-agent/index.js` script Node.js standalone: poll `GET /printers/jobs?status=PENDING` a cada 5s, monta ticket ESC/POS (header empresa, badge template, itens+complementos, totais, endereço delivery, corte), backends: USB (`escpos` lib), Network TCP, debug console. PATCH status → SENT/PRINTED/FAILED. `printer-agent/package.json` + `README.md` com instruções de configuração e variáveis de ambiente.

113. **feat(integrations): iFood homologação completa — OAuth2 + HMAC + ACK + pushCatalog + rawBody + frontend (COMPLETO)**: fechamento dos 5 TODOs do `IfoodProvider` + wiring de produção. (1) `ifood.provider.ts`: `getAccessToken(clientId, clientSecret)` com cache in-memory (60 s de buffer); `timingSafeEqual` no HMAC (previne timing attack + captura RangeError); `sendAck(orderId, token)` `POST /order/v1.0/orders/:id/statuses/confirmation`; `pushCatalog(merchantId, categories, token)` `PUT /catalog/v2.0/merchants/:merchantId/categories`; 12 novos status no `mapOrderStatus` (`INTEGRATION_CONFIRMED`, `WAITING_SELLER`, `PICK_UP_AREA_ASSIGNED`, `CONSUMER_PICKED_UP`, `CANCELLATION_REQUESTED/DENIED`, etc.); getter `tokenExpiry: number` para persistência via service; `mapPaymentMethod` expandido com `CREDIT_CARD`/`DEBIT_CARD`/`MEAL_VOUCHER`/`BANK_SLIP`. (2) `main.ts`: `json({ verify: (req, _res, buf) => req.rawBody = buf })` — rawBody real disponível para HMAC; sem esse fix `req.rawBody` era `undefined` em produção. (3) `schema.prisma` + migration `20260613100000_add_integration_client_id` idempotente: `IntegrationConfig` recebe `clientId String?` (antes só tinha `apiKeyEncrypted` para o secret). (4) `integrations.service.ts`: `upsertConfig` aceita `clientId`/`clientSecret` (persiste em `apiKeyEncrypted`), chama `getValidToken` automaticamente ao ativar iFood com credenciais completas; `getValidToken(config)` verifica token no DB (60 s de buffer), chama `IfoodProvider.getAccessToken()` se expirado, persiste `accessToken + tokenExpiresAt` no DB (sobrevive restarts); `processWebhook` usa `getValidToken` (não mais `config.accessToken` estático) antes de chamar `sendAck`. (5) `integrations.controller.ts`: `PUT /integrations/config` aceita `clientId?` e `clientSecret?` no body. (6) `Dockerfile`: `--rolled-back 20260613100000_add_integration_client_id` adicionado. (7) `frontend/app/integracoes/page.tsx`: estado `clientId`/`clientSecret`; campos OAuth2 aparecem somente quando `p === "IFOOD"` em grid 2 colunas; webhook URL usa `companyId` real do localStorage (não mais placeholder `[SEU_COMPANY_ID]`); payload do `saveConfig` inclui os dois campos. `tsc --noEmit` limpo em ambos (3 erros pré-existentes em whatsapp-ai-prompt ignorados). `prisma generate` OK.

115. **feat(printer-agent): página de download + heartbeat + banner + super-admin (COMPLETO, commit `e1ea8c67`)**: ecossistema completo para o Printer Agent Phase 3. **Backend** — `printers.service.ts`: `Map<companyId, number>` em memória (`agentHeartbeats`) com TTL 90s; `agentPing(companyId)` atualiza timestamp; `getAgentStatus(companyId)` retorna `{online, lastSeen}`. `printers.controller.ts`: `POST /printers/agent/heartbeat` + `GET /printers/agent/status` adicionados ANTES de `@Get()` findAll (match-by-order NestJS). **Frontend** — `components/settings/ImpressaoLocalTab.tsx`: `detectOS()` via `navigator.userAgent` → windows/linux/mac; `AgentStatusPill` verde/vermelho com último ping; token box dark `bg-gray-950` mostra primeiros 48 chars do JWT + botão copiar; botão de download primário (OS detectado) + botões secundários (outros SO); `NEXT_PUBLIC_AGENT_RELEASE_URL` env var para base URL das releases; guia 3 passos com blocos de código e arquivo `.env` de exemplo; polling 30s via `setInterval`. `components/PrinterAgentBanner.tsx`: banner âmbar não-intrusivo no topo da página Pedidos; só para ADMIN/MANAGER; verifica status do agente no mount + a cada 60s; dismiss por sessão via `sessionStorage("printer_agent_banner_dismissed")`; link para `/configuracoes?tab=impressao-local`. `configuracoes/page.tsx`: aba "Impressão Local" adicionada ao grupo Tecnologia; `ImpressaoLocalTab` importado via `dynamic({ssr:false})`. `app/orders/page.tsx`: `<PrinterAgentBanner />` adicionado no topo do JSX. `super-admin/dashboard/page.tsx`: botão "Copiar Link Agente" no dropdown de ações por empresa — copia `{origin}/configuracoes?tab=impressao-local` para clipboard com `alert()` de confirmação. **Printer Agent** — `printer-agent/package.json`: scripts `build:win`/`build:linux`/`build:all` usando `pkg` (targets `node18-win-x64`, `node18-linux-x64`); `printer-agent/index.js` inclui `sendHeartbeat()` — `POST /printers/agent/heartbeat` a cada 30s com Bearer token. **Fluxo de login super-admin**: acesso via `/super-admin/login` (não `/login`); credenciais via env vars `SUPER_ADMIN_EMAIL`/`SUPER_ADMIN_PASSWORD` com fallback `superadmin@system.com`/`SuperAdmin@123`; gera JWT `role=SYSTEM_SUPER_ADMIN` (8h); `platform@foodsaas.internal` é conta de banco com senha randômica (UUID) — acesso via impersonação no dashboard super-admin, não login direto. `tsc --noEmit` limpo em ambos. VPS rebuild necessário para ativar endpoints heartbeat.

119. **feat(retention): cron de limpeza + régua de e-mail + Central de Clientes super-admin (COMPLETO)**: estratégia de retenção completa end-to-end. (1) `Company.wasEverActive Boolean @default(false)` — marca permanentemente quem já pagou; `payments.service.ts activateSubscription()` seta `wasEverActive=true` na ativação. Migration `20260619000000_add_was_ever_active` idempotente com backfill (`UPDATE WHERE subscriptionStatus='ACTIVE'`). (2) `@nestjs/schedule@6.1.3` instalado; `ScheduleModule.forRoot()` em `AppModule`; `RetentionModule` registrado. (3) `RetentionService` (`@Cron('0 3 * * *')` diário às 3h): `cleanupExpiredTrials()` — busca `wasEverActive=false AND createdAt < 31d AND status !== ACTIVE`; pula demos permanentes (`demo-basic@foodsaas.demo` etc.) e empresa matriz; salva Lead (fire-and-forget) antes de deletar; chama `SuperAdminService.deleteCompany` (cascade). `sendRenewalReminders()` — busca `wasEverActive=true AND status !== ACTIVE`; calcula `daysPastDue`; envia `SUBSCRIPTION_REMINDER` nos dias 5, 15 e 30. (4) `NotificationsService`: tipo `SUBSCRIPTION_REMINDER` com template HTML "Sentimos sua falta" + CTA "Reativar Assinatura Agora". (5) `SuperAdminModule` exporta `SuperAdminService` (fix de DI para RetentionModule). (6) `SuperAdminService`: `classifyCompany()` → `{type: DEMO|ACTIVE|EX_CLIENT|TRIAL, status}`; `getCustomersReport({page,limit,type,search})` — une Company + Lead (sem duplicar quem virou cliente), retorna `{items, total, summary:{total,active,trial,demo,exClient,leads}}`; `getCustomersReportCsv()` (UTF-8 BOM para Excel); `getCustomersReportTxt()` (números WhatsApp deduplicated, um por linha). (7) `SuperAdminController`: `GET /super-admin/customers-report/csv`, `/txt`, `/` (JSON paginado) — todos ANTES das rotas `/leads` para match-by-order. (8) Frontend `app/super-admin/clientes/page.tsx`: 6 KPI cards clicáveis (total/ativo/trial/demo/exCliente/leads) com cores distintas; filtro por tipo em pills; busca full-text (Enter ou debounce); tabela com 8 colunas (contato, restaurante, e-mail, WhatsApp link wa.me, tipo badge, status, plano, cadastro); paginação 50 por página; botão "Exportar CSV" + "Exportar TXT" via fetch+blob com `sa_token`; dica de marketing no rodapé. `tsc --noEmit` limpo em backend e frontend. `prisma generate` OK.

118. **feat(trial): 3-day free trial + smart trial gate + AI lock (COMPLETO)**: modelo "Trial 3 dias sem cartão". (1) `auth.service.ts signup()`: `dueDate` = `now + 3 days` (era 7). (2) `ClientShell.tsx`: redirect para `/assinatura` SOMENTE quando `subscriptionStatus === 'PENDING_PAYMENT' AND dueDate < now` — dentro do trial, usuário navega livremente. (3) `frontend/hooks/useSubscription.ts` (NOVO): hook que faz GET `/company/:id/subscription`, cache em sessionStorage, expõe `isActive`, `isInTrial`, `isExpired`, `isAiLocked`, `trialDaysLeft`. Demo users retornam `isAiLocked=false` automaticamente. (4) `frontend/components/AiTrialLock.tsx` (NOVO): overlay de página com ícone cadeado, mensagem contextual (variant "trial" vs "expired") e CTA para `/assinatura`. (5) `app/cadastro-inteligente/page.tsx` e `app/whatsapp-ia/page.tsx`: importam `useSubscription` + `AiTrialLock`; se `isAiLocked` renderizam o overlay antes do conteúdo principal. (6) Backend `src/common/guards/subscription-active.guard.ts` (NOVO): guard NestJS que consulta `company.subscriptionStatus` via Prisma; lança `ForbiddenException("Recurso de IA disponível apenas nos planos ativos…")` se não ACTIVE; bypass para empresa matriz. Registrado como provider em `IaModule` e `SmartImportModule`. (7) `ia.controller.ts`: guards `JwtAuthGuard, SubscriptionActiveGuard` em `POST ask`, `GET conversations`, `GET conversations/:id` — endpoints de IA do painel admin. (8) `smart-import.controller.ts`: `SubscriptionActiveGuard` a nível de classe (cobre todos os endpoints de upload e confirmação). (9) `whatsapp-ai.service.ts _processIncomingWithConnection()`: checa `company.subscriptionStatus !== 'ACTIVE'` antes de processar qualquer mensagem — previne custo de API durante trial. `tsc --noEmit` limpo em backend e frontend.

117. **feat(checkout): subscription gate — PENDING_PAYMENT + checkout wall + demo CTA redirect**: fluxo completo de pagamento para novos cadastros. (1) `auth.service.ts signup()`: empresa criada com `subscriptionStatus: 'PENDING_PAYMENT'` + `dueDate` 7 dias à frente (janela para completar o checkout). (2) `app/signup/page.tsx`: após cadastro redireciona para `/assinatura` em vez de `/pdv` (toast "Conta criada! Escolha seu plano."). (3) `app/assinatura/page.tsx`: checkout gate renderizado quando `sub.subscriptionStatus === 'PENDING_PAYMENT'` — tela dark premium com 3 cards de plano (BASIC R$97 / DELIVERY/Profissional R$197 / ENTERPRISE R$397), `handleCheckout(plan)` chama `POST /payments/checkout { companyId, plan, provider: 'MERCADO_PAGO' }` e redireciona para `checkoutUrl`; ícone `LogOut` + botão Sair, link WA para consultor. `CHECKOUT_PLANS` constant usa chave `'DELIVERY'` (match de `PLAN_PRICES` no backend). (4) `components/ClientShell.tsx`: `useEffect` lê `GET /company/:id/subscription` no mount — se `subscriptionStatus === 'PENDING_PAYMENT'` e não está em `/assinatura` ou `/pagamento`, faz `router.replace('/assinatura')`. Não afeta usuários DEMO. Botão "Ativar sistema real" no banner demo muda de link WhatsApp para `href="/signup"` (interno, sem `target="_blank"`). Gateway de pagamento e webhook já completos no `PaymentsModule` (`createCheckout`, `activateSubscription`, `handleWebhookMercadoPago`). Página `/pagamento/sucesso` já chamava `POST /payments/activate` — fluxo end-to-end funcional. `tsc --noEmit` limpo em backend e frontend.

116. **feat(demo): lead capture gate + email notification + banner CTA (commit `d4d70e4b`)**: fluxo completo de captação de leads antes de liberar o acesso à demonstração. (1) `POST /api/auth/demo-access` — endpoint novo (Throttle 10/min, sem auth): recebe `{name, email, whatsapp, restaurantName, plan}`, salva lead via `LeadsService.upsert()` (fire-and-forget), envia e-mail ao admin via `NotificationsService.send({type:'DEMO_LEAD'})` (fire-and-forget), retorna JWT do usuário demo correspondente ao plano. **Env var obrigatória**: `ADMIN_NOTIFY_EMAIL` — se ausente, dados ficam apenas no log WARN do backend. (2) `NotificationsService`: tipo `DEMO_LEAD` adicionado à union `NotificationType`; template HTML com tabela de dados do lead (nome, restaurante, e-mail clicável, link wa.me), sujeito "🔥 Novo lead quente na demo". (3) `AuthModule` importa `LeadsModule` + `NotificationsModule` + `ConfigModule`. `AuthService` injeta `LeadsService`, `NotificationsService`, `ConfigService`. (4) `/demo/page.tsx`: `LeadCaptureModal` (componente interno) aparece ao clicar "Testar X" — campos Name/E-mail/WhatsApp (opcional)/Restaurante, formatação automática de telefone, validação inline, botão submit com loading. `enterDemoWithLead()` chama `/auth/demo-access` em vez de `/auth/login` direto. Modal fecha automaticamente ao sucesso. (5) `ClientShell.tsx`: banner demo renovado com gradiente violeta, texto contextual mobile/desktop, e botão "Ativar sistema real" (CTA branco que abre WhatsApp do consultor). `tsc --noEmit` limpo em backend e frontend.

122. **feat(plano): marketplace real com catálogo de módulos + modal de contrato + analytics + Brendi (commit `31dbe4f6`)**: `frontend/components/settings/PlanoTab.tsx` reescrito completamente. `MODULES` array fixo no frontend com 7 módulos reais do ecossistema (iFood Integrado R$49,90 / 99 Food R$39,90 / **Brendi R$39,90** / Rastreamento de Entregadores R$29,90 / Nota Fiscal NFS-e/NFC-e R$59,90 / Robô WhatsApp IA R$79,90 / Fidelidade & Cupons R$19,90) — cada um com slug, nome, categoria, preço, descrição e termos específicos. Status cruzado com backend por slug (`.toLowerCase()` garante match independente do formato da API). `ContractModal`: header com ícone+nome, resumo comercial com preço e alerta de cobrança (amber), termos com scroll em bloco monospace, checkbox obrigatório "Declaro que li e aceito as condições e termos do contrato para ativação deste módulo", botão "Confirmar Ativação" disabled até checkbox — só então chama `POST /company-module/activate`. `DetailModal`: modal de detalhes por módulo com descrição completa, grid de 4 funcionalidades e passo a passo numerado. Filtros de categoria (Todos/Operação/Financeiro/Automação/Marketing). `PlanCard` com gradiente por plano + `PlanCard.PLAN_PRICES` fixo (BASIC=97/PRO=197/ENTERPRISE=397). `UpgradeModal` por plano com link WA. **Seção "Analytics e Marketing"** (integrações gratuitas): 3 cards com ícones SVG de marca — GTM (azul), GA4 (barras laranja), Facebook Pixel (fundo escuro azul) — badge "Ativa"/"Não configurado" baseado em `companyFields[fieldKey]`, link para `/configuracoes?tab=aparencia`. **Brendi**: `BrendiIcon` SVG vermelho circular, slug `"brendi"`, passo a passo de cadastro em `integrations.brendi.com.br` e termos específicos no `ContractModal`. `tsc --noEmit` limpo. Zero alterações de backend.

121. **feat(super-admin): redesign completo do dashboard — sidebar nav + stats premium + busca (commit `3762db96`)**: `frontend/app/super-admin/dashboard/page.tsx` reescrito. Layout mudou de página simples para split sidebar+main. **Sidebar fixa** (lg+) com logo FoodSaaS, 4 itens de nav (Dashboard/Clientes/Leads/Preços), seção Ferramentas (Seed Demo, Init Demos), botão Sair com hover vermelho. **Top bar** sticky com breadcrumb, toggle arquivados e botão "Novo restaurante". **Stats cards**: 4 cards `StatCard` com ícone colorido, valor grande, subtítulo percentual. **Plan distribution strip**: 4 cards com mini barra de progresso horizontal por plano. **Tabela**: agora tem barra de busca full-text + filtro por plano, avatar letra inicial por empresa, badges `rounded-full` em vez de `rounded-md`, ícones `Users`/`BarChart3` nas colunas de contagem. `usePathname()` para active state do nav. Background `#0a0a0b` (dark slate premium). `tsc --noEmit` limpo.

120. **feat(segment+layout): seletor de segmento no signup + layout GRID dinâmico + seed automático (commit `996e1079`)**: reformulação estrutural para atender múltiplos nichos. (1) `Company` recebe 3 novos campos: `businessSegment String @default("RESTAURANTE")`, `layoutType String @default("LIST")`, `buttonRadius String @default("MD")`. Migration `20260619200000_add_segment_layout` idempotente (`ADD COLUMN IF NOT EXISTS`). (2) `SegmentSeedService` (`backend/src/modules/segment-seed/`): constante `SEGMENT_DATA` com 9 segmentos (RESTAURANTE, LANCHONETE, PIZZARIA, CHURRASCARIA, MARMITARIA, HOT_DOG, PASTELARIA, PADARIA, DOCERIA, CONVENIENCIA), cada um com 3-4 categorias e 3-5 produtos com `salePrice`/`costPrice`. `seedForCompany(companyId, segment)` é idempotente (skip se empresa já tem categorias). `SegmentSeedModule` registrado em `AuthModule` e `AppModule`. (3) `auth.service.ts signup()`: aceita `businessSegment?`, salva no `company.create`, chama `segmentSeed.seedForCompany()` via `setImmediate` (fire-and-forget, não bloqueia resposta). (4) `UpdateCompanySettingsDto` + `CompanyService.getSettings/updateSettings` incluem os 3 novos campos. (5) Signup redesenhado em 2 passos: Step 1 — grid de 10 cards com emoji+label por segmento; Step 2 — formulário com badge do segmento selecionado + botão "Voltar". `businessSegment` enviado no `POST /auth/signup`. (6) PDV: `layoutType` state buscado de `GET /company/settings` no mount; desktop GRID = `grid-cols-2 xl:grid-cols-4`; mobile GRID = `grid 2-col` — mesmo padrão do `activeIsBeverage` existente. `tsc --noEmit` limpo em backend e frontend. `prisma generate` OK.

123. **feat(whatsapp-ia): personality config — estilo de resposta, personalidade, emojis, pedidos (commit `c55baf28`)**: ConfigTab da página `/whatsapp-ia` reformulada com 11 novos campos em `WhatsappAiSettings`. (1) Migration `20260621100000_add_whatsapp_ai_personality` idempotente: `responseStyle TEXT DEFAULT 'DIRECT'`, `personalityType TEXT DEFAULT 'FRIENDLY'`, `emojiUsage TEXT DEFAULT 'MODERATE'`, `advancedPersonality BOOLEAN DEFAULT false`, `speechHabits/characteristics/principles/humor TEXT NULL`, `menuLinkStyle TEXT DEFAULT 'BUTTON'`, `conversationalOrdering BOOLEAN DEFAULT false`, `orderHandlingMode TEXT DEFAULT 'LINK_ONLY'`. (2) `UpdateSettingsDto` e `AiSettings` type do frontend atualizados. (3) Card **"Seu Atendente"** reformulado: campo de nome em destaque com descrição, radio cards de "Estilo de resposta" (DIRECT/CONVERSATIONAL), radio cards de "Personalidade principal" (FRIENDLY/PLAYFUL/FORMAL), radio cards de "Uso de emojis" (NONE/MODERATE/ALWAYS — sincroniza `useEmojis` legado), seção colapsável "Personalidade avançada" com 4 textareas (speechHabits/characteristics/principles/humor, max 150 chars cada com contador), campo System Prompt preservado. (4) Card **"Pedidos e Cardápio"** novo: radio BUTTON/TEXT para envio do cardápio, toggle "Pedido conversacional", radio com 4 opções para "Retirada de pedidos" (LINK_ONLY / LINK_THEN_HUMAN_3 / LINK_THEN_HUMAN_2 / ALWAYS_HUMAN). `Dockerfile --rolled-back` adicionado. `tsc --noEmit` limpo em frontend e backend.

124. **feat(pdv): leitor de código de barras USB (commit `c55baf28` + anterior)**: campo de busca do `/pdv` preparado para leitores USB. `Product` interface recebe `barCode?`, `sku?`, `eanCode?`. `handleBarcodeSubmit(code)` faz match exato em `p.barCode === trimmed || p.eanCode === trimmed || p.sku === trimmed` → `openProductAdd(exact)`. Enter no input chama `handleBarcodeSubmit`. `addCartItem` usa `requestAnimationFrame(() => searchRef.current?.focus())` após qualquer adição para manter o foco. `autoFocus` no input. Placeholder "Buscar ou bipar código...". Sem mock data alterado.

125. **feat(settings): aba Financeiro — Split de Pagamentos (commit anterior)**: nova aba `financeiro` em `/configuracoes`. `Company` recebe 6 campos: `repasseFrequency TEXT DEFAULT 'DAILY'`, `repasseTime TEXT DEFAULT '03:00'`, `repasseWeekday INT DEFAULT 1`, `creditReleasePlan TEXT DEFAULT 'D30'`, `bankAccountData JSONB`, `walletBalance DECIMAL DEFAULT 0`. Migration `20260621000000_add_financial_settings` idempotente. `UpdateCompanySettingsDto` + `CompanyService.getSettings/updateSettings` incluem os novos campos. `FinanceiroTab.tsx` (novo): Bloco 1 — conta de repasse (PIX key type/key/holder ou banco/agência/conta/tipo/titular/CNPJ); Bloco 2 — frequência (DAILY/WEEKLY) + horário + dia da semana; Bloco 3 — liberação D0 vs D30 com radio cards. Amber card de saldo de carteira quando `walletBalance > 0`. `deductPendingSubscription(companyId)` verifica saldo vs preço do plano.

127. **feat(wallet): motor financeiro — Split de Pagamentos + Carteira + Cron de Repasse (commit `c91a2daa`)**: arquitetura completa de subadquirente com isolamento de saldo por tenant. **Schema**: `OnlineOrder` recebe `grossAmount/feeAmount/netAmount Decimal?`; enum `WalletTxType` (ORDER_CREDIT/SUBSCRIPTION_DEBIT/REPASSE/TRANSFER_FEE/MANUAL_CREDIT/MANUAL_DEBIT); enum `RepasseStatus` (PENDING/PROCESSING/COMPLETED/FAILED); model `WalletTransaction` (amount positivo=crédito, negativo=débito, balanceBefore/After, referenceId/Type, repasseId); model `WalletRepasse` (amount=bruto, transferFee=1,90, netAmount, status, pixKey/bank, gatewayResponse, scheduledFor). Back-relations em Company. Migration `20260623000000_add_wallet_engine` idempotente. **WalletService**: `creditFromOrder(companyId, orderId, gross, paymentMethod)` — calcula taxa (PIX=1,99% / DEBIT=1,99% / Crédito D0=5,69% / D30=3,99%), credita netAmount em Company.walletBalance, cria WalletTransaction ORDER_CREDIT, atualiza breakdown no OnlineOrder (catch silencioso se campo não existe ainda); `deductPendingSubscription(companyId)` — checa se subscriptionStatus≠ACTIVE ou dueDate < now, verifica walletBalance ≥ planPrice (BASIC=97/PRO=197/ENTERPRISE=397), debita e regulariza assinatura em transação atômica; `processRepasseForCompany(companyId)` — cria WalletRepasse PROCESSING, calcula netAmount=balance-1,90, chama **Mercado Pago Payouts API** `POST https://api.mercadopago.com/v1/payouts` com `X-Idempotency-Key: repasse-{repasseId}` + `Authorization: Bearer MERCADO_PAGO_ACCESS_TOKEN`; sucesso→COMPLETED+zera walletBalance em transação atômica; erro→FAILED+devolve saldo+cria MANUAL_CREDIT (rollback sem perda). **RepasseCronService**: `@Cron('0 * * * *')` — `$queryRaw` busca companies com walletBalance>0+isBlocked=false, filtra por `repasseTime===currentHour` e `repasseFrequency=WEEKLY→repasseWeekday===currentWeekday`, por empresa: deductPendingSubscription→processRepasseForCompany. **WalletController**: `GET /wallet/balance|transactions|repasses` com JwtAuthGuard+RolesGuard. **Hook de pagamento**: `PaymentsService.handleOnlineOrderWebhook` quando `paymentStatus=APPROVED` → `setImmediate` busca total+paymentMethod do OnlineOrder e chama `walletService.creditFromOrder`. **Frontend FinanceiroTab**: texto exato especificado ("3,99% + 1,7% de taxa de adiantamento" / "3,99% por transação"); nota de rodapé "Taxa PIX Online: 1,99% | taxa fixa R$1,90"; KPI card de saldo com próximo repasse agendado; Extrato da Carteira — lista WalletTransactions com ícone crédito(verde)/débito(vermelho), badge de tipo, valor colorido e timestamp. `tsc --noEmit` limpo em backend e frontend. **Env vars**: `MERCADOPAGO_ACCESS_TOKEN` (usado por `payments.service.ts` para checkout/webhook) e `MERCADO_PAGO_ACCESS_TOKEN` (usado por `wallet.service.ts` para payouts) — ambas configuradas no VPS via Hostinger Docker Manager (item 128). `(this.prisma as any)` casts em wallet.service.ts serão eliminados após próximo `prisma generate` no deploy.

129. **feat(super-admin): Editor de Tema PDV visual — v2 modular com highlight por elemento + tabs funcionais (commit `0b871ebe`)**: página `/super-admin/tema` reescrita 2x. Layout 2 painéis: sidebar esquerda (290px) + preview direita. **Sidebar**: grid 2×9 de botões de elemento (Fundo/Tipografia/Paleta Global/Botões/Formulários/Cartões/Cabeçalho/Rodapé/Navegação), swatches das sub-cores atuais; ao clicar, painel de cores expande full-width ABAIXO do grid (sub-key tabs, swatch atual, `<input type="color">`, paleta 8×6=48 cores temáticas). **Preview**: mock PDV "Hambúrgueres Gourmet" com 4 produtos Unsplash, nav lateral 4 categorias, header busca+usuário, footer com hora ao vivo. **Highlight UX**: overlay `bg-black/55 absolute z-[5]` cobre o PDV; elementos do tipo selecionado escapam com `relative z-[10]` + `boxShadow` glow na cor `accent`; TARGETS map associa cada ElemId às zones afetadas (`btn`, `card`, `header`, `nav`, `input`, `text`, `footer`). Badge animado no top bar "Editando: X". **Tabs funcionais** (commit `0b871ebe`): "Adicionar" → galeria de 6 temas prontos (Esmeralda/Azul/Vermelho/Roxo/Claro/Dourado) com mini-preview e aplicação imediata; "Conteúdo" → seletor de empresa + botão "Aplicar Tema à Loja" (`POST /themes/:companyId` mapeando `btnBg→primaryColor`, `navActive→secondaryColor`, `pageBg→backgroundColor`, `textPrimary→textColor`); "Tema" → grade de elementos com **auto-scroll** (`useEffect + scrollIntoView`) quando ColorPanel abre abaixo da grade (problema raiz do "painel invisível"). `BroadcastChannel("pdv_theme")` no Save para sincronizar PDVs abertos. `tsc --noEmit` limpo, zero migrations.

128. **infra(vps): MERCADO_PAGO_ACCESS_TOKEN adicionado ao VPS via Hostinger Docker Manager (18/06/2026)**: env var `MERCADO_PAGO_ACCESS_TOKEN=APP_USR-5584568238594140-052515-f604e2736bcf509eed06293774ffbdbb-1030103374` adicionada ao projeto `foodsaas-backend` no Hostinger Docker Manager (hpanel.hostinger.com/vps/1747711/docker-manager → foodsaas-backend → Editar → nova variável index 16). "Implantar" clicado — deploy completou 57%→100% em ~7 min via `docker compose up -d` (imagem local já existia, só reiniciou o container com as novas env vars). Container `comidasaas-backend-backend-1` voltou ao estado `running(1)`. `GET /api/health` confirmou `{"status":"ok","ready":true}`. **Duas env vars de MP no VPS**: `MERCADOPAGO_ACCESS_TOKEN` (pré-existente, usada por payments.service.ts para criação de checkout PIX/cartão) e `MERCADO_PAGO_ACCESS_TOKEN` (nova, usada por wallet.service.ts para payouts/repasse). Mesmo token, nomes diferentes — ambas necessárias. **IMPORTANTE editar env vars no Hostinger**: form Vue.js com inputs lazy — NUNCA usar coordenadas de clique após scroll (erra de linha); usar `values[idx].focus()` via JS; setter nativo `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set.call(el, v); el.dispatchEvent(new Event('input',{bubbles:true}))` para preservar reatividade Vue.

126. **feat(construtor+review): Construtor de Lojas + avaliação Google automática via WhatsApp (commit `0c9c4956`)**: 2 entregáveis. (1) **Construtor de Lojas** — `LayoutTemplate` model (`id/name/segment/config Json/isDefault`, sem companyId — templates são globais do Super Admin). `Company.layoutConfig Json?` + `Company.googleReviewUrl String?`. Migration `20260622000000_add_layout_templates` idempotente. Módulo `layout-templates` completo com `LayoutTemplatesService` (`findAll/create/delete/applyToCompany/saveCompanyLayout`) + controller (`GET / POST / DELETE /:id / POST /:id/apply / PATCH /company/:companyId`). `GET /company/layout/public?companyId=X` (sem auth, Throttle 60/min) expõe `{layoutConfig, layoutType, buttonRadius, googleReviewUrl}` para cardápio. Cardápio digital (`app/menu/[companyId]/page.tsx`) consome via `menuLayoutConfig`: helpers `blockVisible(id)` e `blockOrder(id)` controlam visibilidade e ordenação dos blocos (`banner`, `categories`, `featured`); seção "Destaques" (scroll horizontal com produtos `isFeatured=true`) posicionada antes ou depois das categorias conforme `block.order`; `<header>` inteiro ocultável via `blockVisible("banner")`. Super Admin: `/super-admin/construtor` — seletor de empresa, 6 templates por segmento (PIZZARIA/LANCHONETE/PADARIA/CONVENIENCIA/MERCADO/RESTAURANTE), botões Up/Down de ordenação, toggle Eye de visibilidade, seletor layoutType/buttonRadius, campo Google Review URL, `LayoutPreview` miniatura 200px, "Salvar como template" e "Aplicar". Nav item "Construtor" com ícone `Layout` no dashboard super-admin. (2) **Avaliação Google automática** — `WhatsappAiService.sendTextMessage(companyId, phone, text)` criado (público, reutilizável). `orders.service.ts updateStatus()`: após DELIVERED, `setImmediate` busca `company.googleReviewUrl`, monta mensagem com nome do cliente e envia. `orders.service.ts updateKitchenStatus()`: branch `ONLINE` também dispara a mesma lógica via `prisma.onlineOrder.findUnique` para buscar `customerPhone/customerName`. Ambos silenciosos (catch vazio). `tsc --noEmit` limpo em backend e frontend.

130. **feat(settings): CNPJ autocomplete via Receita Federal + campos IE e Nome Fantasia (commit `4219d1e1`)**: aba "Minha Loja" em `/configuracoes` ganhou 3 melhorias. (1) Campo CNPJ com autocomplete — `handleCnpjChange` com debounce 800ms; ao digitar 14 dígitos chama `https://brasilapi.com.br/api/cnpj/v1/{cnpj}` (sem auth, CORS OK); preenche automaticamente `razaoSocial`, `nomeFantasia`, `inscricaoEstadual`, rua, número, bairro, cidade, UF, CEP, telefone (se vazio) e e-mail (se vazio); spinner `Loader2` durante busca + ícone `CheckCircle2` verde ao concluir; fallback silencioso se CNPJ inválido ou API falhar. (2) Campo "Nome Fantasia" — `Company.nomeFantasia String?`; migration `20260624200000_add_inscricao_estadual` idempotente (`ADD COLUMN IF NOT EXISTS`); `UpdateCompanySettingsDto` + `getSettings/updateSettings` atualizados. (3) Campo "Inscrição Estadual" — `Company.inscricaoEstadual String?`; mesma migration. **Deploy VPS**: o "Implantar" no Hostinger Docker Manager (projeto `foodsaas-backend`) reconstrói da GitHub source automaticamente — executado e confirmado com "O esquema do banco de dados está atualizado!" (57 migrações). **Validação**: `PATCH /company/settings` retornava 400 com `nomeFantasia should not exist` (forbidNonWhitelisted) enquanto VPS rodava código antigo; após deploy retornou 200 + toast "Configurações salvas com sucesso!".

133. **feat(demo): hero 2 colunas + iMac mockup CSS (commit `98e9b00e`)**: `frontend/app/demo/page.tsx` — hero reestruturado de coluna única centrada para layout `lg:flex-row` com texto à esquerda e monitor à direita. `HeroDeviceMockup` (substituído): agora é um iMac CSS-only (sem tablet lateral). Estrutura: bezel `rounded-[18px] bg-[#1c1c1e]` com camera dot no topo; tela `aspectRatio: 8/5` mostrando mini landing FoodSaaS (navbar, 4 feature cards, split "PDV AGILIZADO" texto+screenshot); stand trapézio via `clipPath: polygon(28% 0%, 72% 0%, 85% 100%, 15% 100%)`; base oval. Hero: hero `<section>` passa de `text-center` para `flex-col lg:flex-row items-center gap-10`; coluna esquerda `flex-1` com `text-center lg:text-left`, `lg:justify-start` nos botões; coluna direita `hidden lg:block max-w-[520px]` — invisível em mobile, aparece em lg+. `tsc --noEmit` limpo.

132. **feat(demo): seletor de nicho interativo + simulador de portfólio (commit `ca4c8839`)**: `frontend/app/demo/page.tsx` ganhou 2 novas seções sem tocar em LeadCaptureModal, auth flow ou DemoCard. (1) **Seletor de nicho por `useState`**: `selectedNiche` inicializado de `resolveNiche(searchParams.get("niche"))` (URL continua funcionando) mas agora pode ser trocado clicando em 4 pills — "🍕 Restaurantes & Pizzarias", "🍱 Marmitarias & Dark Kitchens", "🥟 Pastelarias & Food Trucks", "🌭 Hot-Dogs & Lanchonetes"; ao clicar, hero badge e plan cards atualizam imediatamente (sem reload). `NICHES_LIST` array de `{ key: NicheKey; emoji; label }` governa o render das pills. (2) **`PortfolioSimulator`**: componente com `key={selectedNiche}` (reinicia estado automaticamente ao trocar nicho); mock browser frame (barra de dots, URL falsa, indicador verde "aberto"); `NICHE_MENU_ITEMS[niche]` define 6 categorias por nicho com emoji, nome e contagem; toggle Grade (grid 2-3 colunas) / Lista; edição inline de nome ao clicar (input com autoFocus, blur/Enter confirma, Escape cancela); botões Up/Down para reordenação de posição; botão X para exclusão; botão "Resetar" restaura items originais; overlay de controles visível só no `group-hover` (não polui visual). Novos ícones: `ChevronUp`, `LayoutGrid`, `List`, `RotateCcw`. `tsc --noEmit` limpo.

131. **feat(demo): conteúdo dinâmico por nicho via `?niche=` (commit `b1ced584`)**: página `/demo` agora adapta imagens e copy dos 3 planos ao nicho detectado na query string. `frontend/lib/nicheData.ts` (novo) — `NICHE_DATA` com 4 nichos (`restaurante`/`marmitaria`/`pastelaria`/`hotdog`), cada um com `badge`, `heroHighlight`, `screenshots` (URLs Unsplash 800×320) e `plans.{basic|pro|enterprise}.{tagline, features[]}`. `resolveNiche(raw)` valida o param e faz fallback para `"restaurante"`. `frontend/app/demo/page.tsx`: `DemoContent` (componente interno com `useSearchParams`) substitui o `DemoPage` anterior; exportado como `DemoPage` com `<Suspense>` (Next.js App Router). Hero exibe badge do nicho (`{nicheContent.badge}`) quando `niche !== "restaurante"`. Subtítulo troca para `{nicheContent.heroHighlight}` + lista completa condicional apenas para `restaurante`. `DemoCard` recebe `enriched = { ...demo, tagline, features }` mesclado com o nicho ativo; screenshots por plano vêm de `nicheContent.screenshots[pk]`. Zero mudanças de backend. `tsc --noEmit` limpo. **Uso**: `/demo?niche=marmitaria`, `/demo?niche=hotdog`, `/demo?niche=pastelaria`. Imagens Unsplash substituíveis por assets próprios em `/public/demo-assets/niches/`.

134. **fix(auth): welcome email + admin notification on signup + diagnóstico 500 em /super-admin/clientes (commit `4285a42e`)**: causa raiz do 500 em `GET /api/super-admin/customers-report` identificada via Chrome MCP + fetch direto ao VPS — `PrismaClientValidationError: Unknown field 'phone' for select statement on model User`. O campo `phone` estava no `users.select` da query em `getCustomersReport` mas o model `User` não tem esse campo. O fix já existia em commit `f18b9c84` (18/06) mas o VPS estava rodando código anterior ao deploy da migração `20260624200000_add_inscricao_estadual`. **Solução**: git push `4285a42e` inclui as notificações de boas-vindas → ao clicar "Implantar" no Hostinger, o VPS reconstrói com o código correto (sem `phone` no select). **Notificações adicionadas**: (1) `WELCOME` enviado ao email do novo cliente via `setImmediate` — usa `FRONTEND_URL` env (fallback Vercel URL); corpo atualizado de "7 dias" para "3 dias de trial"; (2) `NEW_SIGNUP` enviado ao `ADMIN_NOTIFY_EMAIL` com companyName/adminName/email/segment/trialEnds — mesmo padrão do `DEMO_LEAD`. Ambas fire-and-forget; sem SMTP configurado → WARN log (não quebra signup). `tsc --noEmit` limpo em ambos. **PENDENTE**: clicar "Implantar" no Hostinger Docker Manager (`hpanel.hostinger.com/vps/1747711/docker-manager → foodsaas-backend → Implantar`) para que o VPS rode o código corrigido.

135. **fix(analytics+demo+modulos): 3 correções + redesign modulos + demo delivery (commit `ba897eab`)**: (1) **Bug analytics → login** (`PlanoTab.tsx`): botões "Configurar" e "Abrir Configurações" nos cards de Analytics e Marketing usavam `<a href>` puro (full page reload) — quando cookie `token` não estava presente, middleware redirecionava para `/login`. Fix: trocado por `<Link>` do `next/link` (client-side nav, sem reload). (2) **Super-admin nav "Analytics"** apontava para `/super-admin/leads` (mesma rota do item "Leads") — corrigido para `/super-admin/clientes` que tem 6 KPI cards com breakdown real. (3) **Demo Delivery**: 4º conta demo adicionada: `demo-delivery@foodsaas.demo` / `DemoDelivery@123` / plano `DELIVERY` / cor `#ea580c` / empresa "Marmita Express Delivery". Backend: `DEMO_PLAN_EMAIL`, `initDemoCompanies`, `DEMO_EMAILS`, `SAFE_DEMO_IDS`, `TIERS`, `DEMO_COLORS` atualizados; categoria `pratos` (+ 6 produtos de marmitaria) adicionada ao `CATEGORIES_DEF` e `PRODUCTS_MAP`. Frontend: `demoThemes.ts` — tema laranja + `DEMO_ACCOUNTS` com DELIVERY; `demo/page.tsx` — `PLAN_CARDS` com 4 cards, grid `sm:grid-cols-2 lg:grid-cols-4`. (4) **`/super-admin/modulos`**: redesign completo com `MODULE_CATALOG` (17 módulos categorizados), cards premium escuros com ícone emoji em gradiente colorido, toggle ativo/inativo em tempo real, badge "Em configuração" para módulos sem backend ativo, filtro por categoria (pills), link → `/super-admin/modulos/[slug]` (detalhe). `tsc --noEmit` limpo em ambos. Deploy: **clicar "Implantar" no Hostinger** (corrige o 500 em `/super-admin/clientes` + activa o demo delivery). **PENDENTE**: criar `/super-admin/modulos/[slug]/page.tsx` (página de detalhe do módulo — ainda não existe).

136. **feat(pdv): categorias aninhadas em acordeão + EAN + foto Open Food Facts (commit `76fba3a2`)**: hierarquia pai/filho de categorias no PDV. **Schema**: `Category.parentCategoryId String?` + auto-relação `parent/children` (`@@CategoryParent`). Migration `20260625100000_add_parent_category` idempotente (`ADD COLUMN IF NOT EXISTS` + FK via pg_constraint + `CREATE INDEX IF NOT EXISTS`). `Dockerfile`: `--rolled-back` adicionado. **Backend** `categories.service.ts`: `findAll()` inclui `children[]` aninhados + `parentCategoryId`; `create()` conecta ao pai via `parent:{connect:{id}}` quando `parentCategoryId` fornecido; `update()` aceita `parentCategoryId: string|null`. **Frontend PDV** `app/pdv/page.tsx`: (1) `Category` interface recebe `parentCategoryId?` e `children?[]`; (2) estado `expandedCategories: Set<string>` para acordeão; (3) `filteredProducts` detecta quando categoria selecionada tem filhos e inclui `p.categoryId` nos filhos; (4) `activeIsBeverage` verifica também se algum filho tem `categoryType==="bebidas"`; (5) sidebar desktop mostra apenas top-level, com seta `ChevronDown` para pais com filhos — clique expande acordo + seleciona o pai; filhos aparecem indentados em `ml-3`; (6) sidebar mobile mostra apenas top-level (clique no pai exibe produtos do pai + todos os filhos); (7) grid de bebidas: se produto tem `eanCode` e sem `imageUrl`, usa `https://images.openfoodfacts.org/images/products/{ean}/front.small.jpg` como fallback; EAN exibido abaixo do nome em `font-mono text-[10px] text-zinc-500`. **Configuração**: editar categorias em `/categories`, definir `parentCategoryId` das subcategorias via PATCH. **VPS rebuild**: executado manualmente via `docker build --no-cache ... && docker restart` no terminal web Hostinger (commit `76fba3a2` incluso). `tsc --noEmit` limpo em backend e frontend. `GET /api/health → {status:"ok",ready:true}` confirmado.

137. **fix(p0): JwtStrategy SYSTEM_SUPER_ADMIN bypass + Central de Clientes whatsapp/summary/filter (commits `755c6058`, `e6d5190b`, `1905d3f4`, `8a215e15`, VPS rebuild 23/06/2026)**: 4 bugs críticos em produção corrigidos em sequência cirúrgica. **(1) 500 em todos os endpoints protegidos** — `VisitsModule` adicionou `SuperAdminGuard` como provider (commit `6058fb8f`) mas `SuperAdminGuard` injeta `JwtService`; sem `JwtModule` importado no `VisitsModule`, NestJS falhava ao inicializar → todos os endpoints retornavam 500. Já corrigido em `6058fb8f` (sessão anterior). **(2) 500 específico na ativação de módulos** — após o VPS subir, `POST /company-module/activate` com token de super-admin continuava retornando 500. Causa raiz: `JwtStrategy.validate()` chamava `prisma.user.findUnique({ where: { id: payload.sub } })` sem null-check; token SYSTEM_SUPER_ADMIN tem `sub: undefined` → `PrismaClientValidationError`. Fix em `jwt.strategy.ts`: bypass antes do DB lookup — `if (!payload.sub && payload.role === 'SYSTEM_SUPER_ADMIN') return { userId: null, email, companyId: null, role }`. **(3) WhatsApp ausente em Estação Grill / Xis@** — `getCustomersReport()` em `super-admin.service.ts` não incluía `whatsapp: true, phone: true` no select de Company; `c.whatsapp` era sempre `undefined`. Fix: adicionado ao select; `whatsapp: c.whatsapp ?? c.phone ?? ''` no mapeamento de rows. **(4) "Clientes Ativos" zerando tudo** — duas causas combinadas: (a) Frontend enviava `type=active` (camelCase) para API que comparava `r.type === 'ACTIVE'` (SCREAMING_SNAKE_CASE) → 0 resultados; (b) Backend calculava o `summary` dos KPI cards a partir de `all` (já filtrado) → quando filtro retornava 0 itens, todos os cards zeravam. Fix frontend: `KPI_TO_TYPE` mapping em `super-admin/clientes/page.tsx` (`total→ALL`, `active→ACTIVE`, `exClient→EX_CLIENT` etc.). Fix backend: `const unfiltered = [...companyRows, ...leadRows]` computado antes do filtro; `summary` derivado de `unfiltered`; filtro aplicado em `filtered` separado com `.toUpperCase()` na comparação. **Deploy**: VPS rebuild manual via `docker stop + docker rm + docker compose build --no-cache + docker compose up -d` no terminal web Hostinger (3 rebuilds necessários por conflito de nome de container — resolvido com `2>/dev/null`). `GET /api/health → {status:"ok",ready:true}` confirmado. Vercel auto-deployou frontend `8a215e15`.

138. **feat(super-admin): notificações clicáveis + WA do dono no signup + avisos de trial + SuperAdminTopBar unificado (commits `c0bb0145`, `85538c5b`, `3f2489c4`, `fe13e91d`)**: 4 melhorias na área super-admin. (1) Clique em notificação do sino faz scroll + highlight (ring indigo 2s) na linha da empresa na tabela; `companyId` adicionado aos objetos de notificação; `id="company-row-${co.id}"` nas rows. (2) `auth.service.ts signup()` e `demoAccess()` chamam `notifyOwnerWhatsapp()` (fire-and-forget, AbortSignal.timeout 10s, usa `EVOLUTION_API_URL`/`EVOLUTION_API_KEY`/`EVOLUTION_INSTANCE_NAME`/`NOTIFY_WHATSAPP_NUMBER` env vars). Env obrigatória: `NOTIFY_WHATSAPP_NUMBER=5567991753455`. (3) `RetentionService.sendTrialWarnings()` enviado no cron diário (3h): busca empresas com trial ativo + WhatsApp preenchido, envia mensagem consultiva nos dias -3 e -1 via Evolution API. `SUPPORT_WHATSAPP` env com fallback `5567991753455`. (4) `SuperAdminTopBar` component criado em `components/super-admin/SuperAdminTopBar.tsx` — barra sticky `z-40` dark com botão de voltar `hover:scale-[1.03]` + slot de `actions`; `saBtn` class exportada para botões com hover consistente. Aplicado em: `dashboard`, `clientes`, `leads`, `modulos`, `pricing`, `tema`, `construtor`.

139. **feat(construtor): redesign completo — DnD + paleta de cores + preview ao vivo (commit `a830743a`)**: `/super-admin/construtor` reescrito como painel profissional de 3 colunas. **Layout**: sidebar esq (56px nav), área central (config), coluna dir (preview phone). **4 abas**: Layout (templates por segmento + tipo GRID/LIST + border radius SM/MD/LG/FULL), Cores (8 paletas temáticas prontas + 5 campos customizados com `input[type=color]` nativo), Blocos (drag-and-drop `@hello-pangea/dnd` com handle visual + toggle visibilidade + info por bloco), Google Review (campo URL + feedback visual). **Features**: dropdown de empresa, strip de preview de cores, mini-biblioteca com salvar-como-template, preview mockup estático reativo às cores selecionadas, iframe ao vivo quando empresa selecionada. Zero novas deps, zero migrations, zero backend alterado.

140. **fix(trial) + feat(analytics): período trial 3→7 dias + GA4 + Meta Pixel na plataforma (commits `24344552`, `ec0ee6b2`, `9e0de506`, `e21a7213`)**: 4 mudanças desta sessão. (1) Texto "3 dias" → "7 dias" em 3 lugares: `frontend/app/demo/page.tsx` (stat card + rodapé CTA), `frontend/components/AiTrialLock.tsx` ("teste gratuito de 7 dias expirou"), `backend/src/modules/notifications/notifications.service.ts` (e-mail de boas-vindas). (2) `frontend/components/PlatformAnalytics.tsx` (novo): componente `"use client"` com `next/script` que injeta GA4 (`gtag`) e Meta Pixel (`fbq`) usando env vars `NEXT_PUBLIC_GA_MEASUREMENT_ID` e `NEXT_PUBLIC_META_PIXEL_ID` — adicionado no `app/layout.tsx` para rastrear TODAS as páginas. (3) `app/demo/page.tsx`: `useEffect` de visita expandido com `fbq('track', 'ViewContent')` e `gtag('event', 'page_view_demo')` — cria audiência específica de visitantes da /demo no Meta Ads e GA4. `enterDemoWithLead()` dispara `fbq('track', 'Lead')` + `gtag('event', 'generate_lead')` ao preencher formulário. (4) **Fix build Vercel**: `getSaToken()` em `construtor/page.tsx` usava `localStorage` no nível de módulo → `ReferenceError: localStorage is not defined` durante SSR. Fix: `typeof window !== "undefined"` guard. **Env vars Vercel configuradas**: `NEXT_PUBLIC_GA_MEASUREMENT_ID=G-GGMM25TZYZ`, `NEXT_PUBLIC_META_PIXEL_ID=1116533700670528` (Mestra Gestão Digital Pixel). **VPS pendente**: commits `838cdfb7` (roles-guard SYSTEM_SUPER_ADMIN bypass) e `ede61eac` (clientes sem demos) precisam de "Implantar" no Hostinger Docker Manager.

141. **feat(company): slug amigável por loja — URL /menu/nome-da-loja (commit `cb9be7e1`)**: cada empresa agora tem uma URL legível no cardápio digital. `Company.slug String? @unique` adicionado ao schema. Migration `20260625200000_add_company_slug` idempotente: `ADD COLUMN IF NOT EXISTS`, backfill via CTE com `ROW_NUMBER()` para garantir unicidade (fallback para primeiros 8 chars do ID quando nome vazio), `ADD CONSTRAINT IF NOT EXISTS`. `slugify()` util em `src/common/utils/slugify.ts` (NFD normalize → remove acentos → lowercase → hífens). **Backend**: `CompanyService.resolveId(slugOrId)` + `generateUniqueSlug(name)` + `findOne(slugOrId)` aceitam slug ou ID real. Slug resolution adicionada em: `products.service.ts publicMenu`, `themes.service.ts getTheme`, `delivery-config.service.ts findAllPublic`, `pizza-size-configs.service.ts findAll`, `online-orders.service.ts create` (normaliza `dto.companyId` para ID real logo no início). `auth.service.ts signup()` gera slug único do `companyName` no create. `Dockerfile --rolled-back` adicionado. **Frontend `/configuracoes` Minha Loja**: campo "URL do cardápio (slug)" com prefixo `/menu/` visual e validação inline (só letras minúsculas/números/hífens). **Frontend `/menu/[slug]`**: `realCompanyId` state inicializado com `companyId` da URL; após `companyRes.ok`, `setRealCompanyId(cd.id)` salva o ID real; todos os POST bodies (online-orders, coupons/validate, payments/pix) trocados para `realCompanyId`. URL `/menu/alexandria` agora funciona. `tsc --noEmit` limpo em backend e frontend. `prisma generate` OK.

103. **feat(whatsapp-ia): prompt-mestre multi-ambiente — Kely cerebro central (commit 7aa87ad)**: a IA da Kely agora detecta o ambiente da empresa dona da conexao e troca a persona. **detectAmbiente(email, companyId)**: platform@foodsaas.internal -> R_FOOD_SAAS (vende o sistema FoodSaaS, capta leads, conduz pra fechamento de plano/demo); empresas demo (@foodsaas.demo ou id demo-) -> LOJA_DEMO (demonstra recurso + upsell de plano); demais -> CLIENTE_REAL (mantem fluxo de pedidos atual com buildSystemPrompt + [CMD], INALTERADO). **buildMasterPrompt(ambiente, dadosContexto)** em whatsapp-ai-prompt.service.ts injeta {{AMBIENTE}} e {{DADOS_CONTEXTO}} no MASTER_PROMPT (constante = prompt fornecido pelo usuario, persona ultra-humanizada, 3 modos, resposta JSON obrigatoria). **buildAmbienteContext** monta o contexto: R_FOOD_SAAS=pitch de modulos+planos+diferenciais; LOJA_DEMO=plano atual+cardapio exemplo; CLIENTE_REAL=menuCtx. **parseMasterResponse(raw)** extrai resposta_para_o_cliente + status_interno{etapa, chamar_atendente_humano, dados_carrinho_ou_lead} (tolerante a code fences). Em runAiResponse: isSalesMode = ambiente!=CLIENTE_REAL; modos de venda usam master prompt + parse JSON + envia reply + transbordo; CLIENTE_REAL segue o parseCommands existente. **PENDENTE**: (1) UI dedicada no Super Admin (usuario pediu "rebuild completo" — fiz o cerebro/persona, falta mover a config pra fora da pagina IA Garcom); (2) lead capture real (etapa/leadOrCart sao parseados mas ainda nao gravados via LeadsService); (3) order creation no modo CLIENTE_REAL via JSON (hoje so o [CMD] cria pedido). **Pre-requisito ja resolvido**: modelo Claude corrigido pra claude-haiku-4-5-20251001 (dropdown so tinha modelos 404; commit 88ce4b7 adicionou ao DEPRECATED_MODELS). **PENDENTE (2) RESOLVIDO no item 142**: lead capture do WhatsApp em modo vendas agora persiste via `WhatsappAiService.persistSalesLead` → `LeadsService.upsert`.

142. **feat(ia-funnel): 4 lacunas do funil de captação/conversão + diagnóstico de canais no /health (commits `455d9c91` + `fd36db2c`)**: auditoria do funil de IA (Luna `/ia-demo`, Kely WhatsApp multi-ambiente, RetentionService cron, AiTrialLock) apontou 4 lacunas; todas resolvidas. **(1) Aviso de fim de trial só por WhatsApp** — `retention.service.ts sendTrialWarnings()`: removido o filtro `whatsapp: { not: null }` da query e a guard `if (!company.whatsapp) continue`; agora WhatsApp é canal **primário** (dias -3 e -1) e e-mail é **backup** (ou canal único quando a loja não tem WA, ou fallback se o envio WA lançar). Novo template de e-mail `TRIAL_WARNING` em `notifications.service.ts` (subject + body com urgência, CTA `/assinatura`, link WA de suporte). Contadores separados `sentWa`/`sentEmail` no log. **(2) Leads do WhatsApp não persistiam** — `whatsapp-ai.service.ts`: no modo vendas (`ambiente === 'R_FOOD_SAAS'`), o JSON `dados_carrinho_ou_lead` era parseado mas descartado. Novo helper `persistSalesLead(conv, leadOrCart, reply)` (fire-and-forget, `.catch` em log): `sessionToken` estável `wa-sales-${conv.id}`, `whatsapp = conv.customerPhone` (já qualifica o lead → status QUALIFICADO), extrai `name`/`company` por aliases PT/EN (`nome|name|nome_cliente`, `empresa|company|nome_loja|loja|restaurante|estabelecimento`), normaliza plano citado para BASIC/PRO/ENTERPRISE, `conversationSummary = reply.slice(0,500)`. Injeção: `LeadsModule` adicionado ao `whatsapp-ai.module.ts` imports; `@Optional() private leadsService?: LeadsService` no constructor. Leads do WA passam a aparecer em `/super-admin/leads`. **(3) Welcome genérico** — `notifications.service.ts` template `WELCOME` reescrito com onboarding "comece em 3 passos" (cardápio → cardápio digital → PDV/cozinha) e CTA "Começar agora". **(4) SMTP não verificável** — `GET /api/health` agora retorna `channels: { email: boolean, whatsapp: boolean }` (apenas booleanos, nunca expõe valores). **IMPORTANTE**: o `/api/health` é servido por um **middleware Express em `main.ts` (linhas ~79-88), ANTES do roteador Nest** — o `@Get('health')` em `app.controller.ts` é shadowed e nunca alcançado (deixei nota no controller). O diagnóstico foi colocado no handler de `main.ts` (que já tinha `configService`). `email` = `SMTP_HOST && SMTP_USER && SMTP_PASS` presentes; `whatsapp` = `EVOLUTION_API_URL && EVOLUTION_API_KEY && EVOLUTION_INSTANCE_NAME`. **Env vars do funil de e-mail (VPS Hostinger → foodsaas-backend)**: obrigatórias para qualquer e-mail sair — `SMTP_HOST`, `SMTP_USER`, `SMTP_PASS`; opcionais — `SMTP_PORT` (default 587, `secure:false`), `SMTP_FROM` (default `noreply@foodsaas.com.br`). Sem as 3 obrigatórias, `NotificationsService.send` loga `WARN [EMAIL NOT SENT — SMTP not configured]` e retorna (não quebra signup/cron). Relacionadas: `ADMIN_NOTIFY_EMAIL` (recebe NEW_SIGNUP/DEMO_LEAD), `FRONTEND_URL` (links nos e-mails), `SUPPORT_WHATSAPP` (default 5567991753455), `EVOLUTION_API_URL`/`EVOLUTION_API_KEY`/`EVOLUTION_INSTANCE_NAME` + `NOTIFY_WHATSAPP_NUMBER` (avisos WA do dono e de trial). `tsc --noEmit` limpo (exit 0). **DEPLOY CONCLUÍDO em 24/06/2026** via terminal (ver item 143). `channels:{email:true,whatsapp:true}` confirmado em prod. Fan-out de notificação completado: canal #2 (WhatsApp boas-vindas ao cliente novo — commit `b1849db3` `auth.service.ts sendClientWelcomeWhatsapp`) + canal #4 (`ADMIN_NOTIFY_EMAIL=mestragenciadigital@gmail.com` adicionado ao `.env`). SMTP = Gmail (`smtp.gmail.com:587`, app password). `EVOLUTION_INSTANCE_NAME=kely-cmq7d3dx-mq8orbg9` + `NOTIFY_WHATSAPP_NUMBER=5567991753455` adicionados.

143. **⚠️ PROCEDIMENTO REAL DE DEPLOY NO VPS (corrige itens 128/130/136 — a UI do Hostinger NÃO builda)**: descoberto em 24/06/2026 após 3 deploys que "concluíam com sucesso" mas nunca atualizavam o código. **Causa raiz**: o `docker-compose.yml` em `/opt/foodsaas-backend/` tem `build.context: https://github.com/ruffinuscuritiba/food-system-Sas-ERP.git#main:backend` + `image: foodsaas-backend:latest`. Os botões **"Salvar e implantar"** e **"Atualizar"** da UI rodam `docker compose pull` (tenta baixar `foodsaas-backend:latest` do Docker Hub → **falha** "pull access denied, repository does not exist") e então **recria o container com a imagem LOCAL em cache** (código antigo) reportando "Project deployed successfully". **A UI nunca builda do GitHub.** Só atualiza env vars (que ficam em `/opt/foodsaas-backend/.env`, lido via `env_file`). **Pré-requisito**: `git push origin main` ANTES — o VPS builda do `#main`; commit local não pushado = build do código antigo (erro clássico desta sessão: 3 timeouts até perceber `ahead 3` sem push). **PROCEDIMENTO CORRETO (terminal web Hostinger)**: (1) Gerenciador Docker → linha `foodsaas-backend` → **Terminal** (abre dentro do container) → digitar `exit` para cair no host `root@srv1747711`. (2) `cd /opt/foodsaas-backend`. (3) **`docker compose build --no-cache backend`** — o `--no-cache` é OBRIGATÓRIO: o `docker compose build` normal mostra `CACHED [internal] load git source` e reusa o clone antigo do git (mesmo após push). Build ~60-85s. (4) **Verificar** que a imagem tem o código novo ANTES de subir: `docker run --rm foodsaas-backend:latest sh -c "grep -c <marcador> dist/main.js"` (ex.: `channels`) → `>0` = código novo. (5) `docker compose up -d`. Se der **"cannot remove container ... is running"** ou **"nome já está em uso"** (conflito comum), usar: **`docker rm -f foodsaas-backend-backend-1 && docker compose up -d`** (downtime ~15s). (6) Confirmar: `curl https://api.srv1747711.hstgr.cloud/api/health` deve refletir o código novo. **Env vars**: editar via UI (escreve no `.env`) OU append direto no terminal `echo 'KEY=valor' >> /opt/foodsaas-backend/.env` + recriar container. Verificar carregada: `docker compose exec backend printenv KEY`. **Observação**: após `rm -f` + `up -d` o container pode ficar com nome prefixado (`<hash>_foodsaas-backend-backend-1`) — cosmético, funciona; `docker compose down && up -d` limparia (mais downtime). **Container/compose**: projeto `foodsaas-backend` em `/opt/foodsaas-backend/docker-compose.yml`, rede externa `proxy` (Traefik roteia `api.srv1747711.hstgr.cloud` → :3001), `restart: unless-stopped`, `NODE_OPTIONS=--max-old-space-size=512`.

144. **feat(design-system): upgrade visual ponta-a-ponta — tema por loja + claro/escuro + cinza-azul + realismo (commits `1d756dfe`→`19943196`, `41914506`)**: refatoração do tema seguindo o Contrato de Design System (memória `feedback_design_system_contract`). **(A) Fonte única de cor:** `CompanyTheme.primaryColor` = `--color-primary` (accent) por loja, aplicado pelo `ClientShell` em TODAS as páginas. Cores hardcoded eliminadas — item ativo do menu (`activeColor "green"/"blue"` neutralizado → sempre `bg-primary`), CTAs "QR Code"/"Suporte" (laranja/emerald fixos → `text-primary`). **(B) Tokens-raiz (`globals.css`):** `--surface-0` (#0A0B0E) → `--surface-3` (#1A1D28) cinza-azul, `--border-ui`, `--radius-card` (16px), `--accent`; tokens legados (`--app-sidebar`, `--pdv-*`, `--app-page-bg`) herdam as surfaces. NUNCA preto puro. **(C) Modo claro/escuro POR LOJA:** `CompanyTheme.darkMode` (false=claro) → ClientShell põe classe `theme-light`/`theme-dark` no `<html>`. globals tem flips de texto escopados: `.pdv-root` e `.app-sidebar-nav` (no claro, texto→escuro) e `.admin-page` (no escuro, `:not(.theme-light)`, cards bg-white→surface-2, gray→claro, inputs legíveis; accent mantém branco). **(D) PDV:** fundos pretos NEUTROS (#0a0a0c/#17171c — não tingidos), accent = cor da loja; `pdvTheme` (loadPdvTheme/override de --color-primary) REMOVIDO do `app/pdv/page.tsx`; container ganhou classe `pdv-root`; fix do 404 `pizza-size-configs` (era `fetch("/api/...")` relativo → frontend; trocado por `api.get` → backend, commit `41914506`). **(E) Páginas admin:** wrapper usa `bg-[var(--app-page-bg)]` (escuro=surface-0, claro=#F5F3EF); `<main>` ganhou classe `admin-page`; seguem dark/light da loja. **(F) Construtor (`super-admin/construtor`):** `PhonePreview` = iPhone hiper-realista (frame gradiente titânio, Dynamic Island, sheen, botões laterais) com iframe do cardápio real; showcase de 3 telas (`LayoutPhone`) usa **fotos reais** de `/public/demo-assets/` (const `FP` de gradientes removida). **(G) super-admin roots** → `var(--surface-0)` (commit `a29c362b`). **PENDENTE de verificação:** páginas admin no ESCURO são "primeiro passo" (light-first → dark via override CSS) — gráficos recharts, badges e `bg-[#fff]` arbitrários podem precisar de ajuste pontual.

146. **fix(theme): PDV light-mode — 3 bugs corrigidos + textura de fundo configurável (commit `e64a07a8`)**: (1) **Texto do banner ilegível** — `.pdv-root .text-white { color: #111827 }` era aplicado a TODO o `.pdv-root` incluindo a seção de produtos/banners; corrigido scopando apenas para `aside` e `header`, enquanto `.pdv-root section .text-white` recebe `color: #fff !important` explícito para manter legibilidade no overlay. (2) **Overlay do banner muito escuro** — seletor `[class*="bg-black\/"]` (com escape) não capturava a classe `bg-black/60` no HTML; corrigido para `[class*="bg-black/"]` (sem escape); overlay trocado de `rgba(0,0,0,0.22) solid` para `linear-gradient(to bottom, rgba(0,0,0,0.04) 0%, rgba(0,0,0,0.42) 100%)` — claro no topo, escuro embaixo onde o texto fica. (3) **Botões sem 3D** — `box-shadow` sem `!important` era sobrescrito pela regra global; adicionado `!important` com sombra mais intensa (`0 4px 10px rgba(0,0,0,0.14) + inset brilho`) + `hover: translateY(-2px)`. (4) **Textura de fundo** — `CompanyTheme.backgroundImageUrl String?` + migration `20260629000000_add_background_image_url` idempotente; `ClientShell` aplica `--bg-texture-url: url(...)` quando campo existe; `body::before` pseudo-elemento fixed z-index -1 com `opacity: 0.25` (75% suavidade); editor de tema em `/configuracoes?tab=aparencia` ganhou botão "+ Textura" ao lado do campo Fundo (upload via `/upload`, mini-preview com botão Remover). Dockerfile: `--rolled-back 20260629000000_add_background_image_url` adicionado. **VPS rebuild necessário** para ativar a migration.

147. **feat(qr-campaigns): Recuperação de Clientes via QR Code Inteligente (commit `caee2255`)**: módulo completo end-to-end para converter clientes de apps externos (iFood, 99Food) para o cardápio próprio via cupom impresso no ticket. **Schema**: enums `CampaignType` (RECUPERACAO_IFOOD/FIDELIZACAO/CASHBACK/PRIMEIRA_COMPRA) + `DiscountType` (PERCENTUAL/FIXO). Models: `Campaign` (companyId, name, type, discountType, discountValue, minimumOrder, startsAt, endsAt, limitPerCustomer, limitPerDevice, status, isActive), `QrCode` (token @unique, campaignId, companyId, used, usedAt, usedByPhone, usedOrderId, expiresAt, deviceId, ip, scanCount, printBlock Json), `CouponRedemption` (QrCode ↔ Order/OnlineOrder). Migration `20260630000000_add_qr_recovery_campaigns` idempotente. **NestJS**: `QrCampaignsModule` com `QrCampaignsService` (generateToken 6 chars sem I/O/1/0, detectDevice, extractRealIp, listCampaigns, createCampaign, toggleCampaign, deleteCampaign, generateForOrder, resolveToken, validateForCheckout, redeem, getMetrics, listQrCodes) + `QrCampaignsController` (JWT-protected CRUD) + `QrRedirectController` (prefixo `r`, `GET /r/:token` sem auth, Throttle 60/min, seta cookie `qr_promo` httpOnly:false base64-JSON, redireciona 302 para cardápio). `generateForOrder` detecta `orderSource` (IFOOD/99FOOD → RECUPERACAO_IFOOD, primeiro pedido → PRIMEIRA_COMPRA, demais → FIDELIZACAO). `redeem` usa `$transaction` atômico com anti-double-spend. `getMetrics`: generated/scanned/conversionRate/totalOrders/totalRevenue/totalDiscount/avgTicket/byCampaign. **Hooks**: fire-and-forget em `OnlineOrdersService.create` (`void (async () => {})()` dentro de `setImmediate`) e `OrdersService.create/updateStatus` (`setImmediate(async () => {})`). `@Optional()` injection para evitar circular deps. **Frontend**: `app/campanhas/page.tsx` — KPI cards + lista de campanhas com badges de tipo + inline create form. `app/r/[token]/page.tsx` — server component que redireciona para `/api/r/:token`. `/r` adicionado às PUBLIC_ROUTES em `middleware.ts` e `ClientShell.tsx`. Sidebar: item "Campanhas QR" com `QrCode` icon na seção IA. **fix(super-admin theme)**: `ClientShell.tsx` adiciona `useEffect` que força `document.documentElement.classList.add("theme-dark")` para rotas `/super-admin/*` — sem isso, o redesign item 144 (default claro) deixava texto branco invisível nas páginas super-admin (que são PUBLIC_ROUTES e não recebem o tema da empresa). **Pendente**: integrar `printBlock` nos templates `KitchenTicket`/`Receipt80mm` para imprimir QR no ticket térmico; ler cookie `qr_promo` no checkout do cardápio e mostrar banner de desconto auto-aplicado.

148. **feat(qr-campaigns): desconto automático no cardápio + QR no ticket + WA de boas-vindas + reprint PDV (commits `3a0d68c0`, `0733084f`, `0b665392`)**: completação do módulo QR Campaigns end-to-end. (1) **Desconto automático no cardápio** (`app/menu/[companyId]/page.tsx`): `useEffect` lê cookie `qr_promo` após slug resolver companyId real → `POST /qr-campaigns/checkout/validate` valida token → estado `qrPromo` armazena tipo/valor/campaignName/minimumOrder; banner verde no checkout com toggle "Aplicar"/"Aplicado"; `qrPromoDiscount` calculado (PERCENTUAL ou FIXO) e incluído em `totalDiscount` + body do POST de pedido; step 2b após criar pedido: `POST /qr-campaigns/checkout/redeem` com `customerPhone/customerName` + `keepalive:true` + limpa cookie após sucesso. (2) **QR no ticket térmico**: `QrPrintBlock` type + `printBlock?: QrPrintBlock|null` em `PrintableOrder`; `buildQrFooterHtml(block)` gera rodapé HTML com `api.qrserver.com` QR 110×110, código em monospace e "✂ recorte e guarde"; integrado em `KitchenTicket.tsx` e `Receipt80mm.tsx`. (3) **WhatsApp de boas-vindas no redeem**: `QrCampaignsService.sendWelcomeWhatsapp()` via Evolution API dispara após `$transaction` (fire-and-forget); `QrCode` guarda `usedByPhone`/`usedByCustomer`; migration `20260630100000_add_qr_used_by` idempotente. (4) **Geração para pedidos iFood**: `OrdersService.create/updateStatus` e `OnlineOrdersService.create` chamam `QrCampaignsService.generateForOrder()` via `setImmediate` (fire-and-forget, `@Optional()` injection). (5) **Botão Reimprimir no PDV** (`app/pdv/page.tsx`): `lastOrder` state salvo após `closePaidOrder`; 1.5s depois faz `GET /qr-campaigns/order/:orderId/print-block`; carrinho vazio mostra card do último pedido com botão "🖨️ Reimprimir" (sempre) e "QR Code" (quando `printBlock` disponível); ambos disparam dynamic import dos templates. **Backend**: `getPrintBlockForOrder(orderId, companyId)` busca via `CouponRedemption`; `GET order/:orderId/print-block` ANTES de `DELETE :id` (match-by-order Nest). `tsc --noEmit` limpo em backend e frontend.

145. **fix(theme-editor + auditoria) (commits `b17868cc` + `ecc0943a`)**: **(1) Editor de tema voltou a funcionar** (`app/theme/page.tsx` = `/configuracoes?tab=aparencia`): a troca de tema estava quebrada porque a seção "Tema PDV / Caixa" mexia no `pdvTheme` (localStorage) que foi DESCONECTADO do app no item 144, e seus `<input type="color">` recebiam `rgba(...)` dos presets → erro de console (`does not conform to #rrggbb`). Fixes: helper `toHexColor()` (rgba/rgb/hex → #rrggbb) em todos os color inputs; **Presets Premium reconectados ao CompanyTheme** (`applyPrimary(cfg.primary)` aplica `--color-primary` na hora + `PATCH /themes/:id`); novo **toggle 🌙 Escuro / ☀️ Claro** (`toggleDarkMode` aplica classe + persiste `darkMode`); `saveTheme` aplica o modo. A seção inferior "Tema PDV / Caixa" (Fundos/Cores Principais via `pdvTheme`/`updatePdvTheme`) ficou **LEGADA/desconectada** (sem erros, mas não afeta o app — pode ser removida). **(2) Auditoria externa — 3 itens verificados:** (a) **SEGURANÇA** — `capture-qr.mjs` (raiz) tinha `API_KEY='kely-evolution-2024-secret'` hardcoded e commitada no GitHub público → trocado por `process.env.EVOLUTION_API_KEY/URL/INSTANCE`; **a chave JÁ VAZOU no histórico — DEVE ser rotacionada na Evolution** (ação do usuário, pendente). (b) `middleware.ts` — `/order-status` adicionado às `PUBLIC_ROUTES` (cliente rastreava pedido e caía no `/login`). (c) **build "bloqueante" da auditoria NÃO reproduziu** — `npm run build` do frontend dá `✓ Compiled successfully`, exit 0, zero erros (e os commits vinham deployando na Vercel); criei `frontend/app/global-error.tsx` (error boundary raiz, estilo Design System) mesmo assim como rede de segurança p/ o edge case React 19 + Next 16. Backend build OK após `prisma generate`.

149. **fix(whatsapp-ai): Kely vendedora MUDA — causa raiz settings=null pós-reprovisionamento + modo vendas 24/7 (commit `5d13a051`, deployado 07/07/2026)**: lead real (Carlos, 5547 9985-1588) mandou mensagem às 00:39 e a Kely não respondeu — dono respondeu manualmente. Diagnóstico em camadas: (a) instância antiga `kely-cmq7d3dx-mq8orbg9` NÃO EXISTE mais na Evolution — a reconexão de 03/07 criou **instância `kely-cmq7d3dx-mr5aakwk`** + **conexão `cmr5aaozs002ualnbvo5whlpu`** (substituem os nomes dos itens 102/104); (b) `WhatsappAiSettings` é 1:1 com `connectionId` (`onDelete: Cascade`) → reprovisionar apaga as settings junto; a conexão nova ficou `settings=null` e `_processIncomingWithConnection` fazia `return` silencioso (mensagem salva no banco, zero resposta). **Fixes**: (1) self-healing — `settings=null` agora cria defaults via `upsert` e continua; (2) `detectAmbiente` movido para o início do fluxo: ambientes de venda (`R_FOOD_SAAS`/`LOJA_DEMO`) fazem bypass do gate de trial (`subscriptionStatus`) e do gate de horário (`Company.businessHours` fecha 23:30/terça — vendedora responde 24/7); (3) settings recriadas em prod via `PUT /api/whatsapp-ai/settings/:connectionId`. **Provider**: caminho GEMINI falhava em ~200ms (throw imediato, provável modelo aposentado/quota) → settings mudadas para `aiProvider=ANTHROPIC` + `claude-haiku-4-5-20251001` (Anthropic OK no VPS, tem fallback automático p/ Gemini). **Env fix**: `EVOLUTION_INSTANCE_NAME` no `/opt/foodsaas-backend/.env` atualizada de `kely-cmq7d3dx-mq8orbg9` (morta) para `kely-cmq7d3dx-mr5aakwk` — sem isso, notifyOwnerWhatsapp/trial warnings/QR welcome estavam TODOS quebrados desde 03/07. **Validação E2E**: webhook simulado (formato Evolution `{event:"messages.upsert",data:{key:{remoteJid},pushName,message:{conversation}}}`) → Kely respondeu venda consultiva em ~3,5s no WhatsApp real. **Empresa plataforma** (`cmq7d3dxs0006gw5pabsljy87`) agora se chama "Ruffinu's Pizzaria" mas mantém email `platform@foodsaas.internal` (detectAmbiente continua R_FOOD_SAAS). Login prod: platform@foodsaas.internal/FoodSaaS2024.

151. **feat(cash+fiscal+totem): fechamento de caixa às cegas + Módulo Fiscal BYOK + Modo Totem no cardápio digital (NÃO deployado — pendente commit/push/VPS rebuild)**: 3 gaps do raio-x competitivo (CPlug/SisFood/Consumer) fechados nesta sessão, todos aditivos e testados localmente contra Postgres real. **(1) Fechamento de caixa às cegas** — `Cash` ganhou `declaredValue/systemValue/difference/closedByUserId/closedByName/closedAt`. `CashService.close(companyId, userId, declaredValue)` — operador informa SÓ o valor contado; o backend calcula `systemValue` (balance atual) e `difference` DEPOIS, sem expor o esperado durante o fechamento. Novo `CashService.history()` (últimos 60 fechamentos) restrito a `GET /cash/history` com `@Roles(SUPER_ADMIN,ADMIN,MANAGER)` — CASHIER nunca vê a comparação. Frontend: nova aba **"Fechamento de Caixa"** em `/financeiro` (`TabCaixa` em `app/financeiro/page.tsx`) — modal de abertura, modal de fechamento (só pede valor contado, mensagem explícita "o sistema não mostra o saldo esperado"), card de conferência e tabela de histórico visíveis apenas para `useAuthStore().isAdmin()`; CASHIER vê só "Aguarde a conferência do gestor". **Achado durante implementação**: não existia NENHUMA UI live de abrir/fechar caixa antes disso — `useCash.ts` só era usado em `app/pdv/page.backup.tsx` (arquivo de backup, não a página ativa); a página `/financeiro` só exibia o KPI de saldo, sem ação. Esta sessão criou o primeiro fluxo funcional de abertura/fechamento. **(2) Módulo Fiscal BYOK (NFC-e/NF-e)** — model `CompanyFiscalConfig` (companyId @unique, provider, environment, `apiKeyEncrypted`/`certPasswordEncrypted` via AES-256-GCM em `src/common/utils/crypto.util.ts` com chave derivada de `FISCAL_ENCRYPTION_KEY` env — **obrigatória configurar no VPS antes de usar o módulo, senão lança erro explícito no primeiro uso**, nunca um fallback inseguro), `certFileBase64`, `isActive`, `termsAcceptedAt/By/Ip`. Modelo de responsabilidade: a plataforma NUNCA emite nota — só repassa a credencial que o próprio cliente cadastrou (Focus NFe por enquanto; eNotas/PlugNotas "em breve" no seletor). `FiscalModule` (`src/modules/fiscal/`): `GET /fiscal/config` (mascarado, nunca retorna segredo em texto plano), `GET /fiscal/terms`, `POST /fiscal/accept-terms` (grava IP+userId+timestamp e loga via `AuditService` — prova jurídica do aceite), `PUT /fiscal/config` (bloqueia salvar credencial sem aceite prévio), `POST /fiscal/active`, `POST /fiscal/emit` (repassa para `FocusNfeProvider.emitNfce` via Basic Auth com a API Key do cliente — payload de itens/CFOP/ICMS é responsabilidade do contratante, **não há emissão automática a partir de Order/OnlineOrder nesta entrega** — infraestrutura pronta, wiring do botão "emitir nota" no pedido fica para uma próxima sessão). Frontend: nova aba **"Fiscal"** em `/configuracoes` (`components/settings/FiscalTab.tsx`) — modal com o texto exato do termo de responsabilidade, checkbox obrigatório, upload `.pfx`/`.p12` via `FileReader` (base64 direto no JSON, sem passar pelo `/upload` de imagens), campos de API Key/senha do certificado sempre write-only. **(3) Modo Totem no cardápio digital** — reaproveita 100% do `/menu/[companyId]` existente (que já aceitava `?table=X` para DINE_IN). Novo param `?totem=1&table=X`: esconde o seletor DINE_IN/DELIVERY/PICKUP (trava em mesa), campo de mesa vira somente-leitura, e a tela de sucesso do pedido faz auto-reset (limpa carrinho/cupom/formulário) 10s depois sozinha para o próximo cliente usar o mesmo tablet — sem esse flag o comportamento de `?table=X` sozinho continua 100% igual (não quebra QR codes de mesa já em uso nas lojas). **Migration** `20260708000000_add_cash_blind_closing_fiscal_config` idempotente (`ADD COLUMN IF NOT EXISTS` + `CREATE TABLE IF NOT EXISTS` + FK via `DO $$ EXCEPTION WHEN duplicate_object`) + `--rolled-back` no Dockerfile. **Validação**: `tsc --noEmit` limpo (backend+frontend), `npm run build` OK nos dois, migration aplicada e testada via script direto contra Postgres local (venda R$50 → fechamento declarado R$148 → diferença -R$2 calculada corretamente; upsert de `CompanyFiscalConfig` com API key OK); round-trip de `encryptSecret`/`decryptSecret` confirmado. **Achado colateral (pré-existente, não tocado)**: o Postgres local de dev estava com `prisma migrate deploy` travado desde a migration `20260613100000_add_integration_client_id` (referencia `IntegrationConfig` que só é criada em `20260615000000_add_integrations`, migration mal datada) — específico deste ambiente local, não afeta produção (que usa `--rolled-back` no Dockerfile para pular esses casos). **PENDENTE**: commit + push + deploy VPS (`docker compose build --no-cache backend` — ver item 143) para a migration chegar em produção; sem isso as 3 features ficam só no repo local. **Ajuste na mesma sessão**: `FISCAL_TERMS_TEXT` (`fiscal.service.ts`) e o checkbox em `FiscalTab.tsx` passaram a incluir a cláusula "confirmo que possuo Certificado Digital válido" + banner "consulte seu contador" (evita a plataforma virar consultoria fiscal). `UpdateNoticesService` (item 150) mudou de "máx 1 aviso/dia" para "máx 1 aviso a cada 15 dias corridos" (`daysSince < 15`, helper `brDate` removido por ficar sem uso) — endpoint manual `POST /update-notices/broadcast` continua ignorando o limite. **Impressão por canal (Totem não imprime pré-conta) — feito nesta sessão**: `OnlineOrder.channel String @default("ONLINE")` novo (migration `20260708100000_add_online_order_channel` idempotente, `--rolled-back` no Dockerfile) — valores `"ONLINE"` ou `"TOTEM"`. **CUIDADO — landmine identificada e evitada**: `OrdersService.updateKitchenStatus` roteia por `source !== 'ONLINE'` → trata como `Order` (tabela PDV). Por isso `channel` foi implementado como campo **paralelo e ortogonal** ao `source` (que continua sempre `'ONLINE'` para linhas de `OnlineOrder`, preservando 100% o roteamento existente) — usado SÓ para decidir impressão, nunca para roteamento de status. `online-orders.service.ts create()` aceita `channel?: string` no `CreateOnlineOrderDto` (interface plana, sem DTO class — `forbidNonWhitelisted` não se aplica, mas o service sanitiza para só aceitar `'TOTEM'` ou fallback `'ONLINE'`). `orders.service.ts findAllForKitchen()` propaga `channel: o.channel ?? 'ONLINE'` nas linhas online sem tocar em `source`. Frontend: `PrintableOrder.channel` novo; `PrintRouterService.route()` pula o ticket `CASHIER` quando `order.channel === "TOTEM"` (a conta real fecha depois via PDV/comanda, não faz sentido pré-conta de kiosk); `/menu/[companyId]/page.tsx` envia `channel: isTotem ? "TOTEM" : undefined` no POST `/online-orders`. `/orders/page.tsx` já espalhava o objeto inteiro (`...order`) no `printOrder`, então herda o campo automaticamente; `/kitchen` e `/historico-pedidos` não precisaram de mudança (kitchen nunca imprime CASHIER; histórico só lista `Order` PDV, que não tem esse campo). **TEF e Modo Offline**: decisão explícita de NÃO implementar nesta sessão — TEF depende de qual adquirente físico (Stone/Cielo/Rede) o cliente já usa (sem essa info e sem hardware/SDK real pra testar, construir um stub seria não-funcional); Modo Offline mantém a recomendação anterior de não fazer (custo arquitetural de banco local + sync, baixo diferencial real pra um SaaS cloud). Migrations pendentes de deploy: `20260708000000_add_cash_blind_closing_fiscal_config` + `20260708100000_add_online_order_channel` — nenhuma foi enviada ao VPS ainda.

**Continuação na mesma sessão — auditoria de caixa + heartbeat de totens (NÃO deployado)**: recomendação externa validou a arquitetura de `Cash` (correto: 1 tabela por tenant, `isOpen` já existe) e pediu 3 fechamentos de lacuna + monitoramento de frota de totens. Decisão sobre bloqueio de venda por caixa fechado: **aviso suave primeiro** (banner, não bloqueia) — hard block fica pra depois que as lojas adotarem o abrir/fechar. (1) **`Order.cashId`** — campo nullable + FK pra `Cash` (`ON DELETE SET NULL`), preenchido em `orders.service.ts create()` com o caixa aberto no momento (lookup simples fora da transação Serializable, não bloqueia a criação se não houver caixa aberto). Migration `20260708200000_add_order_cash_id` idempotente. (2) **Aviso suave no PDV** — `app/pdv/page.tsx` busca `GET /cash/current` no mount; se `isOpen===false`, mostra banner âmbar fixo no topo do `<main>` linkando pra `/financeiro?tab=caixa` — **não bloqueia nenhuma venda**, só avisa visualmente. (3) **Dashboard multi-tenant Super Admin** — `SuperAdminService.getCashStatusReport()` + `GET /super-admin/cash-status` (exclui demos/matriz, mesmo filtro de `getCustomersReport`); widget colapsável "Status de Caixa por Loja" em `/super-admin/clientes` (entre KPIs e filtros) lista todas as lojas com badge aberto/fechado/nunca-abriu + timestamp. (4) **Heartbeat de totens** — novo `TotemModule` (`src/modules/totem/`) espelha o padrão do Printer Agent (item 115): `Map` em memória `${companyId}:${deviceId} → {lastSeen}`, TTL 90s (ping a cada 30s). `POST /totem/heartbeat` público (Throttle 120/min, sem auth — tablet de totem não loga) recebe `{companyId, deviceId, tableNumber}`; `GET /totem/status` autenticado (`@CompanyId()`, ADMIN/MANAGER/SUPER_ADMIN) retorna lista + resumo online/total. Frontend: `/menu/[companyId]/page.tsx` gera `deviceId` estável via `localStorage("totem_device_id")` (crypto.randomUUID) quando `isTotem`, envia heartbeat a cada 30s com `keepalive:true`; widget "Totens Conectados" adicionado em `ImpressaoLocalTab.tsx` (mesma aba do agente de impressão — consistente como "monitoramento de hardware"). `tsc --noEmit` + `npm run build` limpos em ambos; `Order.cashId` validado via `information_schema`/`pg_constraint` local; `TotemService` heartbeat validado via smoke test direto (`node -e`) confirmando isolamento por `companyId`. **MDM (Scalefusion/Esper) explicitamente fora de escopo** — contratação de terceiro pra travar Home/instalar remoto, não é código. Nenhum commit/push feito ainda — 4 migrations locais aguardando deploy.

**Continuação — impressão seletiva por forma de pagamento + cupom de auditoria (NÃO deployado)**: diagnóstico prévio confirmou 2 gaps reais: `Company.printingSettings` (JSON, já existe, item 111) não tinha regra de forma de pagamento; e a decisão de imprimir é 100% client-side (`PrintRouterService`) — a fila `PrinterJob`/`enqueueJob` (Fase 2, item 87) existe no banco mas nunca é chamada por ninguém, é infraestrutura morta. Optado pelo "caminho rápido" (mantém `window.print()`, filtro no cliente) em vez de ativar a fila real (só compensaria pra quem já usa o Printer Agent físico). **Zero migration** — `printingSettings` já é JSON opaco (`@IsObject()` no DTO, sem schema rígido). (1) `ImpressaoTab.tsx`: `PrintingSettings` ganha `printMode: "ALL"|"SELECTED"` (default `"ALL"` preserva 100% do comportamento atual) + `printPaymentTypes: string[]`; nova seção "Cupom do Cliente" com toggle Sempre/Seletivo + checkboxes por forma de pagamento (`PAYMENT_TYPE_OPTIONS` exportado). Merge `{...DEFAULT_SETTINGS, ...saved}` já existente garante que lojas com `printingSettings` salvo ANTES desta mudança não tenham a chave `printMode` sobrescrita (spread só sobrescreve chaves que existem no objeto salvo). (2) `PrintRouterService.route()`: novo `opts.printPaymentRule?: {mode, types}` — pula ticket `CASHIER` quando `mode==="SELECTED"` e `order.paymentMethod` não está na lista (regra ortogonal ao `channel==="TOTEM"` já existente, ambas compostas num único `cashierAllowedByPaymentRule`). (3) `app/pdv/page.tsx`: novo `useEffect` busca `GET /company/settings` no mount, guarda `printPaymentRule` em state, passa pro `PrintRouterService.printAll()` do auto-print pós-pagamento. **Reimpressão manual (botão "Reimprimir") NÃO foi alterada** — a regra só se aplica à impressão automática; clique explícito do operador sempre imprime (decisão deliberada). (4) **Cupom de Auditoria** ("dica de ouro" do diagnóstico): `CashService.auditSummary(cashId, companyId)` — `Order.groupBy(['paymentMethod'], where:{cashId,...})`, exclui `CASH` (já está na gaveta física, não precisa conferir), soma total+contagem por forma de pagamento não-dinheiro. `GET /cash/:id/audit-summary` (ADMIN/MANAGER/SUPER_ADMIN). Frontend `TabCaixa` (financeiro): botão "Cupom de Auditoria" no card do caixa aberto + ícone de impressora em cada linha do histórico — abre popup com ticket 80mm (CSS inline, sem dependência de `printTicket.ts` pra não acoplar módulos) mostrando só cartão/PIX/transferência + total. `tsc --noEmit` + `npm run build` limpos. `auditSummary` validado via `groupBy` real no Postgres local (PIX 50+30=80/2 pedidos, CREDIT_CARD 80/1, CASH 40 excluído do total não-dinheiro — resultado bateu exato). **Ainda NÃO implementado (fora do "caminho rápido")**: ativar a fila `PrinterJob` real pra quem usa o Printer Agent físico (só filtro client-side por enquanto); Offline-First PWA (IndexedDB) — avaliado e recomendado NÃO fazer a menos que uma loja específica tenha problema real e recorrente de internet (custo arquitetural alto: IDs client-side, fila de sync, conflitos de estoque/preço, tela de "pendente de confirmação" — não é patch). Nenhum commit/push desta sessão foi feito.

**Continuação — trava de caixa ESCOPADA (403 só PDV+CASH, NUNCA iFood/WhatsApp) — implementado após push-back técnico**: recomendação externa pediu bloqueio de venda em TODOS os canais (iFood/WhatsApp/Tablet) quando `Cash.isOpen=false`. Análise identificou 2 problemas reais em seguir isso literalmente: (a) confunde concorrência de estoque (já resolvida via transação `Serializable` em `updateStatus`, não relacionada a caixa físico) com contabilidade de gaveta física; (b) bloquear iFood quebra o SLA de aceite do parceiro (pedido cai o app deles, penaliza a métrica de aceitação da loja) e bloquear WhatsApp anula o modo de venda 24/7 da Kely — nenhum dos dois passa por gaveta física. Confirmado no código: `WhatsappAiService.createOrderFromCart` e `IntegrationsService.processWebhook` já chamam `ordersService.create()` (mesmo serviço do PDV — "fila única" do ponto 2 da recomendação **já existia**). `IfoodProvider.mapPaymentMethod`/`MockProvider.mapPaymentMethod` PODEM retornar `'CASH'` (cash-on-delivery é real no iFood) — por isso o gate não pode travar por `paymentMethod==='CASH'` sozinho, precisa também excluir canais externos. **Implementação**: `OrdersService.create()` (`orders.service.ts`) — no topo da função, ANTES de qualquer lookup de produto, consulta `Cash.isOpen` uma única vez (reaproveitada depois pro `cashId` de auditoria) e lança `ForbiddenException` (403) SE E SOMENTE SE `paymentMethod==='CASH' && (!channel || channel==='PDV') && !openCash`. `channel` explícito (`'IFOOD'`, `'MOCK'`, `'RAPPI'`) sempre passa direto — `IntegrationsService` sempre define `channel: providerName`. WhatsApp não define `channel` (cai no default `'PDV'`) mas seu `paymentMethodMap` só produz `pix/credit_card/debit_card`, nunca `CASH` — logo nunca aciona o gate hoje (documentado no código como risco latente caso isso mude no futuro). `OnlineOrdersService` (cardápio digital/Totem) usa serviço PRÓPRIO, não passa por este gate — decisão consciente, fora do escopo aprovado ("só PDV/balcão"). Fluxo "Lançar" (comanda contínua em `tables/page.tsx` e `garcom/mesa/[id]/page.tsx`) envia `paymentMethod:"PIX"` como placeholder enquanto a conta não fecha — confirmado que NÃO aciona o gate prematuramente; só o "Fechar Conta" final com Dinheiro selecionado aciona. PDV já exibe `error.response.data.message` em toast (6s) sem limpar o carrinho — UX de erro já correta, zero mudança de frontend necessária. **Validado com 5 cenários reais contra Postgres local** (PDV+CASH+sem caixa→bloqueia; IFOOD+CASH+sem caixa→permite; PDV+PIX+sem caixa→permite; WhatsApp+PIX→permite; PDV+CASH+com caixa→permite) — todos bateram. `tsc --noEmit` + `npm run build` limpos. Nenhum commit/push feito.

**Continuação — impressão em 4 setores reais (Bar/Pizzaria/Lanchonete/Caixa) a partir de UM pedido (NÃO deployado)**: zero migration — `Category.categoryType` já era `String` livre, só faltava reconhecer mais valores além de `"bebidas"`. (1) `frontend/components/printing/printDispatcher.ts`: `PrintSector` ganha `"PIZZARIA"` e `"LANCHONETE"` (união de 6 setores agora: KITCHEN/BAR/PIZZARIA/LANCHONETE/CASHIER/DELIVERY). (2) `KitchenTicket.tsx`: `buildKitchenTicket(order, sectorLabel = "COZINHA")` — parâmetro novo pro título do ticket (`<h1>`), reaproveitado pra Pizzaria/Lanchonete em vez de criar 2 templates novos do zero. (3) `PrintRouterService.route()`: classificação por `categoryType` vira 4 baldes (`"bebidas"`→BAR, `"pizza"`→PIZZARIA, `"lanche"`→LANCHONETE, resto→KITCHEN) em vez de 2; default `opts.sectors` (quando a página não filtra) passou a incluir os 6 setores — sem isso os tickets novos ficariam classificados certo mas nunca sairiam pra impressão (a whitelist de setores habilitados excluiria silenciosamente). (4) `app/categories/page.tsx`: `CATEGORY_TYPE_OPTS` ganha "Pizzaria" e "Lanchonete" (visual: botões + badge colorido + emoji no card); toggle "Multi-sabores" (ligado ao `PizzaBuilder`) que só aparecia para `categoryType==="normal"` agora **também** aparece pra `"pizza"` — extensão deliberada, não acidente, pra não capar a feature de sabores combinados quem migrar a categoria de pizza pra este novo tipo. (5) `app/kitchen/page.tsx`: `printKitchenOrder` — `sectors` hardcoded passou de `["KITCHEN","BAR"]` pra incluir `"PIZZARIA","LANCHONETE"` (senão o board da cozinha pararia de imprimir os 2 setores novos). `/orders/page.tsx` e `/historico-pedidos/page.tsx` não precisaram de mudança — já usavam o default de `route()`, que herda os 6 setores automaticamente. **Validado**: classificação testada com 6 itens sintéticos (1 bebida, 2 pizza, 1 lanche, 1 normal, 1 sem categoria) → 1 Bar / 2 Pizzaria / 1 Lanchonete / 2 Cozinha, bateu exato. `tsc --noEmit` + `npm run build` limpos. **Limitação conhecida e comunicada ao usuário**: isso resolve a DIVISÃO do pedido em tickets corretos (testável hoje via popup do navegador), mas o roteamento físico automático pra 4 impressoras de hardware diferentes depende da fila `PrinterJob`/Printer Agent (Fase 2/3), que continua **dormente** (`enqueueJob` nunca é chamado — achado da sessão anterior, não mexido aqui). A tela `/impressoras` (cadastro de hardware) continua só com 4 papéis antigos (KITCHEN/BAR/COUNTER/DELIVERY, enum Prisma) e não foi estendida — extender o enum sem ligar o dispatch criaria falsa expectativa. Hoje, "cada setor imprime na sua impressora" na prática depende de 4 terminais/navegadores abertos fisicamente perto de cada impressora (um por setor), ou de um único PC com as 4 impressoras cadastradas no Windows e o operador escolhendo a impressora certa em cada popup que abre.

**Continuação — reorganização da página `/demo` (NÃO deployado)**: usuário reportou "cliente se perde" ao entrar em `/demo`. Diagnóstico: a página tinha **10 seções sequenciais** (Header→Hero→Trust Metrics→Pillars/tabs→3-Phone Showcase→Como Funciona→Escolha Demonstração [13 pills de nicho + theme picker sempre aberto + 4 plan cards]→Testimonials→Comparison Table→Footer CTA) — a ação real (escolher nicho+plano+CTA) só aparecia depois de 4 blocos grandes de conteúdo de marketing, obrigando scroll longo antes de qualquer botão clicável relevante. Achado extra: `ALL_NICHES` tem **13** valores (Restaurantes/Pizzaria/Hamburgueria/Lanchonetes/Churrascaria/Hotdogs/Marmitarias/Padaria/Confeitaria/Pastelaria/Açaí/Conveniências/Mercados), não 4 — muito mais denso do que a percepção inicial. **Fix (só reorganização de JSX, zero rewrite de lógica/backend)**: (1) a seção "Escolha uma demonstração" foi movida pra logo após Trust Metrics — vira a segunda coisa que aparece na página, antes de Pillars/3-Phone/Como-Funciona (que continuam depois, intactos, como conteúdo de apoio pra quem rola mais); (2) reformulada como wizard de 2 passos explícitos — badge "Passo 1 de 2 · Qual é o seu segmento?" (as 13 pills, inalteradas) seguido de "Passo 2 de 2 · Escolha um plano pra testar" (4 plan cards); (3) o "Theme picker" (6 presets de cor) — que antes era uma caixa sempre aberta ocupando espaço fixo — virou um accordion colapsado por padrão (`showThemePicker` novo state), rotulado "🎨 Personalizar a cor da demonstração (opcional)", só expande se o visitante clicar. Nada foi removido — todo conteúdo de marketing (Pillars, 3-Phone Showcase, Como Funciona, Testimonials, Comparison Table) continua existindo, só reordenado pra depois da ação principal. `id="demos"`/`ref={demoSectionRef}` (scroll do botão "Testar Demonstrações" do Hero) movido junto — zero mudança de comportamento do CTA do hero, só passou a rolar uma distância menor. **Validado no navegador local** (não só tsc): fluxo completo testado via preview — Hero→Trust Metrics→Passo 1 (13 pills)→accordion de tema colapsado→Passo 2 (4 planos)→clique em "Testar Basic" abre o `LeadCaptureModal` normalmente→fecha→accordion de tema expande/mostra os 6 presets corretamente. Zero erros no console. `tsc --noEmit` + `npm run build` limpos. Nenhum commit/push feito. **Fora de escopo desta mudança** (confirmado com o usuário antes de mexer): Oficina/Estética/Moda são produtos SaaS **totalmente separados** (repos, bancos, deploys e domínios próprios — ver `reference_project_paths.md`/`project_multi_vertical_vps.md`) — a recomendação externa de "Module Registry + tenant_type + dynamic imports" partia da premissa errada de que é um monolito único servindo os 4 nichos; não implementado por não se aplicar à arquitetura real.

152. **fix(stock+smart-import): auditoria de estoque completa + correção do bug de match de nota fiscal + apelidos de ingrediente (DEPLOYADO, commits `5d7fe8bb`→`9a0e16b8`)**: sessão iniciada por relato do usuário — deu entrada de uma nota (65 itens, TOKO FRIOS) via Cadastro Inteligente e o estoque mudou mas nada apareceu em Movimentações. 5 correções encadeadas, todas com `tsc --noEmit` limpo e deploy real no VPS validado por `curl /api/health`. **(1) Edição manual de estoque sem rastro** (`ingredients.service.ts update()`) — `PATCH /ingredients/:id` sobrescrevia `stock` direto, sem `StockMovement`. Fix: transação que compara `previousStock` e cria `StockMovement` tipo `ADJUSTMENT` quando o valor muda. **(2) Matching de nome invertido no import de nota** (`smart-import.service.ts confirmInvoiceItems`) — comparava `ingredient.name.contains(item.name)`; nome da nota é sempre mais longo/ruidoso que o cadastrado, então quase nunca casava, e sem "Criar novo" marcado o item era **descartado silenciosamente** (sem erro, sem log). Fix intermediário: match reverso (`item.name.includes(ingredient.name)`) — **quebrou pior**: "Pimenta Calabresa", "Linguiça Calabresa Sereno" e "Calabresa Fatiada" caíram todos no mesmo ingrediente por conterem a palavra em comum, inflando estoque errado (+2 unidades de produtos diferentes no ingrediente "Calabresa"). **(3) Fix definitivo**: removido todo match fuzzy. Tela de revisão da nota (`InvoiceReviewTable`) ganhou **seletor de ingrediente explícito** por item — só pré-preenche sozinho quando o nome bate **exatamente** (normalizado: sem acento, case-insensitive); campo `skipped[]` retornado pelo backend + toast listando itens sem match, em vez de sumir. **(4) Bloqueio de nota duplicada** — `confirmInvoiceItems` busca `StockMovement` existente com `reason` contendo `NF nº {docNumber}` (extraído pela IA); se achar, retorna `{duplicate:true, existingDate}` sem criar nada; frontend mostra `window.confirm` explicando o risco antes de permitir reenvio (`force:true`). **(5) Relatório de Movimentações** — `GET /stock/movements` ganhou filtros `from/to/type`; `/stock` (frontend) ganhou esses filtros + coluna "Nota/Motivo" (agora populada com `NF nº X — Fornecedor (data)` em vez de texto genérico) + botão "Imprimir/Exportar PDF" (monta HTML limpo, `window.print()` — sem lib nova, mesmo padrão dos tickets). **(6) Apelidos de ingrediente** (`IngredientAlias` model novo, migration `20260720000000_add_ingredient_alias` idempotente) — quando o usuário vincula manualmente um item de nota a um ingrediente com nome diferente e marca "lembrar", grava `{companyId, alias: nomeDaNota, ingredientId}`; da próxima vez (nessa nota ou qualquer outra, de qualquer fornecedor) o texto já vem pré-selecionado. Matching continua sendo SEMPRE igualdade exata (nome oficial OU apelido salvo) — nunca aproximação, mantendo a lição do item (2)/(3). `GET /ingredients/aliases` novo endpoint. **Limpeza de dado corrompido**: os testes desta própria sessão (antes do fix 3) já tinham gravado 4 ingredientes/6 movimentações errados na empresa matriz "Ruffinu's Pizzaria" (`cmq7d3dxs0006gw5pabsljy87`) — confirmado **zero** `RecipeItem` referenciando (loja ainda não abriu, cardápio cadastrado mas sem receita vinculada) via `docker exec foodsaas-backend-backend-1 node -e` (Prisma direto, sem tocar senha do Postgres — o classificador do Claude Code bloqueou `cat .env` por conter credencial, rota alternativa usada). Apagado via `stockMovement.deleteMany` + `ingredient.deleteMany` scoped por `companyId` — **0 receitas afetadas, catálogo/produtos intactos**. **Gotcha de shell**: `!` dentro de aspas duplas no bash dispara *history expansion* (`!c` virou o último comando começando com "c") — corrigido usando aspas simples no `docker exec ... node -e '...'` com JS usando `==null` em vez de `!x`. **Achado de estilo (não é bug)**: regex com caracteres Unicode combinantes literais (range U+0300–U+036F escrito como caractere real, não como `̀-ͯ` escapado) já é usado em produção em `slugify.ts` e `whatsapp-ai.service.ts` — funciona idêntico à forma escapada; usei a forma escapada (`new RegExp("[\\u0300-\\u036f]")`) nos arquivos novos desta sessão só por clareza de leitura, sem necessidade de "corrigir" os existentes. Deploy: `docker compose build --no-cache backend` no VPS, procedimento padrão do item 143, verificado por `grep -c <marcador> dist/...js` antes de subir em cada um dos 4 rebuilds desta sessão.

153. **feat(menu): produtos promocionais e em destaque — card estilo iFood/Rappi (DEPLOYADO, commit `0065c1c8`)**: usuário mandou 3 screenshots de apps de delivery (iFood pizzaria, Rappi hamburgueria, app nativo genérico) mostrando carrosséis "Ofertas"/"Destaques" e pediu um "modelo campeão" sintetizando os 3. Depois refinou: quer um seletor de rótulo no cadastro do produto (Sem rótulo/Destaque/Novidade/Recomendado — igual print de referência de um 4º app), não um simples toggle. **Schema**: `Product.originalPrice Decimal?` (preço riscado, opcional — vazio = sem promoção) + `Product.featuredLabel String?` (`null|"DESTAQUE"|"NOVIDADE"|"RECOMENDADO"`); `Product.isFeatured` (já existia desde `20260530000000_add_sort_order`, nunca fora exposto em lugar nenhum — achado do Explore: sem DTO, sem UI, sempre `false`) passa a ser **derivado** de `featuredLabel` no service (nunca gravado direto pelo cliente, elimina risco de dessincronia). Migrations `20260720100000_add_product_original_price` + `20260720110000_add_product_featured_label`, ambas `ADD COLUMN IF NOT EXISTS` idempotente. **Backend**: `CreateProductDto` ganha `originalPrice`/`featuredLabel`; `ProductsService.create/update` gravam os 3 campos (`isFeatured = !!featuredLabel`); `publicMenu()` não precisou de mudança (sem `select`, todo campo escalar já passa). **Admin** (`app/products/page.tsx`): `FEATURED_LABEL_OPTIONS` (4 opções) + `MoneyField` "Preço original (promoção) — opcional" adicionados nos formulários de criar E editar produto (fora do bloco `!isFormPizza`, então aplica a pizzas também). **Cardápio digital** (`app/menu/[companyId]/page.tsx`): `discountPercent(product)` calcula % só quando `originalPrice > salePrice` (guard contra divisão/valores inválidos). Novo componente `FeaturedProductCard` extrai e substitui a marcação **duplicada 2x** (antes/depois de categorias, mesmo JSX colado) da seção "Destaques" — badge desconto (verde, `-X%`) OU rótulo (laranja/azul/verde por tipo) no canto superior-esquerdo da imagem, botão "+" circular flutuante no canto inferior-direito (cor da loja), preço riscado + promocional quando aplicável. `featuredProducts` trocou de `(p as any).isFeatured === true` para `!!p.featuredLabel` (fonte única, sem cast). Mesmo tratamento (badge + preço riscado) aplicado ao card "estilo app" (`layoutType === "LIST"`) que já tinha um badge "Destaque" fixo hardcoded. **2 bugs pegos na auto-revisão pós-implementação** (usuário pediu explicitamente "revise o arquivo" após toda correção): (a) botão "+" posicionado com `-bottom-2.5` (pra fora da borda da imagem) ficaria **cortado** pelo `overflow-hidden` do card — corrigido pra `bottom-1.5` (dentro da área da imagem); (b) `{discountPercent(product) && (<span>...)}` — `&&` com número: se o desconto arredondar pra `0%` (preços muito próximos), renderiza o **literal "0"** na tela (footgun clássico do React) — trocado por ternário `? … : null`. `tsc --noEmit` limpo (backend+frontend) + `npm run build` completo do frontend (59 rotas, incluindo `/menu/[companyId]` e `/products`) rodado depois dos fixes, sem erros. Deploy VPS confirmado via `curl /api/health` + `grep -c featuredLabel dist/modules/products/products.service.js` (5 ocorrências) antes de subir. Frontend (Vercel) deployado automaticamente no push. **Não implementado** (fora do pedido): seções separadas "Ofertas" vs "Preferidos" (as 3 referências tinham seções distintas; optei por uma única "Destaques" melhorada, já que o pedido final do usuário foi sobre o rótulo do produto, não sobre arquitetura de múltiplas seções); badges de ranking numerado (1º, 2º mais pedido) — precisaria de agregação de vendas, não implementado.

154. **feat(sidebar+trafego-pago): seções recolhíveis + painel de dados para anúncios manuais (DEPLOYADO, commits `29f77aaf`, `d367cabf`)**: usuário mandou print do 99Food/Brendi — seções da sidebar deles têm seta expandir/recolher (a nossa sempre mostra tudo, ~27 itens em 8 seções, ficando comprida) — e print de um card "Tráfego Pago" deles (ativar anúncio Facebook/Instagram em 10min, com dados de pedidos/ROAS). Pediu os dois: sidebar recolhível + oferecer tráfego pago com dados de produto mais pedido + página mais visitada, como "conjunto de configurações completas", não uma página solta. **Decisão de escopo (perguntei antes de construir)**: o 99Food consegue ativar anúncio real porque são Parceiro de Marketing Meta aprovado (acesso à Ads API `ads_management`, exige aprovação externa do Meta Business — semanas, verificação de empresa — não é código). Ofereci 2 caminhos via `AskUserQuestion`; usuário escolheu o **painel de dados pra anúncio manual** (não a infra de OAuth+self-service, que ficaria semi-funcional até aprovação externa). **(1) Sidebar recolhível** (`ClientShell.tsx`): `collapsedSections: Set<string>` persistido em localStorage (`sidebar_collapsed_sections`, mesmo padrão de `products_collapsed_cats`); título de cada seção virou `<button>` com `ChevronDown` que gira -90° quando recolhido; `hasActiveItem` calcula se a página atual está dentro da seção e **força expandida** mesmo se o usuário tinha recolhido antes (evita esconder a própria página aberta) — reaproveita a mesma comparação `currentHref === item.href` já usada pro estado "active" dos links, não introduz heurística nova. **(2) Painel Tráfego Pago**: `Product` mais pedido **já existia** pronto (`reports.service.ts getProductRanking` → `GET /reports/products`, usado em `/bi`) — reaproveitado sem mudança. "Página mais visitada" **não existia de forma multi-tenant** (só havia `PageVisit` sem `companyId`, exclusivo de páginas de marketing da própria plataforma, `super-admin`-only) — criado do zero: model `MenuAnalyticsEvent` (`companyId`, `type: MENU_VIEW|PRODUCT_VIEW`, `productId?`), migration `20260720120000_add_menu_analytics_event` idempotente; módulo `menu-analytics` — `POST /menu-analytics/track` público (Throttle 60/min, resolve slug-ou-id via `CompanyService.resolveId`) chamado pelo cardápio digital: 1x por carregamento de página (`MENU_VIEW`) + a cada clique num produto — `addToCart()` e `openFlavorModal(product)` (fluxo de pizza meio-a-meio, que **não** passa por `addToCart`, tinha ficado de fora se não fosse tratado à parte) — `PRODUCT_VIEW`; `GET /menu-analytics/summary?from=&to=` autenticado agrega contagens em memória (`Map`) e faz join com `Product` só pros top 10 IDs (produto deletado depois do evento é silenciosamente descartado da lista, sem quebrar). Nova página `/trafego-pago`: KPIs (visualizações do cardápio, visualizações de produto, total vendido), comparação lado a lado "Mais visualizados" vs "Mais vendidos", e um bloco **"Resumo pronto para anúncio"** — pega o produto #1 em vendas (fallback pro #1 em visualizações), monta um texto de copy pronto (headline + preço + CTA) com botão "Copiar" (`navigator.clipboard`) pra colar direto no Gerenciador de Anúncios do Facebook. Sidebar: item "Tráfego Pago" (`TrendingUp` icon) na seção IA, ao lado de "Marketing Digital" e "QR Recuperação". `tsc --noEmit` limpo (backend+frontend) + `npm run build` completo do frontend confirmando a rota nova compilando (59→60 rotas). Deploy VPS confirmado via `curl /api/health` + `grep -c menuAnalyticsEvent dist/modules/menu-analytics/menu-analytics.service.js` (3 ocorrências). **Não implementado (consciente, fora do escopo escolhido)**: qualquer integração real com a Ads API do Meta (OAuth, `ads_management`, criação/gestão de campanha via Graph API) — depende de aprovação externa do Meta Business, não é buildable numa sessão de código; exportação de lista de telefones de clientes como Custom Audience pro Facebook — feature separada e sensível (PII), mencionada mas não construída sem confirmação explícita adicional do usuário.

150. **feat(update-notices): aviso automático de atualização — WA + e-mail, máx 1x/dia (commit `21b842b7`, deployado e VALIDADO 07/07/2026)**: após cada deploy, avisa clientes e reconvida leads. **Mecânica**: Dockerfile gera `dist/BUILD_ID` (`date +%s`) no build; `UpdateNoticesService` (`OnApplicationBootstrap`, `src/modules/update-notices/`) aguarda `prisma.readyPromise`, lê o BUILD_ID e: builds já processados são ignorados (restart não reenvia); se já houve envio hoje (America/Sao_Paulo), registra o build como `skipped`; senão registra ANTES de enviar (crash-safe) e dispara. **Públicos**: clientes `ACTIVE` + trial vigente (`PENDING_PAYMENT` com `dueDate >= now`), excluindo `@foodsaas.demo` e `platform@foodsaas.internal` → WhatsApp ("sistema já atualizado automaticamente") + e-mail (template `SYSTEM_UPDATE` em notifications.service); leads (`Lead.whatsapp` preenchido, `status != PERDIDO`, número não pertencente a nenhuma Company) → WhatsApp com reconvite para `/demo`. Throttle 400ms entre envios. **Envio WA self-healing**: tenta env `EVOLUTION_*`; se falhar, cai para a `WhatsappConnection` ativa mais recente do banco (imune a env drift do item 149). **Model** `SystemUpdateNotice` (buildId @unique, sentAt, skipped, recipients, summary Json) + migration `20260707000000_add_system_update_notice` idempotente + `--rolled-back` no Dockerfile. **Endpoint manual**: `POST /api/update-notices/broadcast` (SUPER_ADMIN ou ADMIN da matriz — `isMatrixCompany`) ignora o limite diário. **Primeiro disparo real no boot**: `clientes WA=1 email=3 leads WA=3` (1 número deu 400 na Evolution — sem WhatsApp). **Deploy VPS via Chrome MCP**: sessões do terminal web morrem com poucos minutos de idle — SEMPRE mandar o deploy como UM comando encadeado logo após abrir a sessão (`exit` → sed/build/up em uma linha), e verificar marker com `grep -c` no ARQUIVO COMPILADO CERTO (`dist/modules/.../*.js`, NÃO `dist/main.js` — o build Nest não é bundle).
